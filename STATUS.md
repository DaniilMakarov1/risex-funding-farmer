# Current status

## Central baseline

- Git `main` is the exact accepted-history authority and is published to `origin/main` after every accepted Chief checkpoint.
- All three fixture lifecycle cores are integrated. Paper remains default; no venue is strategy-ready.
- User permanently authorizes bounded venue-local diagnostics, authenticated testnet access, wallet/account/API setup, faucet funding, product/economic selection, sequential testnet writes, and recoverable local operational-database recovery for RISEx, Nado, and Extended without repeated approval. These wallets are testnet-only; current spreads and minimum notionals are not blockers and have no fixed USD ceiling. Mainnet, real funds, strategy execution, and unrelated accounts remain separately governed.

## Venue readiness

### RISEx

- Accepted private-read evidence has complete counters, authoritative zero orders, and exact flatness; its historical run identities remain immutable.
- The accepted fixture lifecycle now binds `client_order_id` to composite, wide, and resting order identities, matching order/fill records, the exact cancel action/body identity split, and a fresh authoritative market position. It preserves durable no-replay and terminal zero-order/exact-flat barriers.
- The isolated Level C credential/signing/fixed-write-transport binding is accepted with exact signed-action/submitted-body linkage and remains absent from normal startup. A fresh pre-state gate stopped before network/auth on a noncanonical local lifecycle database; its terminal runtime row is immutable and no write was dispatched.
- The observed lifecycle-clear rejection now has an accepted distinct sanitized safety classification that persists before every public, credential, signing, private, and write effect and remains terminal across restart.
- Read-only recovery inspection found one obsolete empty three-column `cancels` table; integrity, identities, and zero intents/cancels were otherwise correct. A protected exact backup was verified before the table was transactionally replaced with the accepted canonical schema. `LifecycleClearBinding` now passes repeatedly, and the consumed private-read runtime database remained byte-identical.
- The fresh Level B run completed all nine first-barrier public requests, then stopped on an unclassified public-sweep semantic failure before source load, credential access, signing, or private dispatch. Its new runtime row is terminal and immutable; no retry or write occurred.
- Separate credential-free diagnostics showed the BTC/USDC BBO was not literally crossed, but its spread was about 18.83%. The user explicitly accepted arbitrary current testnet spread for the first bounded lifecycle because the purpose is execution-contract proof with test assets. The previous 0.30% maximum-spread blocker is removed only for this isolated RISEx testnet path; authoritative non-crossed BBO, exact grids/depth, price bounds, notional, identity, reconciliation, zero-order, and exact-flat safeguards remain mandatory.
- The accepted RISEx correction removes only the isolated testnet maximum-spread rejection; crossed-book, freshness, grid, depth, adverse price bound, notional, identity, reconciliation, zero-order, and exact-flat safeguards remain. Focused review passed 151 tests and the clean Python 3.11 full suite passed 1,923 tests.
- A fresh post-correction Level B passed completely: both nine-request public barriers agreed, every authenticated private-read/signing/capability phase completed exactly once, and authoritative orders/positions evidence was zero/flat. No write occurred.
- The sealed zero-argument Level C runner is accepted and integrated. On its first live invocation it durably dispatched one minimum-size `MARKET+FOK` opening intent once, but received no terminal order event and halted without replay. Two independent authenticated observations plus a full fresh public sweep then proved the nonce consumed, zero open orders, exact position zero, and no unrelated state. The protected local lifecycle database was backed up and recovered through the already-accepted expired/no-identity/zero-flat reconciliation rule to `COMPLETED_NO_FILL_FLAT`; no venue recovery write was required.
- A second sequential invocation again dispatched one minimum-size `MARKET+FOK` intent exactly once and reached the same authoritative no-identity, zero-order, exact-flat state, but a quiet receive timeout bypassed the first eight-frame absence fallback. After a protected backup, Chief used the accepted expired/no-identity recovery rule to record `COMPLETED_NO_FILL_FLAT`; no venue recovery write was required.
- The accepted RISEx correction now treats only an ordinary bounded receive timeout after validated initial zero-order and flat-position snapshots as event absence for the exact dispatched-once opening vector. Disconnect/reset, missing snapshots, identity, nonflatness, stale evidence, and contradictions remain manual recovery. The combined clean Python 3.11 tree passed 2,007 tests.
- The final authorized sequential invocation again dispatched exactly once and reconciled its intent to terminal zero fill/zero position, but a later final-barrier transport failure overwrote the operational outcome with manual recovery. Two fresh independent authenticated observations plus fresh public sweeps then proved nonce consumption, zero open orders, exact flatness, and no contradiction. After a protected backup, Chief restored only the outcome to `COMPLETED_NO_FILL_FLAT`; no venue recovery write occurred. Three safe no-fill invocations prove durable single dispatch and authoritative no-fill reconciliation but do not prove the filled-position cancel/close path; do not repeat this opening unchanged.
- The accepted fill-seeking correction changes only the opening to a price-bounded crossing `LIMIT+IOC` at the current ask-side adverse bound. Minimum size, depth/grid checks, durable identity, no replay, reconciliation, cancel/close, and final zero-order/exact-flat barriers remain. The combined accepted tree passed 2,019 tests in clean Python 3.11.
- Its fresh sequential invocation durably dispatched one `LIMIT+IOC` opening exactly once and reconciled it terminal with zero fill, no order identity, and zero position. A later final-barrier transport failure conservatively halted the local journal. Two fresh independent authenticated observations each proved the consumed nonce, zero open orders, exact flatness, and no unexplained state; after a protected backup Chief restored only the already-proven terminal outcome to `COMPLETED_NO_FILL_FLAT`, with no replay or recovery write. The current bound still did not prove a taker fill and must not be repeated unchanged.
- The accepted official-vector correction uses minimum-size `MARKET+IOC` with zero price ticks while retaining the prior BBO freshness, depth, grid, 30-bps bound, and notional preflight. It durably classifies accepted, terminal venue rejection, transport ambiguity, response ambiguity, and local failure; its clean Python 3.11 tree passed 2,025 tests and Chief independently reran 101 focused tests. The fresh invocation was accepted by the venue with an authoritative order ID and reconciled terminal, but again filled zero and left position zero. A later final-barrier transport stop was resolved by two independent consumed-nonce/zero-order/exact-flat observations and protected local outcome recovery to `COMPLETED_NO_FILL_FLAT`; no replay or recovery write occurred. RISEx place/signing/taker encoding is proved, but current testnet matching still has not produced a position to close.
- Current official integration documentation states that `price_ticks` remains the slippage bound for a market order. The accepted BUY `MARKET+IOC` vector instead sends zero price ticks, so its valid accepted order cannot cross any positive ask; this explains the repeated accepted zero fills without attributing them to absent matching. A fresh credential-free observation still found an active/unlocked market with the prior approximately 18.83% positive spread and ample minimum-size book depth. The next RISEx action is one narrow Builder correction binding the market-order price ticks to the already-validated adverse ask bound; the zero-price vector must not repeat.
- The accepted correction binds opening `MARKET+IOC` `price_ticks` through intent, packed action, signature body, and dispatch to the validated 30-bps ask-side adverse bound. Chief independently passed 72 focused tests, the clean Python 3.11 full suite passed 2,052 tests, and dependency/compile/diff checks were clean. One fresh Level B proved agreeing public barriers, zero orders, and exact flatness before the only post-correction write. The venue accepted the exact minimum-size opening once with an authoritative order ID at bound `85817.6`, but reconciled it terminal with zero fill and zero position; two independent post-state observations proved consumed nonce, zero orders, exact flatness, and no unexplained state. A byte-identical pre-recovery database with SHA-256 `a4001dd0aea2d92edd821d1fd735e0e9149d48ac599c1471bb3f3e41cd192902` was preserved before Chief restored only the already-proven outcome to `COMPLETED_NO_FILL_FLAT`; the recovered active database SHA-256 is `e79a4c4fa0577a2e77b10a71c8d31b0b23cb4ccc2ff7b245d6d584b8b17e606f`. No replay or recovery write occurred. Correct market-price-bound encoding is now proved, so the remaining RISEx blocker is executable testnet matching/liquidity on the selected product, not signing or price encoding.
- The accepted fixed-product correction binds the sealed lifecycle to ONDO/USDC market 29 with exact current tick `0.00001`, step `0.1`, and minimum `25`; its synchronized tree passed 654 focused and 2,090 full tests, and Chief independently passed 154 key tests. The prior BTC database was recoverably archived byte-identically with SHA-256 `e79a4c4fa0577a2e77b10a71c8d31b0b23cb4ccc2ff7b245d6d584b8b17e606f`. Fresh ONDO Level B stopped before signature/write because current official market data reports unused `high_24h=0` and `low_24h=0`, while index/mark/last, open interest, volume, grid, live book, and all identity responses are valid. This is a narrow local statistics-classification defect; execution-critical positivity, BBO/depth, price-bound, identity, zero-order, and exact-flat gates remain unchanged.
- The accepted statistics correction treats only finite `high_24h`/`low_24h` as nonnegative while index/mark/last/max-position remain strictly positive; its clean Python 3.11 tree passed 2,105 tests and Chief independently passed all 15 adverse regressions. The next fresh Level B completed all 18 public reads with two semantically agreeing sweeps but stopped before private nonce/signature/write because the final aggregate gate re-aged the earliest individually fresh response after the entire sequential sweep exceeded five seconds. Per-response freshness had already passed at consumption. This is one local timing contradiction, not venue state or transport failure.
- The accepted sequential-freshness correction retains the five-second check at each response and at the exact private barrier while removing only redundant end-of-sequence re-aging; its clean Python 3.11 tree passed 2,107 tests. A fresh public/private gate then reached nonce/sign/auth but the private snapshot stream timed out; a separate production-capability observation proved fixed ONDO market 29, repeated zero orders/position, and no unexplained state. Exactly one sealed ONDO write was accepted with an authoritative order ID, terminally reconciled once at 25 units and bound `0.50251`, and filled zero. Two independent official post-state sweeps proved zero orders/positions and consumed nonce index 4; no cancel/close/replay occurred. The protected pre-recovery database SHA-256 is `4f80434ea4646e6b20e0b925a62257fbefe05b97e3ebe91a2036954aee8ba274`; only the proven outcome was recovered to `COMPLETED_NO_FILL_FLAT`, active SHA-256 `b13f2aecfc03749c98c26d102d91b8da1088300ba3dbc951a9686d68cd77ef16`.
- Public trade history explains why ONDO book depth did not prove executable matching: its latest trade was about 31 hours old. A bounded current activity scan found ETH/USDC market 2 materially stronger evidence: latest trade about 2.2 hours old, two asks, best ask quantity `13.508` against minimum `0.1`, exact tick `0.01`, and step `0.001`. This materially changed product evidence permits one fixed ETH correction; ONDO must not repeat.
- The accepted fixed ETH correction binds market 2, `ETH/USDC`, tick `0.01`, step `0.001`, and minimum `0.1`; its clean Python 3.11 candidate passed 759 RISEx-local and 2,108 full-suite tests, and Chief independently passed 14 key identity/vector regressions. Fresh authoritative pre-state proved zero orders, exact flatness, no unexplained state, and nonce `(1,4)`. Exactly one sealed ETH `MARKET+IOC` write at bound `2607.66` was accepted with order ID `0x00000000000339b4000000000323657f0000000000000022`, reconciled terminal with zero fill and position zero, and consumed nonce index 5. Two independent final observations proved zero orders, repeated exact flatness, and no unexplained state. The byte-identical pre-recovery database SHA-256 is `9108b2f0730d069fe8aadcedef69c718b975511f3f12e7405eaf380b1a89af63`; Chief restored only the proven outcome to `COMPLETED_NO_FILL_FLAT`, active SHA-256 `3c22f856084541e1679adc537a9c1417ff2b2c5d5cee089873f025d37c6f55f6`. No cancel, close, replay, or venue recovery write occurred. BTC, ONDO, and now ETH all accepted the corrected wire identity but zero-filled; RISEx is blocked on current testnet matching until materially new execution evidence appears.
- A fresh credential-free all-market/book comparison found BTC/USDC still represented by one ask order at the same approximately 18.83% spread, while unlocked ONDO/USDC market 29 had five ask orders and materially broader aggregate depth. This is changed product-liquidity evidence and permits one narrow fixed-product candidate; do not repeat BTC unchanged or build a generic product framework.
- With the user's explicit authority to change safety invariants, the stale durable `MARKET+FOK` wording was corrected to the already-accepted official `MARKET+IOC` opening contract and one fixed Chief-selected unlocked testnet product with materially better observed depth. IOC identity/reconciliation/close/final-flat safeguards remain; dynamic selection, paper changes, and mainnet execution remain out of scope.

### Nado

- The fixed identity is securely provisioned; Ink Sepolia gas and test USDT0 collateral are available, with exactly 10 test USDT0 deposited.
- A fresh credential-free official catalog observation completed on its first transport attempt. Its complete uniform spot/perpetual structure exactly matches the accepted parser and fixtures, so no catalog code change is required.
- Runtime run identity is accepted. The latest Level B run stopped on public `all_products` before credential access; its historical runtime row remains immutable.
- The accepted private-read report now durably distinguishes sanitized `TRANSPORT`, `HTTP`, `SCHEMA`, `AUTH`, `IDENTITY`, and `SAFETY` failures without raw bodies, secrets, signatures, or account identity.
- A fresh credential-free `all_products` diagnostic received a valid transport/envelope but stopped because current product IDs are not contiguous. Official documentation treats the returned IDs as the valid set and does not require contiguity, so this is an observed Nado-local parser defect, not a rate limit or credential failure. No credentials, signing, private dispatch, or write occurred.
- The accepted Nado correction now treats unique non-negative product IDs returned by official `all_products` as the authoritative sparse set while retaining exact collateral, duplicate, malformed, account-coverage, cross-response, counter, signing, and no-rearm safety checks.
- The next fresh Level B run passed contracts, status, sparse `all_products`, and linked-signer public checks, then stopped with sanitized `SCHEMA` on public `subaccount_info`. Credential loader, derivation, signing, private trigger dispatch, and writes remained unused; its runtime row is terminal and immutable.
- The first separate credential-free schema diagnostic confirmed the failure is in the public-response prefix but did not retain enough bounded envelope evidence to identify the exact field mismatch. That diagnostic is terminal and will not be replayed; one narrower envelope-shape observation is required before deciding whether code is defective.
- The narrower observation stopped deterministically because the current public `subaccount_info` response exceeded the accepted 65,536-byte transport ceiling before a complete body could be retained. Official documentation confirms this response includes account balances plus complete spot and perpetual product structures. This is an observed Nado-local response-size compatibility defect, not credentials or rate limiting; the basic query has weight 2.
- The accepted Nado correction gives only `subaccount_info` a finite 1,048,576-byte ceiling; all unrelated responses retain 65,536 bytes and strict JSON, fixed-host, timeout, redirect, freshness, and fail-closed protections. Focused review passed 243 tests and the synchronized clean Python 3.11 suite passed 1,898 tests.
- The next fresh Level B run again completed four of five first-round public requests, then stopped on public `subaccount_info`, now as sanitized `SAFETY` rather than response-size `SCHEMA`. Credential loading, derivation, signing, private trigger dispatch, and writes all remained at zero; its fresh runtime row is terminal and immutable.
- The separate credential-free diagnostic classified the predicate as `embedded product catalog disagrees`. Both observations contained the same aggregate 90-product coverage (15 spot, 75 perpetual), complete matching-size balance vectors, and three health rows; the account response had 175 health-contribution rows because official semantics index that vector by sparse product ID rather than by product count. The current check hashes whole product objects across sequential responses, including live state/book/oracle fields, so ordinary market movement creates a false mismatch before the already-visible sparse contribution-vector incompatibility.
- The accepted Nado correction validates each complete snapshot strictly, compares stable identity/kind/coverage/risk/config across responses while tolerating oracle/state/book drift, and validates contributions through the maximum sparse product ID with exact zero triples in unused slots. Independent focused review passed 252 tests and the clean Python 3.11 full suite passed 1,907 tests.
- The next fresh Level B run still completed four of five first-round public requests and stopped with sanitized `SAFETY` on public `subaccount_info`; credential, derivation, signing, private trigger, and write counters all remained zero. Its new runtime row is terminal and immutable, so the next remaining account predicate must be classified separately.
- The separate current-state diagnostic then passed the accepted account validator completely: stable catalog equality, 15/75 complete balance vectors, collateral above floor, zero non-collateral spot balances, zero perpetual amounts/vQuote, three health rows, and 175 sparse contribution slots. The preceding `SAFETY` was therefore transient cross-snapshot inconsistency; current external state has materially changed and no further code defect is evidenced.
- A fresh Level B using the full public sequence nevertheless reproduced the same fifth-request `SAFETY` with every credential/sign/private/write counter still zero. The contrast with the immediately adjacent two-request diagnostic shows a field classified as stable is changing during the longer contracts/status/catalog/linked/account sequence; do not retry Level B until that field is identified.
- The exact full-prefix diagnostic proved product IDs and kinds remained identical while 12 of 90 products changed only live fields: spot/perpetual `oracle_price_x18`, selected `state` fields, and `risk.price_x18`. The validator already tolerates oracle/state drift but still includes the strictly validated positive `risk.price_x18` in its cross-snapshot stable hash, causing the repeated false mismatch.
- The accepted Nado correction keeps `risk.price_x18` mandatory, canonical, and positive in every snapshot but excludes only that observed live value from the stable cross-snapshot hash; risk weights and spot configuration remain stable inputs. Independent focused review passed 268 tests and the clean Python 3.11 full suite passed 1,923 tests.
- The next fresh Level B passed the five-request public account prefix for the first time, then stopped with sanitized `SCHEMA` on the first public `subaccount_orders` request for product 0. Credential loader, derivation, signing, private trigger, and write counters all remained zero; its runtime row is terminal and immutable.
- The separate orders diagnostic stopped earlier than `data` validation with `wire envelope schema mismatch`, proving current `subaccount_orders` uses a top-level envelope different from accepted `{status,data,request_type}`. That observation is terminal; no order content or identity was retained.
- The narrower envelope observation showed this is the venue's explicit error shape `{status,request_type,error_code,error}`, not a successful response-shape drift. The API is rejecting `subaccount_orders` for collateral product 0; the runner currently misclassifies that semantic API rejection as `SCHEMA`.
- The bounded error diagnostic classified venue error code `2015`: no market exists for the supplied product/ticker ID. Product 0 is the fixed USDT0 collateral product and must remain in account/balance safety coverage, but it is not a market and must not receive a `subaccount_orders` market query. This is not authentication or rate limiting.
- The accepted Nado correction retains product 0 in every collateral/account safety vector, excludes it only from market-order queries, and queries each non-collateral product exactly once per public round. Independent focused review passed 266 tests and the clean Python 3.11 full suite passed 1,921 tests.
- The next fresh Level B completed 12 of 13 attempted first-round public requests, then another non-collateral product failed at `subaccount_orders` with sanitized `SCHEMA`; every credential/sign/private/write counter remained zero. Product catalog membership is therefore not sufficient proof that a product has a market. Do not exclude IDs one by one or retry until an authoritative current market-bearing set is observed.
- The authorized credential-free market diagnostic showed the official V2 `pairs` set contains exactly all 89 non-collateral catalog IDs, but product 11 is the special `NLP_USDT0` liquidity-provider vault token rather than an orderbook instrument: its direct official `subaccount_orders` and aggregate `market_prices` queries both return venue error `2015`, while official NLP documentation defines mint/lock/burn vault-share behavior. This explains the 13th-request stop without credentials or quota exhaustion and provides a bounded Nado-local correction: preserve product 11 in complete account safety coverage but exclude it, like collateral product 0, only from regular orderbook-order queries.
- The accepted Nado correction excludes fixed testnet product 11 only from regular orderbook-order queries while preserving complete catalog/account/balance/health coverage; focused review passed 268 tests and the clean Python 3.11 full suite passed 1,923 tests.
- The fresh post-correction Level B completed all 94 first-round public/account/order requests and exact fixed owner capability loading/derivation, then stopped before signing or private trigger dispatch on `server_time` with sanitized `SCHEMA`. A separate credential-free observation showed the valid current time envelope is `{status,method,id,server_time}` with additive `id:null`; the parser requires the older exact three-key shape. This is a narrow Nado-local additive-response compatibility defect, not credentials, rate limiting, or a semantic time failure.
- The accepted time-envelope correction allows only the observed additive `id:null` while retaining exact required semantics and strict rejection of every other addition/value; its clean Python 3.11 full suite passed 1,934 tests.
- The next fresh Level B completed all 94 round-A requests, capability load/derive, fresh server time, signing/recovery, private trigger zero-order read, and all 95 round-B requests. It then stopped with sanitized `SAFETY` because the two sequential 88-market order sweeps took 47,621 ms after round-A evidence against the 30,000 ms temporal barrier; every individual response completed and no write occurred.
- Official current orders documentation defines a multi-product `type: orders` query. A credential-free observation derived the exact current catalog set, excluded only fixed non-orderbook products 0 and 11, and received one successful `query_orders` response covering all 88 requested product IDs exactly once with zero nonempty order lists. This is the authoritative bounded fix for the temporal incompatibility; the earlier manually composed diagnostic that accidentally included nonexistent product 12 failed with venue `2015` and is not contract evidence.
- The accepted aggregate-orders correction performs one exact complete 88-product `query_orders` check per round and preserves complete account/health coverage; focused review passed 290 tests and the clean Python 3.11 full suite passed 1,947 tests.
- The fresh post-correction Level B finalized successfully: round A used 7 completed requests, fixed owner capability load/derive, server time, signing/recovery, and private trigger zero-order read each completed exactly once, round B used 8 completed requests, and both stable fingerprints agreed for all 90 account products. No write occurred.
- The sealed zero-argument Nado Level C runner is accepted and integrated with durable pre-dispatch identity, exact place/cancel payloads, aggregate account/order/trigger evidence, bounded compressed transport, IOC close attempts, and terminal zero-order/exact-flat barriers. The combined accepted tree passed 1,992 tests in the project-compatible isolated Python 3.11 environment.
- Its first live invocation stopped before preparing an intent or dispatching a venue write. A fresh standalone accepted Level B immediately finalized with all counters, proving the key/owner/private trigger capability is valid. The runner now reuses that accepted 30,000 ms signed-read window while preserving the 100 ms write contract.
- The next invocation also stopped before preparing an intent or dispatching a venue write. The complete current 88-market catalog contains auxiliary products whose published minimum amount is not itself divisible by their step; the target product remains aligned. The accepted correction therefore validates catalog minima independently, enforces exact step alignment on the actual target opening amount and clamped closing amount, and preserves every target notional/safety check. The combined clean Python 3.11 tree passed 2,007 tests; the empty intent history permits one sequential retry.
- The retry again created zero intents and sent zero writes. One initial read-only observation had a transient transport failure and its single allowed retry passed; the exact terminal preflight then showed fixed product 2 currently requires a minimum 100-unit order at about USD 78,817 per unit, far above the project-wide USD 500 gate. This is neither credentials nor quota exhaustion. The current official catalog and V2 pair identity show multiple perpetual alternatives below USD 500, including product 20 `MON-PERP_USDT0` at about USD 2.90 minimum notional and product 30 `PUMP-PERP_USDT0` at about USD 0.45. Changing the fixed product/economics requires explicit user selection before a fresh Builder correction.
- The user removed the fixed USD ceiling for the named testnet wallets. The accepted current-market correction binds product 44 `SKR-PERP_USDT0` through V2 identity, signing, opening, reconciliation, cancel, close, and barriers, and computes the smallest step-aligned amount satisfying the venue's USD 5 minimum; the observed amount was 650 SKR at about USD 5.04.
- The first SKR invocation durably prepared and dispatched one post-only entry, but the execute response was not accepted by the runner and the intent halted `AMBIGUOUS`. Two post-window authoritative observations proved zero regular/trigger orders, zero positions, zero fills, and no unrelated state; exact `query_order` returned venue code 2020, order not found. After a protected backup, Chief used the existing completion barrier on a database copy and then the working database to recover the intent `RECONCILED` and lifecycle `COMPLETE`; no replay or recovery write occurred. A fresh visible Builder must preserve the execute failure class and use a taker opening rather than repeat the post-only path.
- The accepted taker correction uses product 44 IOC and durably distinguishes terminal venue rejection from transport/response ambiguity; its clean Python 3.11 tree passed 2,023 tests and Chief independently reran 393 focused tests. The exact-ask attempt and then the accepted tick-aligned 10%-buffered attempt each dispatched exactly once and received terminal venue rejection code 2011. The latter durable intent is `REJECTED`, its lifecycle is `HALTED`, and exact post-window evidence proves zero regular/trigger orders, zero positions, zero fills, and `query_order` code 2020/not found, so neither write can replay. The price bound is therefore excluded as the cause.
- Current official Nado semantics changed `min_size` to USDT0 denomination on 2026-01-29. The runner still interprets that field as a base-amount minimum and separately hard-codes a USD 5 minimum. Current `symbols` evidence says every live perpetual, including product 44 SKR, has `min_size = 100 USDT0`; the rejected SKR IOC was only about USD 5.55 and the account has about 10 USDT0 collateral. This is the concrete local contract-drift cause of the repeated rejection, not credentials, quota, spread, market status (`SKR-PERP` is `live`), or signing. Before another lifecycle, correct the minimum-size semantics and provision enough free testnet collateral for the smallest current venue minimum.
- Chief used the official Nado Python SDK 2.0.0 testnet-only path to mint 200 mock USDT0, approve exactly 200 USDT0, and deposit exactly 200 USDT0. Each accepted on-chain transaction has receipt status 1; an initial SDK nonce-cache failure on approve was complete `nonce too low`, so mint was not replayed and fresh clients completed approve/deposit. Authoritative engine state now reports collateral/assets/health about 210 USDT0, zero liabilities, and no position/order exposure. Funding is sufficient for the current 100-USDT0 minimum; no mainnet asset was involved.
- The accepted Nado correction now binds current `min_size` to `minimum_notional_x18`, uses `size_increment` as the one-step base floor, and derives the least step-aligned base amount satisfying the quote minimum at both entry and close bounds. The accepted 10%-buffered IOC, product identity, signing, durable intent/no-replay, reconciliation, bounded close, and final barriers are unchanged. Luna-max Builder evidence passed 109 focused, 34 adverse, and 2,038 full-suite tests; Chief independently reran the 109 focused tests before integration. The rejected buffered-IOC database was archived byte-identically with SHA-256 `5686e6bd06fd833a8f89bf6649e8f2aab2ffbf5ba61d2ad76070b6cb4c800c83`; no fresh lifecycle DB or write exists.
- Fresh accepted Level B evidence finalized twice with every counter exactly `1/1`, matching round fingerprints, and authoritative zero regular/trigger orders plus exact flatness. Two subsequent sealed Level C invocations each stopped before identity or intent preparation and sent zero writes, but the outer runner collapsed the cause to `OPERATIONAL_PREREQUISITE_FAILED`; each durable database retained only one `STARTED` runtime row with lifecycle `RUNNING`, zero intents/failures/fills/identity rows, and passing integrity. A bounded current-state diagnostic passed the fixed product-44 observation, 100-USDT0 quote minimum, 12,900-SKR step-aligned buffered order derivation at about 110.37 USDT0, entry preflight, owner signature validation, and temporary-store intent preparation. The unused databases were archived byte-identically with SHA-256 `bb130273299fe3df2536264b1dc0e9b3e1773d175c98507dd80d5eda1eddc14c` and `4093a6085f2cd56563902382cbd88b07bb76fc29cc30c31dea756075403bd3d1`. No venue write occurred in either invocation.
- The accepted reporting correction now durably terminalizes pre-intent failures as runtime `BLOCKED` plus lifecycle `HALTED`, retains a bounded class/stage, uses `UNEXPECTED_FAILURE` rather than misclassifying unknown exceptions, and preserves execute rejection/transport/response ambiguity behavior. Luna-max Builder evidence passed 53 focused and 2,049 full-suite tests; Chief independently reviewed the exact two-file diff and reran 54 focused tests.
- The next sealed invocation passed fresh Level B, preflight, signature, and durable preparation, dispatched exactly once, and received complete venue rejection code 2011. The database SHA-256 is `1dd5f71cca43d23ca374950611c342f60982c06dcced4d0bc51adea92f589459`; it durably records one `REJECTED` entry, `VENUE_REJECTION/2011`, runtime `BLOCKED/DISPATCH`, lifecycle `HALTED`, zero fills, and passing integrity. A fresh post-window Level B and separate digest-bound observer proved zero regular/trigger orders, zero matching orders/fills, and exact flatness, so the intent cannot replay. Official SDK 2.0.0 confirms the local IOC appendix value but defaults order receive-time nonce fencing to 90 seconds; the runner still uses 100 ms despite signing, durable fsync, and HTTPS dispatch before arrival. Because rejection 2011 persists after the quote-minimum/collateral correction, this is the concrete remaining Nado-local contract mismatch to correct before another lifecycle; the undocumented code itself is not treated as proof of the inference.
- The accepted SDK-conformance correction now uses the official 90,000 ms order receive window consistently for entry, cancel, and close while preserving nonce/digest identity and every write/reconciliation barrier. Luna-max Builder evidence passed 56 focused, 40 adverse, and 2,051 full-suite tests; Chief independently reviewed the exact two-file diff and reran all 56 focused tests. The preceding 100-ms rejection database was archived byte-identically with its recorded SHA-256.
- The one post-correction sealed invocation completed its embedded Level B with every counter exactly `1/1`, then stopped on a complete sanitized `HTTP` failure during the first Level C `LIVE_OBSERVATION`, before identity or intent preparation. Its database SHA-256 is `f1b663dc5dc2797500c4e390c10de5ddba975532516de54af7151d0f9957d332`; it contains runtime `BLOCKED/HTTP/LIVE_OBSERVATION`, lifecycle `HALTED`, zero intents/execute failures/fills/identity rows, and passing integrity. No venue write occurred. The complete HTTP failure is terminal under the Level B transport rules; do not retry Level C without materially changed endpoint evidence through a separately bounded diagnostic gate.
- A separately bounded read-only route/status diagnostic then completed the full authoritative observation with eleven HTTP 200 responses, including the trigger service, proving materially changed endpoint evidence. The zero-intent blocked database was archived byte-identically as `.risex-funding-farmer-nado-level-c-v1.live-http-blocked-20260826.sqlite3` with its existing SHA-256 `f1b663dc5dc2797500c4e390c10de5ddba975532516de54af7151d0f9957d332` before one fresh sealed attempt.
- That attempt filled one corrected-minimum 12,850-SKR IOC entry. A concurrent protective invocation observed the now-existing journal, sent no write, and halted it before the first process could prepare its close; authoritative evidence showed one entry fill, zero orders, and the exact nonzero position. Chief performed the authorized guarded local recovery from `HALTED` to `RUNNING` only after the entry was reconciled and the first process had exited, then dispatched exactly one new durable reduce-only close identity through the accepted runner primitives. The close filled exactly `-12,850 SKR`. The lifecycle is `COMPLETE` with ENTRY/CLOSE both `RECONCILED`, zero execute failures, two persisted fills, passing integrity, and database SHA-256 `5dafe4a6ce51f50c65d419edeca2a413675c554d17e6f52f48f28733617bdd77`. An independent final observation proved zero regular orders, zero trigger orders, zero isolated positions, and exact flatness. Nado Level C is complete.

### Extended

- The fixed identity and API-key capability are securely provisioned. The accepted account-shape witness completed `CAPTURED` with every counter exactly `1/1` and no write.
- The exact `{status, data}` parser correction is accepted. The latest private read completed its first REST pass, then the official account stream returned HTTP 503 before any frame; no write occurred.
- A fresh credential-free handshake confirmed that the old source-bound testnet stream host resolves and completes TLS but returns HTTP 503. Current official SDK configuration instead names `wss://starknet.sepolia.extended.exchange/stream.extended.exchange/v1`; its account path returned HTTP 403 without an API key, proving the current host is available and enforcing authentication. This is an observed Extended-local endpoint drift, not a rate limit; no credentials or frames were sent.
- The accepted Extended correction binds the current official testnet stream host and allocates a fresh durable runtime run ID in a protected multi-row journal while preserving historical rows, account/write identities, direct TLS, header-only API-key authentication, and normal-startup isolation.
- The next fresh Level B run completed credential load and all three first REST reads, then failed during the authenticated stream open before upgrade validation or any frame. The durable result is sanitized `UNEXPECTED_FAILURE`; no signing, account mutation, or write occurred, and the new runtime row is terminal and immutable.
- A separate authenticated handshake classified the current account-stream response as HTTP 403, not rate-limit HTTP 429. Official SDK source sends both `X-Api-Key` and an explicit SDK `User-Agent`; the accepted runner actually supplies only `X-Api-Key` while claiming both header names in evidence. This is a concrete implementation/attestation mismatch requiring one SDK-conformant handshake observation before any correction is opened.
- The SDK-conformant handshake, using the official `X10PythonTradingClient/2.5.0` user-agent form and the existing valid REST API key, still returned HTTP 403 before upgrade and sent no frame. The missing explicit user-agent is a local implementation/attestation defect but not the cause of the 403; the remaining blocker is external account-stream access/ingress authorization, not quota exhaustion.
- The accepted Extended correction now sends the official SDK user-agent explicitly with `X-Api-Key` and derives sanitized header-name evidence from the actual request. Focused review passed 205 tests and the synchronized clean Python 3.11 suite passed 1,898 tests. It does not clear the external HTTP 403 account-stream blocker.
- The user authorized a final detailed independent diagnosis. One exact official Python SDK 2.5.0 testnet account-stream open may be run from an isolated temporary environment with the existing protected REST-valid API key, receiving no application frame beyond the passive first observation and retaining only sanitized status/class evidence. This is to distinguish a local client-stack mismatch from provider-side key/account/ingress authorization; it does not authorize key rotation, reprovisioning, account mutation, or a write.
- The exact published SDK diagnosis is terminal and identifies an upstream configuration split. PyPI's latest official SDK 2.5.0 still configures `wss://api.starknet.sepolia.extended.exchange/stream.extended.exchange/v1`; with its declared unconstrained `websockets>=12` dependency it first fails locally against current `websockets` 17 because the SDK uses the removed `extra_headers` argument, and with compatible `websockets` 12 its exact `/account` open reaches the network but returns HTTP 503. The current official source host used by the accepted runner instead returns HTTP 403 with the same REST-valid key. No frame, account mutation, signing, or write occurred. This is provider/package and account-stream access inconsistency, not quota exhaustion or a remaining evidenced local header defect.
- The testnet-only Extended wallet was generated locally by Chief; it is not a user-provided wallet and will never hold real funds. Its protected L1 owner key was independently matched to the existing Extended client and its protected Stark identity matched the sole subaccount. Official SDK REST reads proved zero orders and zero positions. One authorized testnet claim completed and balance then became readable. One new API key was created and proved REST-valid but received the same account-stream HTTP 503, so it was not activated locally; the existing REST-valid key remains unchanged. Official stream RPC v2 returned HTTP 404. Wallet, balance, stale key, quota, and local headers are therefore excluded; the remaining v1 503/v2 404 is provider-side.
- The accepted Extended correction restores the current official SDK `api.starknet.sepolia.../v1/account` host and accepts optional/additive irrelevant stream fields while preserving required types, sequence, identity, and activity rejection. The combined accepted tree passed 2,019 tests in clean Python 3.11; one fresh Level B may confirm the current provider result.
- That fresh Level B completed all three first REST reads, then received no stream frame and failed during the WebSocket open; no mutation or write occurred. The runner conservatively persisted `BLOCKED`, but collapsed the handshake failure to `UNEXPECTED_FAILURE` instead of the required sanitized HTTP/transport class. Automated stream retries are stopped. Extended remains provider-stream blocked, and a fresh visible Builder may correct only the local failure classification without performing another authenticated attempt.
- The accepted reporting correction maps pre-upgrade WebSocket response/handshake failures to sanitized `HTTP` and connection/timeout/TLS/OS failures to `TRANSPORT`, persisting only that class with correct counters and redaction. Its clean Python 3.11 tree passed 2,022 tests and Chief independently reran 211 focused tests. No authenticated attempt was repeated; the provider-stream blocker remains.
- Current official SDK 2.5.0 source still publishes the same `api.starknet.sepolia.extended.exchange/stream.extended.exchange/v1` account-stream host, `X-Api-Key`, and SDK user-agent already used by the accepted runner; it also continues to publish the v2 RPC endpoint. No changed official endpoint or client contract justified another authenticated attempt. With REST identity/balance/orders/positions valid and the prior v1 HTTP 503 plus v2 HTTP 404 reproduced across keys, the remaining blocker was provider-side; at that checkpoint a REST-only substitution was still outside the governing barrier.
- The user explicitly authorized revising the safety invariant to clear this testnet blocker. Fresh credential-free probes isolated the venue failure: the official testnet `api.starknet.sepolia...` public BTC orderbook WebSocket returns HTTP 503, the documented non-api/CDN testnet host returns HTTP 403, and the same official public path on mainnet immediately returns a valid frame. The testnet failure therefore covers public ingress before API-key or account handling and is not caused by local credentials, Stark identity, account state, or private-frame parsing.
- Fresh authenticated testnet REST observations returned HTTP 200/`OK` for account info, open orders, positions, BTC trades, and BTC order history; orders, positions, trades, and history were all empty. Current official documentation provides exact order lookup by Extended ID and durable user external ID, and trade rows carry both identities. This evidence authorizes one Extended-testnet-only REST reconciliation fallback: two fresh agreeing bounded rounds must bind exact lifecycle identities, fills, zero open orders, and exact flatness and must reject unrelated state. It does not establish mainnet readiness; mainnet still requires its currently reachable private stream to pass.
- The accepted REST-fallback candidate preserves historical stream rows, uses explicit `connected=false`/`gap_free=false`, opens no socket, and binds two strict REST rounds to account/order/trade/position identities. Its synchronized Python 3.11 tree passed 2,071 tests; Chief independently reran all 19 fallback tests before integration. The first fresh production Level B loaded the correct identity and passed account info, then stopped on HTTP-200 `/user/orders` with sanitized `REST_WRAPPER_MALFORMED`; it made two REST calls, zero stream effects, and no write. Separate shape-only authenticated diagnostics proved both current empty `/user/orders` and `/user/positions` responses are exact `{status,data}` wrappers with empty arrays and no pagination member. This is one local empty-wrapper compatibility defect, not auth, transport, account state, or provider ingress.
- The accepted empty-wrapper correction permits absent/null pagination only for exact empty `OK` lists and still rejects nonempty lists without complete terminal pagination. Its clean Python 3.11 suite passed 2,078 tests and Chief independently passed all 26 focused fallback tests. One fresh production REST-only Level B then completed two agreeing account/orders/positions rounds: six HTTP reads, verified identity, zero orders/positions, zero stream effects, and durable terminal `READY` with `REST_FALLBACK_CONTRACT_PROVED`. Extended is ready for one separate sealed Level C operational binding; the provider-side testnet stream outage remains relevant only to mainnet-readiness hardening.
- The accepted Level C binding and subsequent narrow official-contract corrections are published at `c29569172045715d16070ceb84aff649ed850e96`. They validate the official SDK-derived Stark key, canonical decimal-string `l2Vault`, and exact single-market/fee/leverage REST lookup forms while preserving durable identity, no replay, strict nonempty reconciliation pagination, and final barriers. A fresh REST Level B then proved the current account identity and two agreeing zero-order/exact-flat rounds with no stream dependency.
- The next sealed Level C invocation stopped in `INITIAL_REST` before preparing any write identity. Its protected database has SHA-256 `3244bb477472f3bd37400080c3a96659e1c8fdae30afac77f5d11af2797cefe9`, runtime `BLOCKED/SCHEMA/INITIAL_REST`, lifecycle `HALTED_PAGINATION_SCHEMA`, zero intents, and passing integrity. A bounded authenticated shape-only diagnosis proved that current empty `/user/orders/history` and `/user/trades` responses use `{pagination:{count:0}}` without `cursor`; the local empty-page validator incorrectly requires both fields. This is a narrow Extended-local compatibility defect. No venue write occurred, and no lifecycle replay is authorized before one fresh Luna-max correction and acceptance.
- The count-only empty-pagination correction is accepted at `dceed3ba67f79f67b96fb0c2f39d5ca23183e395`; Chief independently passed 96 focused and 2,204 full-suite tests on Python 3.11/aiohttp 3.12.15. The preceding zero-intent database was archived byte-identically as `.risex-funding-farmer-extended-level-c-v1.blocked-empty-pagination-20260826.sqlite3` with unchanged SHA-256 `3244bb477472f3bd37400080c3a96659e1c8fdae30afac77f5d11af2797cefe9`. A fresh REST Level B then returned `READY` with verified identity, six reads, and two agreeing zero-order/exact-flat rounds.
- The following single sealed Level C invocation stopped in `INITIAL_REST` with `FRESH_REST_OBSERVATION_REQUIRED`, runtime `BLOCKED/SAFETY`, lifecycle `HALTED`, zero intents, and passing integrity; its database SHA-256 is `6f5545ecb98fb5348a5915f54bd82930dded84d9f2b88d60cccc922612d330e6`. No write occurred. A bounded timing diagnosis showed the ten required sequential REST reads take about 14.0 seconds and about 8.8 seconds elapse after the order book, exceeding the honest 5-second book freshness bound. The next correction must make all write-critical state genuinely fresh; it may reorder or split the observation but must not raise the bound, relabel timestamps, remove required reconciliation reads, or weaken durable identity/final barriers.
- The accepted freshness correction is published through `89c882cc467dc5d622a39089dc3386a66ecdff58`. It uses bounded parallel reads with per-response receipt timestamps and independent direct-TLS connections, rejects stale mutable state, and rechecks freshness before durable claim and again immediately before dispatch. Luna-max Builder evidence passed 98 focused and 2,206 full-suite tests; Chief independently passed the same 98 focused tests and a 2,206-test clean Python 3.11 tree with aiohttp 3.12.15. The preceding blocked database was archived byte-identically with unchanged SHA-256 `6f5545ecb98fb5348a5915f54bd82930dded84d9f2b88d60cccc922612d330e6`.
- A first operational launch used a clean review environment without the pinned optional Extended SDK and stopped before journal creation, intent preparation, authentication, or write. Its zero-byte file was archived with the empty-file SHA-256. After installing official SDK 2.5.0, its offline signing/key regressions passed and a fresh REST Level B again returned `READY` with verified identity and two agreeing zero-state rounds. The only post-prerequisite Level C invocation completed its fresh parallel observation, then stopped in `ENTRY_PREPARATION` with `PRICE_BOUND_INVALID`; database SHA-256 `6d9435bfd13978704303966d34ec316c40dcf3a7db57862fd7f08ced65b30a62`, lifecycle `HALTED_PRICE_BOUND_INVALID`, runtime `BLOCKED/SAFETY`, zero intents, and passing integrity prove no write or replay ambiguity.
- A bounded authenticated diagnostic observed current BTC-USD minimum `0.0001`, non-crossed executable BBO, ample top-level depth, a 1.45-second required-receipt span, and `limitPriceCap = limitPriceFloor = 0.05`. Official Extended price requirements define those fields as mark-relative ratios for `LIMIT` orders. The runner instead compares the absolute ask/bid directly to `0.05`; this is the concrete local blocker. One fresh Luna-max correction may change only this price-ratio validation while preserving the existing `LIMIT+IOC` lifecycle and every write/reconciliation/final barrier.
- The accepted price-ratio correction is published at `d539b1a9a852e75a488c6612e13f53aaff3e4079`; Luna-max Builder evidence passed 15 targeted, 338 focused, and 2,224 full-suite tests, and Chief independently passed 338 focused plus the 2,224-test clean Python 3.11 suite. A fresh REST Level B then returned `READY` with verified identity and two agreeing zero-state rounds.
- Exactly one post-correction Level C entry was durably dispatched and accepted as Extended order `2092620936462331904`, external identity preserved in the protected journal. Authoritative exact-ID, exact-external-ID, order-history, and trade observations all bind it as a fully filled BTC-USD BUY of `0.0001`; open orders are zero and the account has exactly one matching `LONG 0.00010` position. The runner halted `HALTED_NONEMPTY_PAGINATION_REQUIRED` during entry reconciliation and must never replay the entry. The active database has SHA-256 `3e25e4a9205b04e6c371e4384277d11419ed4b7f49d5dca970b6ac6abce454b5`.
- Official Extended documentation defines `GET /api/v1/user/positions` as returning all open positions, exposes no `cursor` or `limit` parameters, and shows the non-paginated general response; positions history is separately paginated. The local general-list validator therefore incorrectly rejects a valid nonempty current-position response. One fresh Luna-max correction may accept bounded nonempty no-pagination data only for current positions, while retaining exact account/market/side/size validation, unrelated-state rejection, and strict pagination for orders history, trades, and every endpoint whose contract is paginated. After acceptance, Chief must reconcile the existing entry and dispatch at most one new durable reduce-only close identity; no entry replay is allowed.
- The current-positions correction is accepted at `63f8d8e95918ed9bb7ebedefe5fcd6aeedc7598b`; Chief independently passed 123 focused and 2,231 full-suite tests in Python 3.11. A byte-identical protected pre-recovery backup of the active filled-entry database was created with unchanged SHA-256 `3e25e4a9205b04e6c371e4384277d11419ed4b7f49d5dca970b6ac6abce454b5`. The first fresh corrected reconciliation then stopped before database mutation or write because the exact-external-ID lookup also returns a valid nonempty response without pagination.
- Official Extended documentation defines `GET /api/v1/user/orders/external/{externalId}` as a bounded exact lookup, exposes no cursor or limit, and shows an unpaginated list response. One fresh Luna-max correction may therefore permit absent/null pagination only for this exact route while preserving its one-result identity requirement and strict pagination on orders history, trades, and every documented paginated endpoint. The entry remains accepted and must never replay; after acceptance Chief resumes the same guarded reconcile-and-close plan.

## Exit condition

- Each venue must independently pass one bounded testnet place/reconcile/cancel/close lifecycle ending in authoritative zero open orders and exact flatness. Only then may a separate strategy-testnet task begin.
