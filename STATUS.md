# Current status

- Accepted published baseline: `024547ea5168a2c90a97455ecc2b0b8a6ff6700c`. The new Chief independently audited this exact clean tree on `2026-09-05`; verdict `REVISE`.
- The owner has authorized GPT-6 Astra `medium` as new Chief and simple public scanner completion through visible GPT-5.6 Luna `max` Builders. The Chief alone reviews, accepts, integrates, and pushes. One Spread Builder at a time; prior candidates remain historical residue, not implementation bases.
- Active product: public-only RISEx maker / Lighter Standard exact-q delayed hedge scanner. It measures conditional fee-adjusted entry spread, not realized cycle PnL. No paper trader, private/credential access, signing, orders, or trading is authorized.
- Confirmed blockers: horizon capture can omit pre-deadline books waiting in ingress; eligible trade keys can be counted again after pruning; calibration bypasses final gap validity, lacks transitive units, accepts mixed effective levels, and retains growing quote history. The frozen economic thresholds can pass a negative aggregate score. Stored markout uses quote-edge instead of explicitly binding horizon zero. CAL-001 and HOLDOUT-001 have not been opened in accepted project records and remain closed.
- Historical DG-007 remains `NO_SNAPSHOT_EDGE` under its original rules and configured RISEx maker fee `0.5 bps`. Recorded account fee observation SS-001Q is maker `1 bps`, taker `3 bps`; Lighter Standard maker/taker `0/0 bps`, taker latency `300 ms`. New scanner configuration must bind the recorded fee provenance without importing or repeating the opt-in fee reader. The fee reader has separate audited no-echo/partial-body defects and must not be invoked during scanner work.
- Audit verification: 28 no-argument pure contract tests and 9 pure calibration tests passed in memory, with independent counterexamples for queued deadline capture, duplicate eligible keys, ignored gaps, mixed price separation, and negative aggregate despite passing quantiles. This is not a new full-suite acceptance.
- Immediate slice: `SCAN-001 — Deadline Capture and Run-Wide Trade Identity`, as bounded in NEXT_TASK.md. Subsequent sequence and authority are in SYSTEM_SPEC.md 0.20. No new market observation until measurement, evaluator, fixed configuration, and final prospective gate are independently accepted.
- Earlier acceptance detail, immutable run identities/hashes, rejected candidates, and superseded current-state entries remain in Git history and SYSTEM_SPEC.md. No new history/governance file is introduced.
