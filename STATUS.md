# Current status

## PAPER locked-quantity decision

On `2026-09-01` the user explicitly authorized one bounded PAPER correction for the observed AAVE quantity-drift cancellations. A maker-entry attempt will lock its exact canonical activation quantity for its entire lifetime; later optimized target-quantity drift alone will no longer cancel or re-enter it. All continuing safety and economics checks must be recomputed at that immutable locked quantity, including exact-q taker depth/VWAP, venue grids/minimums, both-leg freshness, route/direction/cycle identity, maker post-only validity, fees/funding/execution, and planned net PnL. Genuine locked-q failure remains fail-closed. Clips, resizing, partial-position entry/hedge, evidence transfer, route switching, and every private/live write remain prohibited.

Accepted candidate `f6fd1dde218c8d309be28b4ed7939f9a8a8ec848` implements the bounded behavior only in the Scanner's exact-quantity evaluation and the paper broker's immutable activation quantity/re-evaluation path, with focused regressions for both observed AAVE drifts and genuine locked-q failure gates. The Chief independently reviewed scope, diff, route/cycle identity, exact-q depth, grids/minimums, economics, price-version evidence reset, fill/cutoff/notification preservation, and no-write surfaces; repeated `207` focused/adjacent tests and one clean isolated Python 3.11 full suite with `3607` passed and `3` skipped; and fast-forward integrated the exact candidate. Compile, dependency, local-import, ancestry, diff, and Git checks passed; accepted main is `1c9ba1cf7386cca5c74409a411e16f063d59ff6a`. The user stopped live testing before a fresh-process validation. No PAPER restart or replacement is authorized until separately requested.

## PAPER transport and cutoff follow-up

Accepted and published main `505c45b7fe92a593ce018e74c72fc79afbba38f3` corrects both proven defects without changing the two-minute entry window or freshness/economic rules. The owned Lighter combined public stream now sends the exact application `{"type":"ping"}` every 10 seconds, accepts only the exact application-pong object as connection confirmation, and retires the heartbeat on stream replacement, disconnect, or shutdown. An active PAPER entry now owns an independent exact `T−5 seconds` cancellation task, so catalog refresh, focused scans, reconnects, and recovery work cannot postpone terminal persistence; broker/fill/cutoff mutation remains serialized and exactly once. Independent Chief review passed the adverse heartbeat/session, blocked-scan cutoff, duplicate-cancel, and pre-cutoff atomic-open races, followed by the clean integrated Python 3.11 suite with `3598` passed / `3` skipped.

The historical cancellation causes remain exactly reconciled and intentionally fail closed. SNDK and AAVE `DATA_STALE` coincided with RISEx book resyncs; LIT `DATA_STALE` coincided with the former Lighter EOF; SNDK `ROUTE_INVALID` followed a common executable quantity change from `0.3402` to `0.3392`. A maker gap can hide a fill, and an unavailable exact-q taker book cannot guarantee the atomic hedge, so the fixed `T−120` to `T−5` opportunity window never permits continuing blindly through either condition. The separate sixth-AAVE scheduling defect is fixed: exchange timestamps at or after cutoff remain ineligible and the owned deadline now persists cancellation promptly even when the general scheduler is busy.

Former `chief37` stopped terminal `STOPPED_SAFE` with SQLite integrity `ok`, one cancelled order, and zero active orders, positions, fills, funding settlements, or completed trades; preserve and never resume its database. Accepted replacement `chief38` later stopped by external `SIGTERM` at `2026-09-01T04:25:26Z` and reached terminal `STOPPED_SAFE`, SQLite integrity `ok`, `14` cancelled PAPER entries, and zero positions, fills, funding settlements, or completed trades; preserve and never resume its database. At the user's explicit request, public-only PAPER `chief39` ran on owner-only database `mainnet-paper-live-20260901-chief39.db`, reached `PAPER_RUN_READY`, all `128` routes including `48` Lighter directions, all four venue readiness signals, and no fatal/blocked evidence. The final boundary produced four additional zero-evidence no-fill attempts: two AAVE `DATA_STALE` cancellations coincident with full RISEx book-resync waves, one AAVE `ROUTE_INVALID` caused by the old optimized-quantity mismatch `4.002→3.999` despite the refreshed plan remaining eligible with positive planned net PnL, and one LIT cutoff. No taker hedge, position, funding, or PnL existed. On the user's stop request, launchd restarted once after the first safe SIGINT; Chief then unloaded only the exact label and the replacement also persisted `STOPPED_SAFE`. No PAPER PID, launchd label, or heartbeat remains. Database integrity is `ok`, mode `0600`, all `10` orders are cancelled, and fills/positions/funding/completed trades are `0/0/0/0`. Preserve and never resume this database.

## PAPER entry-outcome correction

Accepted main `560f6755a01fe2df80952f8b5e5efbdbabf66d49` closes the observed PAPER outcome gap without changing scanner, ranking, one-position limit, maker evidence, hedge timing, funding, exit, or PnL economics. Every unfilled maker-entry termination now atomically persists one sanitized attempt-specific `PAPER_ENTRY_CANCELLED_NO_FILL` result and emits one Telegram lifecycle message with the exact cancellation reason, active duration, and actual cumulative qualifying maker evidence; it explicitly states no full maker fill, no taker hedge, zero opened position, and return to `FLAT`. Repeated attempts in the same route/cycle no longer collide. A trade-time taker-staleness/depth cancellation now also releases the flat in-memory broker so later eligible attempts can activate without restart. Independent Chief verification passed focused/adverse tests, dependency/compile/diff review, and one clean integrated Python 3.11 suite with `3593` passed / `3` skipped.

The former `chief36` runtime stopped durably `STOPPED_SAFE` with SQLite integrity `ok`, six cancelled entry attempts, and zero open orders, positions, fills, completed trades, funding settlements, or realized PnL; preserve and never resume its database. The single active public PAPER replacement is PID `98546`, launchd label `com.risex.paper.chief37`, on fresh owner-only mode `0600` database `mainnet-paper-live-20260831-chief37.db`. It reached `PAPER_RUN_READY` and simultaneous readiness for all four public venues with `128` routes. A later Lighter public-socket EOF recovered its connection but current funding-stat validation is fail-closed as `PUBLIC_FUNDING_STATS_UNUSABLE`; the latest full scan therefore has no eligible route. This is a public-data degradation, not evidence of a fill or position, and the process remains running with orders/positions/fills/completed/fatal at `0/0/0/0/0`. Protected Telegram files remain owner-only and are loaded by the accepted fixed-path boundary. No venue write occurred.

## Central baseline

- On `2026-08-31` the current sole Chief independently re-audited takeover; the only tracked user change remains `README.md`, and the named untracked operational artifacts, historical branches, and worktrees remain preserved. The single active process is public-only PAPER PID `98546` under launchd `com.risex.paper.chief37`; its fresh owner-only database has SQLite integrity `ok`, `128` current routes, and zero exposure. Current Lighter funding statistics are fail-closed after a recovered public-socket EOF; RISEx, Extended, and Nado remain ready. Both recorded Codex automations are paused, and no mainnet venue-write process exists.

- The user now separately authorizes a Lighter-only mainnet onboarding/readiness slice and, only after every safety gate passes, exactly one smallest-executable manual lifecycle on the exact funded Lighter account: one open, authoritative reconciliation, reduce-only exact-position close, and two agreeing terminal zero-order/exact-flat rounds. Protected hidden-input onboarding and exact-account authenticated reads are authorized now. Strategy/automatic trading, withdrawals/transfers, other wallets, position increase, repeat lifecycle, blind replay, and unrelated-state mutation remain forbidden. The user supplied Lighter-specific positive absolute caps of `maximum_deposit_or_collateral_usd = 20` and `maximum_total_loss_usd = 10`; the loss cap does not exceed the collateral cap. These caps remove only the missing-cap blocker and authorize no payload preparation, signing, or dispatch until implementation acceptance, protected onboarding, exact-account clean-state readiness, authoritative current fees/minimums/liquidity, and one exact smallest-executable plan are independently proven within both caps.

- On `2026-08-31` the new sole Chief independently completed takeover. The only pre-existing tracked user change remains `README.md`; all named untracked operational artifacts and historical worktrees/branches are preserved. Former PAPER processes through `chief36` are stopped and their databases remain immutable. The single active public PAPER process is `chief37` as recorded above; no mainnet venue-write process exists.

- Accepted main `5a3975ea44152d3863a83c59ab3255932a3aba2f` corrects the observed Lighter PAPER funding error. Public `market_stats` rates are percentage-number units and now divide by `100` exactly once; cash uses the official `mark_price`. `current_funding_rate` remains the next-hour prediction, while a contiguous `funding_timestamp` advance applies that frame's `funding_rate` exactly once to the registered lifecycle settlement, with stale, duplicate, gapped, wrong-session, and unregistered frames rejected. Live PUMP evidence at the `2026-08-31T18:00:00Z` boundary changed from a pre-boundary estimate near `0.0050%` to applied `0.0053%` and next estimate `0.0074%`; the applied effect on a `$500` short is about `+$0.0265`, not `+$2.65`. Independent Chief verification passed `209` focused tests and the clean Python 3.11 full suite with `3589` passed / `3` skipped. Fresh `chief36` reached `PAPER_RUN_READY`; its first `LIVE_STREAM` scan contains all `128` routes and all `48` Lighter directions, with maximum absolute Lighter-leg funding only about `$0.0431`. PUMP/Lighter now shows approximately `±$0.0430` Lighter funding and approximately `±$0.00186` net two-leg funding, while both directions are correctly blocked by negative planned net PnL. Orders, positions, fills, completed trades, and fatal events remain `0/0/0/0/0`.

- Accepted and published main `117bbf47ee09dd0ede73f698089a225022263533` removes the Lighter public-data storm and unnecessary requests. Lighter now uses one `market_stats/all` subscription plus one sequence-bearing `order_book/{market}` subscription per shared market, with no Lighter trade subscription; the 24-market live profile is `25` subscriptions instead of `49`. Lighter REST books can never establish or recover sequence health, a healthy WebSocket book/funding quote wins concurrent and periodic REST work, and a gap recovers only through a fresh owned WebSocket subscription snapshot with bounded timeout and no same-session retry storm. RISEx maker-trade readiness remains mandatory while the Lighter virtual-taker leg is depth-gated. Independent Chief review repeated `9` adverse tests, proved a `120`-subscription dispatcher holds exactly `50` in flight, and accepted the Builder's clean Python 3.11 suite with `3584` passed / `3` skipped. Fresh `chief35` produced a `LIVE_STREAM` full scan with all `128` routes, all `48` Lighter directions healthy (`24` eligible and `24` blocked only by negative planned PnL), zero Lighter resync/recovery events, and only catalog REST completions after startup. Orders, positions, fills, and completed trades remain `0/0/0/0`.

- The first Lighter testnet Level-C lifecycle is terminal `COMPLETE`. Exact account `202`, API key `4`, and market `120` opened one authoritative short `2.79 LIT` fill under order `34058472185700930` / trade `260361`, held it through boundary `1788184800000`, and received authoritative account funding `+0.000121` test USDC at rate `0.000012`. One in-flight recovery continuation dispatched exactly one fresh reduce-only close; immediate reconciliation was ambiguous, so no replay occurred. Later authoritative evidence bound close order `34339947154989401` / trade `266054`, exact `2.79` fill at `3.6023`, zero regular/trigger orders, exact flatness, and no unrelated state. Two fresh agreeing terminal rounds and the resolved source write identity are preserved in owner-only journal `/Users/daniilmakarov/.config/risex-farmer/lighter-testnet/level-c-terminal-evidence-chief80.db`, mode `0600`, SQLite integrity `ok`, SHA-256 `454a1c087e45e0e3a96343598f95643296d6fa5000f0971db8290870760be0c5`. No further Lighter testnet write is authorized by this completed slice. Its evidence may inform, but never substitute for, the newly authorized isolated Lighter mainnet readiness gates.

- Accepted and published main `271bdb24a6ea4ec0f9d1a63188ed954cc8a8d638` corrects the observed Lighter `PositionFunding.timestamp` unit mismatch: official/observed Unix seconds are normalized to milliseconds under a strict magnitude gap, while valid millisecond values remain unchanged and bool, nonpositive, ambiguous, overflow, and future values fail closed. Independent Chief verification passed `10` focused tests, dependency/compile/diff/surface checks, and one clean Python 3.11 full suite with `3545` passed / `3` skipped. The correction changes no write, close, no-replay, PAPER, or mainnet behavior.

- The former `chief31` PAPER runtime was proven to have started without all three `RISEX_TELEGRAM_*` variables, explaining its missing notifications. Accepted implementation `931d58dbf9c983ef51aef5ffe1ac2929c4becb78` adds the fixed protected restart boundary; independent Chief verification passed `26` focused/adverse tests and one clean Python 3.11 full suite with `3571` passed / `3` skipped, plus dependency, compile, diff, and no-write checks. The test-only bot token and private-chat contract are atomically provisioned without output under `/Users/daniilmakarov/.config/risex-farmer/telegram/bot-token` and `chat-id`; both files are owner-only mode `0600`, link count one, with safe SHA-256 prefixes `b13df54835ee` and `ce21d919b68c`. A bounded `getUpdates` read now proves the user's current private `/start` matches the stored chat. One explicit `SYSTEM_READY` payload was accepted and drained through the real application `NotificationOutbox`; fresh `chief33` independently revalidated the protected loader and file metadata after restart. Telegram is active for subsequent actual runtime events; startup itself is not a normal product notification. Inbound polling/commands remain forbidden.

- Accepted and published main `b45e70c5d6b4366137dcd7be0b59a0341ac85956` corrects the missing Lighter PAPER routes. The observed causes were rejection of valid initial `subscribed/market_stats`, rejection of the venue's valid `+1 ms` hourly funding boundary, and `72` startup subscriptions for 24 markets exceeding the documented `50` in-flight limit while REST book bootstrap delayed WebSocket snapshots. The correction uses one all-market stats subscription plus book/trade per market (`49` total), accepts only the observed strict stats forms and exact-or-`+1 ms` hourly boundary, and boots books from the authoritative WebSocket snapshot without turning the historical trade snapshot into fill evidence. Independent Chief verification passed `42` focused tests and one clean Python 3.11 full suite with `3575` passed / `3` skipped, plus dependency, compile, diff, scope, and no-write checks. On fresh `chief33`, the first post-readiness cycle ranked all `48` Lighter directions and allowed `24`; the other `24` were blocked only by negative planned net PnL. All false funding/book/trade health blockers disappeared.

- Accepted Nado candidate `625f58a79f1f61dea4126e885f7b81b4cdb56129` adds the isolated fixed-host public Archive POST gate for the exact mainnet default subaccount: matched orders, matches, account events, and interest/funding payments with bounded complete SDK-compatible pagination, strict identity/cross-history agreement, durable sanitized journaling, one transport retry only, and permanent `NO_MAINNET_WRITE_AUTHORITY`. The Chief independently verified current official endpoints and SDK contracts, repeated `23` focused/adverse tests and the clean integrated Python 3.11 full suite with `3250` passed / `3` skipped, and passed compile, dependency, diff, Git, request-body, redaction, and no-write/private/trigger surface checks. No live request, credential, signature, payload preparation, dispatch, transfer, withdrawal, or venue write was used for acceptance. The next Nado action is one bounded live unsigned Archive read for the exact accepted identity; its result remains read-only evidence and cannot authorize an order.

- The single authorized Nado Archive live invocation `nado-mainnet-archive-read-32e418af3dc1a741f7daf757f2afb3e2` made one unsigned read-only orders-page POST and stopped terminal `SCHEMA/INTEGER_TEXT_INVALID_ORDER_0_CLOSED_MARGIN` without retry. Its owner-only journal is mode `0600`, SQLite integrity `ok`, and terminal; no secret, signature, trigger/private surface, payload preparation, dispatch, transfer, withdrawal, or venue write was used. Current official Nado SDK evidence defines cross-margin `closed_margin` as `null`. Accepted candidate `8b9714971d99a2ce5cfee46d8c5cba07276d32c3` corrects only that observed nullable field while preserving strict non-null integer-string validation and every identity/pagination/no-write gate. Independent Chief verification passed `112` focused/adjacent tests and one clean Python 3.11 full suite with `3262` passed / `3` skipped, plus ancestry, diff, dependency, import, AST, fixed-host, and no-write/private/trigger surface checks. The consumed live invocation is not replayable; historical account funding remains `UNKNOWN` until a separately bounded fresh read is authorized.

- Accepted main `ee40fee9ad6a4db83eaa380d14489cc74a87f99e` adds Lighter as the fourth public PAPER venue with the one approved execution profile: RISEx maker then exact-quantity Lighter taker on entry and normal close. It adds only official public metadata, volume, book, trade, market-stats/future-funding parsing, dynamic routes, fixed Standard-tier zero fees, and the minimum profile-aware scanner/runtime/lifecycle/persistence changes; it does not simulate the documented Standard-account delay. Independent Chief verification matched the current public catalog contract, passed `243` focused/adjacent tests, the candidate full suite with `3246` passed / `3` skipped, and the integrated Nado-plus-Lighter full suite with `3281` passed / `3` skipped, plus compile, dependency, diff, ancestry, and no-write checks. No Lighter credential, account/private endpoint, applied account-funding claim, testnet/mainnet order path, signing, dispatch, transfer, withdrawal, or live strategy execution exists. Lighter testnet remains a separate not-yet-started slice.

- The first fresh live PAPER validation on database `mainnet-paper-live-20260831-chief26.db` exposed one bounded Lighter catalog defect: the official `filter=perp` response returned a valid required perp array while the irrelevant spot field was `null`, and the adapter rejected it. The run persisted zero orders/fills/positions, stopped `STOPPED_SAFE`, and has SQLite integrity `ok`; preserve and never resume it. Accepted main `69b9856678009ba69c6f4fe402a6513fdf3cf7e0` removes only that irrelevant spot-array requirement while retaining strict required-perp and unsafe-metadata rejection. Independent Chief verification passed `27` focused/adverse tests, a live public read of `231` markets / `197` active, and the clean integrated Python 3.11 full suite with `3289` passed / `3` skipped, plus dependency, ancestry, diff, and no-write checks.

- The next fresh validation on database `mainnet-paper-live-20260831-chief27.db` reached `PAPER_RUN_READY` and persisted `48` Lighter route directions, then correctly failed Lighter closed because the official initial order-book frame is `subscribed/order_book` while subsequent frames are `update/order_book`. It stopped `STOPPED_SAFE`; preserve and never resume it. Accepted main `1c204393fd4703c17c1a355347f3da2f2a19b93e` makes that phase contract exact and rejects either type in the opposite phase while preserving channel/market and nonce continuity. Independent Chief verification passed `29` focused/adverse tests and the clean integrated Python 3.11 full suite with `3291` passed / `3` skipped, plus exact observed public-frame, dependency, ancestry, diff, and no-write checks.

- Fresh database `mainnet-paper-live-20260831-chief28.db` again reached `PAPER_RUN_READY` with `48` Lighter route directions, then failed Lighter closed on the official initial `subscribed/trade` batch. Direct public evidence proved that batch contains historical trades and must never become maker-fill evidence; only later increasing-nonce `update/trade` batches are live. The run stopped `STOPPED_SAFE` with integrity `ok` and zero orders/fills/positions; preserve and never resume it. Accepted main `0cc32cd3b9c18f9c7dd2715d3ff8e92dc8579d67` validates the exact initial identity/nonce while suppressing its historical rows, delivers only strictly newer updates, ignores equal/decreasing batches, and resets the sequence on physical reconnect. Independent Chief verification passed `10` exact focused/adverse tests and the clean integrated Python 3.11 full suite with `3300` passed / `3` skipped, plus scope, ancestry, diff, dependency, and no-write checks.

- Continuous public PAPER runtime `mainnet-paper-live-20260831-chief29.db` stopped terminal `RUNTIME_FATAL` at `2026-08-31T05:58:59Z` in `scheduled_scan` with sanitized exception class `ValueError`. Before the failure it created one PAPER-only LIT/Lighter entry order and preserved the resulting virtual `HOLDING` position; there was no private endpoint, signing, dispatch, or venue write. Preserve and never resume that database. A fresh replacement runtime is active on new database `mainnet-paper-live-20260831-chief31.db`; it reached `PAPER_RUN_READY`, reports all four public venues available with the Lighter book/trade stream ready, and had zero orders and positions at the last checkpoint. It is local-only and will not survive a Mac shutdown/reboot.

- Lighter testnet onboarding and authenticated readiness are complete for test-only L1 identity `0x84da822bbd7518b252dc74F0168afaACb562Ed9A`, account index `202`, and API-key index `4`. Protected path contracts are `/Users/daniilmakarov/.config/risex-farmer/lighter-testnet/wallet-private-key`, `/Users/daniilmakarov/.config/risex-farmer/lighter-testnet/api-key-4-private`, `/Users/daniilmakarov/.config/risex-farmer/lighter-testnet/identity.json`, and `/Users/daniilmakarov/.config/risex-farmer/lighter-testnet/onboarding-chief01.db`; secret bytes never entered Git, task/chat, arguments, logs, reports, fixtures, or the operational journal. Exactly one official faucet request created and funded the account with `10000.000000` test USDC margin plus the documented ETH/LIT bootstrap assets; exactly one official-SDK API-key registration was durably claimed before dispatch and reconciled without retry. The SDK was pinned outside the repository at official commit `ebc50660efc99f31e4055418e4514255456cb060`. Accepted main `43fd479770b0cc168e016e460accfb93e88ee54a` adds only the isolated authenticated Level-B gate; independent verification passed `56` focused/adverse tests and the clean integrated Python 3.11 full suite with `3356` passed / `3` skipped. The live gate made `7` GET reads with no retry and proved exact account/API-key identity, Standard tier maker/taker fee ticks `0/0`, positive collateral, exact three-asset faucet baseline, zero active orders, exact flat positions, and empty trade/funding history. This is `READY` evidence only: no testnet order lifecycle or funding accrual has yet been executed, and no Lighter mainnet authority exists.

- Accepted and published main `53eeb0df6d4087cc0699a1099743445137d13e4e` adds the isolated Nado mainnet unsigned exact-subaccount GET-only gate. Chief pre-commit review corrected incomplete balance-vector flatness, zero-balance count semantics, nonquote spot and `v_quote` residues, non-orderbook product queries, and retry/failure evidence before the final candidate. Independent Chief verification passed `371` focused/adjacent tests and a clean Python 3.11 full suite with `3227` passed and `3` skipped, plus current official-contract, dependency, compile/import, GET-only surface, ancestry, diff, and Git checks. Fresh durable invocation `nado-mainnet-unsigned-read-15ea49dc0cd30a33d9fd976d15f2ec53` made exactly `98` completed unsigned GET reads with no retry: exact wallet/default subaccount exists, quote collateral is `19.929591325623796517`, all `94` product balance vectors agree, all `92` orderbook products have zero open orders, regular and isolated positions are zero, nonquote spot state is zero, and the complete-vector result is `EXACT_FLAT`; linked signer `0xc24a1f83ad80cc2b4d4dc82871d04b5d1736e74d` is active. The terminal result remains intentionally `BLOCKED/UNSIGNED_GET_COMPLETE_FUNDING_BLOCKED`: archive funding/order/match/event history is POST-only, trigger listing and the private order/fill/position stream require signed authentication, and historical account funding is therefore `UNKNOWN`, never zero. The owner-only durable journal has SQLite integrity `ok`. No Nado private key was read or persisted and no signing, order preparation, dispatch, transfer, withdrawal, or venue write occurred.
- Accepted and published main `3f3d77c20fd91c56335dd7e5853534542450ca42` adds the one-shot RISEx mainnet session-signer registration boundary for the exact user-authorized identity, with a durable cryptographically fresh runtime ID separate from the write-intent ID, hidden one-use main-wallet-key input, claim-before-dispatch, one POST without retry, no blind replay, and read-only signer/nonce reconciliation. Chief caught and corrected an invalid full-bitmap nonce blocker before candidate commit; the accepted implementation preserves the official cursor-`208` rollover to the next anchor/index `0`. Independent Chief verification passed `57` focused/adverse tests and a clean Python 3.11 full suite with `3203` passed and `3` skipped, plus current official-contract, dependency, compile, import-isolation, write-surface, ancestry, diff, and Git checks. Operational runtime `020ac5efa6a4887d49690ddb45f58da0` durably claimed intent `e7faaa09aad3ed100581a255c14f579a` and made exactly one registration POST. A separate no-dispatch reconciliation proved signer `0x9c904d9145a45fbe2d5645cd9226def5efc9c5de` active for wallet `0xb13c2bbe1f07f58efbbbdf86d948b49da2e0a56f`, chain `4153`, expiration `1790689990`, with nonce transition `(1,1)` to `(1,2)` and terminal `REGISTERED/COMPLETE/PROVEN`. No order, transfer, withdrawal, or other venue write occurred; the main wallet key was not persisted.
- Accepted and published main `349ff8c478d7acfbbfe8dfc6697d27a5197d56b4` completes the Extended mainnet exact-account read-only implementation and the observed USD/spot-equity corrections. Independent Chief verification passed `181` focused/adjacent tests and a clean Python 3.11 full suite with `3180` passed and `3` skipped; the surface remains GET plus the authenticated private account stream and has no write method. Fresh invocation `extended-mainnet-read-a5b3db7d6afd60813e0163a73a03b5b2` made exactly `12` completed GET reads, verified account `303919`, found zero open orders and zero positions, authoritative empty funding history, maker `0` / taker `0.000225` for `BTC-USD`, and agreeing balance/spot-equity arithmetic. It stopped terminal `BLOCKED/SAFETY/STREAM_UNRELATED_SPOT_STATE` because the exact account has a nonzero `ETHSPOT` balance in addition to USD collateral; that unrelated asset is not flatness evidence and may not be silently treated as collateral. No write occurred. The separately authorized exact RISEx signer registration has since been consumed and reconciled as recorded above; it grants no replay or order authority.
- On `2026-08-30` protected onboarding completed for all three real mainnet identities without creating write authority. RISEx retained only its generated session key and sanitized wallet/session identity; signer registration remains `NOT_PREPARED`. Extended accepted published main `e44aab8c1b3760f5469ce3253a4367a72fbf149c` and then used its hidden API-key-first discovery command to reconcile exact account `303919`, index `0`, public L2 key `0xa78ee93989d14c80ea6a5423053260599b0d2d723d4381382799bd027dd555`, and vault `403919`; only the read-only API key and public identity persist. Nado used one hidden derivation to persist only wallet `0xf3c1b239f2978856839c3b676f22682c04500ac4` and bytes32 `default` subaccount `0xf3c1b239f2978856839c3b676f22682c04500ac464656661756c740000000000`; no Nado secret persists or is required for authoritative reads. All protected directories/files are owner-only `0700`/`0600`, link count one, and inspected without reading secret bytes. Independent Extended acceptance passed `51` focused tests and a clean Python 3.11 full suite with `3159` passed and `3` skipped. No registration intent, JWT, private stream, signing payload, signature, order, or venue write was created; the next gate is sequential exact-account read-only evidence.
- The public-mainnet PAPER process previously recorded as PID `6950` disappeared without a durable `STOPPED_SAFE` or `RUNTIME_FATAL` marker after its last normal public readiness event at `2026-08-30T13:45:21.459331Z`. The immutable database `mainnet-paper-live-20260830-chief25.db` has SQLite integrity `ok`; all three historical PAPER orders are `CANCELLED`, and active orders/fills/positions/completed trades/funding settlements are `0/0/0/0/0`. No paper or venue-write process remains. Do not resume this database or blindly restart the runtime; a future public PAPER run requires a fresh database and explicit terminal ownership.
- On `2026-08-30` the user explicitly expanded the mainnet scope to protected local private-key onboarding and eventual smallest-executable-order testing across the funded RISEx, Extended, and Nado accounts. Secret material remains forbidden in task/chat and every persistent/logged surface; a main wallet/Stark key may be used locally only where official venue contracts require it to derive/validate identity, register a narrower session/linked signer, or sign inside a later separately approved bounded lifecycle. Protected provisioning and exact-account private reads are now authorized. The user requires the venue credentials needed for reconciliation and reduce-only close to persist across Chief/task/process restarts at fixed protected paths; secret bytes never enter governance or handoffs. Accepted published main `4df7b35704368457836a75131aca8b2cf5467259` established the first isolated venue-local hidden-input onboarding checkpoint with fixed protected paths, descriptor-relative no-follow traversal, owner/mode/link/size checks, redacted evidence, restart discovery, and no default-runtime or venue-write surface. Its independent verification passed RISEx `431` focused and `3055` full tests with `3` skipped, Extended `33` focused and `3088` full with `3` skipped, and Nado `48` focused and `3136` full with `3` skipped on Python `3.11.5` with aiohttp `3.14.3`; no credential or live access was used at that checkpoint. The user set the absolute caps for one future lifecycle to `deposit_cap_usd = 20` and `maximum_loss_usd = 12`; the loss cap does not exceed the deposit cap. No mainnet write is authorized yet because remaining provisioning, exact identities, authoritative flat/unrelated state, current fees/minimum/funding semantics, executable quantity within both caps, and one exact route/account/direction lifecycle approval remain absent. The failed invisible Codex-terminal prompt was cancelled empty; no secret was entered or stored there.
- On `2026-08-30` the user reported funding three separate mainnet venue wallets/accounts and requested smallest-order mainnet canary preparation across the two existing RISEx-centered routes. This authorizes credential-free public/offline readiness work and the missing narrow RISEx–Nado Level-D readiness slice; it does not authorize secrets in task/chat, payload preparation/signing, dispatch, or any mainnet write before protected provisioning, authoritative exact-account private reads, explicit positive absolute deposit and maximum-loss caps, exact venue/account binding, and one separate manual lifecycle approval. The user's earlier manual small positions/closures are context only and are not authoritative terminal evidence; both routes must independently prove zero unrelated state and exact flatness through private reads before any later dispatch.
- A fresh credential-free BTC observation at `2026-08-30T10:02Z` found a common `2026-08-30T11:00:00Z` public funding boundary and executable two-sided books on all three venues. RISEx published minimum `0.00015`, step `0.000001`, BBO `78008/78008.1`; Extended published minimum `0.0001`, step `0.00001`, BBO `77964/77965`, making the smallest common RISEx–Extended quantity `0.00015 BTC` (about `$11.70` per leg). Nado published quantity step/minimum `0.00005 BTC`, BBO `77969/77970`, product `min_size` `$100`, and minimum taker-fee notional `$100`. Official Nado FAQ/fee contracts state there is no universal trade minimum and some order types allow smaller trades, so `$100` is not an unconditional execution floor. The theoretical common RISEx–Nado grid candidate is `0.00015 BTC` (about `$11.70` per leg), but the exact executable minimum of a selected Nado order type remains unresolved and therefore `BLOCKED`.
- Accepted and published candidate `a6651db728f61a9693b54ceb9cc00c694bb5ed37` adds the isolated RISEx–Nado Level-D public/offline readiness evidence gate. It reuses the canonical protected RISEx identity path by metadata only; Nado read evidence uses exact public wallet/bytes32 subaccount identity and authoritative unsigned queries, with no Nado API-key/secret provisioner. It contains no network-private transport, signing, order preparation, dispatch, or normal-runtime surface and every result reports `NO_MAINNET_WRITE_AUTHORITY`. The baseline correctly remains `BLOCKED:NADO_EXECUTABLE_MINIMUM_PENDING`, exposes only theoretical `0.00015 BTC`, and does not claim a write-ready common quantity. Independent Chief verification passed `93` focused/adverse tests and one clean Python 3.11 full suite with `3021` passed and `3` skipped, plus official-contract, dependency/import/write-surface, diff, and Git checks.
- On `2026-08-30` the user explicitly started a new continuous public-mainnet PAPER observation with outbound-only Telegram delivery. Fresh database `mainnet-paper-live-20260830-chief25.db` reached durable `PAPER_RUN_READY` at `2026-08-30T06:16:52.904969Z`, persisted all `58` current route directions, and has SQLite integrity `ok` with exact orders/fills/positions `0/0/0`. Configuration overlay is exactly zero. The protected Telegram configuration remains outside Git with mode `0600`; inbound polling/commands and every venue write remain disabled. The process is active locally and will not survive a Mac shutdown or reboot without a separately accepted service deployment.
- On `2026-08-29` the single authorized v11 RISEx–Nado Level-C owner lifecycle ended `BLOCKED` and is permanently frozen; no replay or identity rotation is authorized. The corrected non-cancelling owner retrieval behavior was exercised across repeated 900-second checkpoints, but after entry and the target boundary Nado durable progression stopped at `PUBLIC_EVENT_WAIT_STARTED` without public-event acceptance, account-history completion, funding evidence, or a persisted blocker, while the owner surfaced `RISEX_FAILED`. Funding therefore remains `UNRESOLVED`, never zero. Bounded recovery reused only the accepted sequential reduce-only paths: RISEx primary and counterparty each have exactly one reconciled entry and one reconciled close; Nado has exactly one reconciled entry and one reconciled close. Two agreeing authoritative terminal rounds prove zero relevant/trigger/isolated orders, exact flatness, and no unrelated state on both venues. The three mode-`0600`, SQLite-integrity-`ok` frozen journal SHA-256 values are Nado `2c9967faca10401033e8aa290654b9007fd0d2dbecccf1addb5059a1c6d6756f`, RISEx primary `88595ee97b61de0c04e26fcb06d6e89ea9c876421109c2772f2169e82730208d`, and RISEx counterparty `db8a95fbccef5bf7b5a7aa90e5af6c327c6ea4d16364a87b83b9aea95e75f78a`. No venue-write process remains.
- Accepted and published main `e640aa1d76387c45bfebfdae8fee7ac90153051a` adds the isolated RISEx–Extended Level-D readiness evidence gate and fixed local interactive protected-identity provisioning interface. It has no network, database, signing, order-preparation, dispatch, or normal-runtime surface and every phase reports `NO_MAINNET_WRITE_AUTHORITY`. Phase A currently evaluates the accepted public BTC evidence as `READY_FOR_PROTECTED_PROVISIONING`; real protected identities are absent and were not requested or provisioned. Later phases separately require safe mode/ownership/link state, authoritative exact-account private reads and planned deposits, explicit user-bound absolute deposit and maximum-loss caps plus exactly one manual lifecycle approval, fresh durable runtime and four ordered per-dispatch identities, and post-lifecycle entry/fill/position/funding/close/two-round-flat evidence. Independent Chief verification passed `109` focused/adverse tests and the clean isolated Python 3.11 full suite with `2928` passed and `3` skipped, plus dependency, compile, import-isolation, surface, diff, and Git checks.
- On `2026-08-29` the new sole Chief independently completed takeover on published `main == origin/main == ef898ebe3784e7f812c0549599b3b4f514aac8e1`: the only tracked user change is `README.md`, the six named untracked operational artifacts remain present, no PAPER or venue-write process is active, the prior Chief is idle/read-only, protected credential material remains outside Git with mode `0600`, frozen v10 funding journals retain their recorded SHA-256 values and SQLite integrity `ok`, and all three v11 lifecycle stores remain absent. The user now authorizes exactly one final bounded v11 RISEx–Nado testnet funding-boundary attempt after the existing two-round funding-specific Level-B and owner-wrapper gates pass; failure or venue-quality insufficiency is terminal for this slice and must not be blindly retried.
- A bounded credential-free mainnet read gate at `2026-08-29T18:50:56Z` proved the current RISEx–Extended BTC route is two-sided and non-RFQ on both venues. RISEx BTC/USDC publishes minimum `0.00015`, step `0.000001`, BBO `78226.1/78226.2`, and about `$27.1m` 24h quote volume; Extended BTC-USD publishes minimum `0.0001`, step `0.00001`, BBO `78210/78211`, and about `$40.5m` daily volume. The smallest common step-aligned quantity is therefore `0.00015 BTC` (about `$11.7` per leg at observation). Both venues published the same next hourly boundary `2026-08-29T19:00:00Z`; fresh public applied histories independently show hourly settlements. Extended's official public schedule is maker `0` / taker `0.00025`; RISEx's exact account fee is intentionally unresolved because its official current-fee endpoint is JWT/account-scoped and no mainnet credential/private read is authorized. Public evidence opens one offline/read-only Level-D readiness implementation slice, not mainnet credentials, deposits, signing, order preparation, dispatch, positions, or strategy execution.
- On `2026-08-28` the user explicitly authorized one temporary six-hour PAPER/testnet behavior experiment over only `RISEx–Nado` and `RISEx–Extended`: add exactly `$0.50` to expected route PnL for test eligibility and ranking while preserving the unmodified expected PnL, overlay amount, adjusted test PnL, and every actual fee/funding/realized value as distinct evidence. Accepted published main `523f2a42ff493d97ed56c21f598a4cd4308ff4c8` implements the default-zero opt-in PAPER overlay, durable database configuration identity, separate persistence/report/Telegram evidence, and unchanged actual accounting and safety blockers. Independent Chief verification passed `255` focused/adverse tests and the clean Python 3.11 full suite with `2693` passed and `3` skipped. No runtime or venue write has started; current testnet blockers remain unchanged.
- Fresh public preflight then evaluated all 58 directions with all venues ready: raw-positive count was zero, while the exact `$0.50` overlay made 35 routes test-eligible (`22` Extended and `13` Nado); its database has integrity `ok` and zero orders/fills/positions. The first six-hour runtime attempt is not an accepted window: after `PAPER_RUN_READY` it recorded 28 snapshots and two safely cancelled virtual entry orders, then stopped terminal `RUNTIME_FATAL` at `2026-08-28T20:59:06Z` with `ValueError: activation must use the shared scan timestamp`. It ended with zero fills, positions, or funding settlements and SQLite integrity `ok`. Its database is preserved and must not be resumed. A fresh correction must reproduce the exact activation/refresh timestamp race without weakening the shared-scan identity invariant; no testnet or venue write occurred.
- Accepted published main `9dad61d00af422598e1f9280ddf30bb3e286b51c` corrects that race. It serializes only scan publication and activation identity, performs a current activation-time focused scan when required, and fails closed on missing/stale/superseded publication. Chief review additionally found and rejected the first lock scope because it could delay disconnect cancellation behind network refresh and permit a stale detached trade commit; the final candidate uses synchronous per-market invalidation plus a short PAPER-entry commit lock and leaves network refresh outside both safety locks. Independent Chief verification passed all three exact adverse regressions, `37` adjacent tests, and the clean Python 3.11 full suite with `2696` passed and `3` skipped. A fresh short live activation-boundary validation is required before restarting the six-hour window.
- Fresh public-only validation `mainnet-shadow-synthetic-all-routes-20260829-chief21-activation-validation.db` crossed the `2026-08-28T22:00:00Z` boundary on accepted `9dad61d0`: two PAPER entries activated at `21:58:00Z` and `21:58:14Z`, then cancelled safely as route-invalid and at cutoff with coherent persisted timestamps. It ended by bounded SIGINT at `22:00:29Z` with `STOPPED_SAFE`, SQLite integrity `ok`, zero active orders, fills, positions, and funding settlements, and no blocked, fatal, or disconnect event. Both RISEx–Nado and RISEx–Extended groups retained distinct raw expected, `$0.50` overlay, and adjusted test expected values; the validation database is immutable and must not be resumed. Fresh replacement `mainnet-shadow-synthetic-all-routes-20260829-chief22-6h.db` reached durable `PAPER_RUN_READY` at `2026-08-28T22:02:20.980005Z`; its exact six-hour stop threshold is `2026-08-29T04:02:20.980005Z`.
- The replacement six-hour window failed early and is not credited. It completed 21 scans through `2026-08-28T22:42:30.623768Z`, then the cadence task silently stopped: the `22:44:20Z`, `22:46:20Z`, and `22:48:20Z` deadlines produced no `PUBLIC_SCAN_DEADLINE`, `PUBLIC_SCAN_BLOCKED`, or fatal evidence while the process and venue-readiness tasks remained active. Chief stopped only that process at `22:48:53Z`; it reached `STOPPED_SAFE` with SQLite integrity `ok`, zero orders, fills, positions, funding settlements, blockers, fatals, and disconnects. Preserve and never resume this database. This is a concrete PAPER scheduler supervision/persistence defect; no venue write occurred and a fresh six-hour run is forbidden before a bounded Builder correction and independent Chief acceptance.
- Accepted main `d88cd4562c28a0fa0e069ee78cae55e2e0034f36` closes that exact supervision gap: an unexpected scheduled-tick exception or cancellation now records sanitized task/class `RUNTIME_FATAL` evidence and terminates fail closed, while an intentional stop-owned cancellation remains `STOPPED_SAFE`. Scope is limited to `runtime.py` and focused/adverse regressions. Independent Chief verification on the unchanged final SHA passed `187` focused/adjacent tests, compile/import/dependency checks, and the clean Python 3.11 full suite with `2699` passed and `3` skipped. Fresh replacement `mainnet-shadow-synthetic-all-routes-20260829-chief23-6h.db` ran from durable `PAPER_RUN_READY` at `2026-08-28T23:16:16.325570Z` through all six hourly public funding boundaries and reached `STOPPED_SAFE` after bounded SIGINT at `2026-08-29T05:24:56.242008Z`. It completed `377` scans and `312` scheduled deadlines, evaluated both RISEx–Nado and RISEx–Extended throughout, and created `20` Extended PAPER entries that all cancelled safely (`5` cutoff, `13` data-stale, `2` route-invalid); Nado produced no entry. Raw expected PnL was positive in `21` evaluations across `12` scans, while the isolated `$0.50` test overlay made `15,276` evaluations positive across `376` scans. Final SQLite integrity is `ok`; active orders/fills/positions/funding settlements/completed trades are `0/0/0/0/0`; blocked/fatal events are `0/0`; socket disconnects/reconnects are `10/10`. Actual PnL, funding, and fees remained exactly zero and never included the overlay. The sanitized untracked report is `mainnet-shadow-synthetic-all-routes-20260829-chief23-6h-report.json`; the database is immutable and must not be resumed. This completes the authorized synthetic PAPER experiment but does not advance either testnet funding-boundary lifecycle. No venue write occurred.
- Git `main` is the accepted-history authority and is published to `origin/main` after every accepted Chief checkpoint. Paper remains the default; public unauthenticated mainnet shadow reads and bounded outbound-only Telegram delivery are authorized, while authenticated/private venue access, writes, real funds, inbound Telegram control, and strategy execution are not.
- RISEx, Nado, and Extended have each completed one historical bounded Level C testnet lifecycle ending with authoritative zero open orders and exact flatness. The user has since authorized exactly two additional testnet funding-boundary routes, RISEx–Nado and RISEx–Extended; no other lifecycle is authorized.
- The accepted tree keeps normal startup isolated from testnet operational runners. Protected credentials and operational journals remain runtime data outside Git.
- The user ended the initial Top-5 public-shadow baseline early after a safe zero-write run and authorized the next public-shadow window over every currently eligible route in `RISEx ∩ (Extended ∪ Nado)`, with liquidity-conditioned measurement rather than liquidity-based truncation. Accepted main `f73c7fb0cd9167d3c54e4a62129295a438694a16` implements the dynamic all-route universe and liquidity-conditioned reporting; its full suite passed `2439`, with `3` skipped.
- The first all-route public preflight evaluated 58 directions across 15 assets (15 Extended pairs and 14 Nado pairs), with all venues `PUBLIC_MARKET_READY`. It found the first two positive planned paper routes, both VVV/Nado in the `< $250k` bucket; best planned net was about `$0.378`, while immediate executable unwind remained about `-$0.855`, so no profitability conclusion follows.
- Accepted main `f856a9931c5fa17e385102038d21ab97b0b582c2` corrects the first observed all-route startup/REST-fan-out/shutdown defects. Independent Chief verification passed `2445` tests with `3` skipped. A fresh preflight then evaluated all 58 directions across 15 assets with all venues ready, zero positive planned routes in that snapshot, SQLite integrity `ok`, and zero orders, fills, or positions.
- The next bounded paper checkpoint reached `PAPER_RUN_READY` only after persisting all 58 startup routes and completed its bounded initial refresh, but a new Extended transport-health blocker prevented the first scheduled FULL scan. Forty-five per-market Extended WebSocket tasks produced a synchronized disconnect/staleness wave; the main tick awaited their recovery sequentially, so neither `PUBLIC_SCAN_DEADLINE` nor `PUBLIC_SCAN_BLOCKED` was persisted. Chief stopped the run at this fail-closed blocker. The journal records `STOPPED_SAFE` 21.25 seconds after SIGINT, SQLite integrity is `ok`, and orders/fills/positions remain zero. This is not a completed statistical window.
- Accepted main `1796ffe272cac2fa91db0042d8a108ddb1577ecf` moves Extended health recovery off the cadence path; independent Chief verification passed `2448` tests with `3` skipped. The `chief03` run persisted 58/58 startup rows and completed seven scheduled FULL cadences before the user stopped it to reduce Telegram output. Its intentional SIGINT then failed to finish within 30 seconds and required an exact-process force stop after Chief verified SQLite integrity `ok` and zero orders/fills/positions. The window is incomplete and the scheduled follow-up was removed.
- The user explicitly changed Telegram presentation to Top-10 ranked routes per FULL digest. All currently eligible directions must still be scanned, persisted, reported, and analyzed by liquidity; this is notification truncation only, not a return to a Top-10 universe.
- Accepted main `2f5467f07caa6807f252655e9e83d91b9596742e` implements that presentation boundary and drains dynamic runtime-owned tasks during shutdown; independent Chief verification passed `2450` tests with `3` skipped. Operational `chief04` persisted all 58 routes, delivered the first 10 ranked rows for FULL, and reached durable `STOPPED_SAFE` about 11 ms after SIGINT with integrity `ok` and zero orders/fills/positions. The superseded `chief05` 24-hour window was stopped early for the notification/shutdown defects recorded below.
- During `chief05`, Telegram incorrectly labeled routine Extended socket churn as `Critical public data loss`. The completed 15-minute watch distinguished two regimes: early episodes recovered in about 2.9 seconds on average, but the window ultimately contained 95 new disconnects with average recovery about 24.2 seconds and maximum about 44.2 seconds. All reconnected, yet one FULL correctly persisted `PUBLIC_SCAN_BLOCKED:PUBLIC_REFRESH_DEADLINE_EXCEEDED`; no confirmation-stale event represented the >25-second pending socket episodes. A preliminary suppress-all-transient candidate is superseded before acceptance: the correction must delay critical severity until the existing 25-second threshold, pair later recovery, and alert precisely on a blocked FULL.
- Intentional SIGINT on `chief05` again failed to finish within 30 seconds despite the prior cooperative-test shutdown correction. Chief verified SQLite integrity `ok` and zero orders/fills/positions, force-stopped only the exact process, and removed the scheduled 24-hour follow-up. Operational shutdown remains blocked and requires a cancellation-resistant adverse regression rather than another cooperative fake-only claim.
- Accepted main `b959da8daa9c1737406fe7ea02e8b01707bf7d0a` preserves every raw socket event but suppresses sub-25-second transient Telegram churn, coalesces a venue-wide persistent socket wave to one Critical plus one Recovery, alerts a blocked FULL with its exact reason, and hands an active tick into owned shutdown drain. Independent Chief verification passed `2463` tests with `3` skipped. Cancellation-resistant external session/WebSocket close remains an explicit operational risk; no timeout or force-close claim was added.
- Fresh public-only `chief06` validation ran from `2026-08-27T13:10:22Z` through the full 15-minute watch and ended `STOPPED_SAFE` 8.3 seconds after SIGINT with `forced_close=false`. It completed nine FULL scans and observed one Nado combined-stream EOF episode lasting about 2.05 seconds; the raw disconnect/reconnect pair remained durable without a transient Critical/Recovery Telegram pair. There were no blocked scans, stale-confirmation events, snapshot-recovery failures, fatal events, or unmatched socket episodes. SQLite integrity is `ok` and orders/fills/positions are exactly zero. This operational checkpoint accepts the notification classification and active-tick shutdown correction; the residual external-close risk remains monitored rather than claimed impossible.
- Fresh isolated `chief07` passed public-only preflight with all 58 current directions and reached `PAPER_RUN_READY`, but was stopped fail-closed after three `PUBLIC_SCAN_BLOCKED:PUBLIC_REFRESH_DEADLINE_EXCEEDED` events. The run recorded 181 Extended socket disconnects and 181 reconnects; one synchronized wave took about 3 minutes 15 seconds to recover, a refresh took 120.825 seconds, and cadence lateness reached 392 seconds. Notification aggregation behaved as accepted, but the per-market Extended public transport still cannot sustain the all-route window. SIGINT recorded `STOPPED_SAFE` immediately with `forced_close=false`; integrity is `ok` and orders/fills/positions are zero. Its obsolete scheduled follow-up was removed.
- At the user's request, an isolated existing-fixture paper lifecycle tested an otherwise-negative observed NEAR/RISEx–Extended direction across the 17:00 MSK funding boundary without changing product code. The synthetic position opened at `13:58:01Z`, recorded one assumed `APPLIED_RATE +$5` leg and one `ESTIMATED +$3` leg at the `14:00Z` boundary, and closed exactly flat at `14:00:01Z`. Price PnL was `-$10`, fees `$0.21`, and simulated closed net PnL `-$2.21`. This proves only before/during/after paper accounting; its assumed funding and fixture prices are not public-market profitability evidence. Separate public scans before and after covered all 58 directions with zero real write-state rows.
- Accepted merge `2e18509f8c619ac329ddf2265acbcc99dde1f418` makes an Extended symbol already in a transport gap skip redundant background REST book/funding fan-out while preserving every route with explicit unhealthy-stream blockers; independent Chief verification passed `2465` tests with `3` skipped. Fresh `chief09` invalidated operational acceptance of the complete cadence path: after READY, 85 Extended disconnects all eventually reconnected, but more than three 120-second scan intervals elapsed with no `PUBLIC_SCAN_DEADLINE`, `PUBLIC_SCAN_BLOCKED`, FULL scan, or deferred-observation event. The starvation occurs before the corrected refresh path. Chief stopped it `STOPPED_SAFE` in about 0.09 seconds; integrity is `ok` and orders/fills/positions are zero. A fresh correction must explain and reproduce this pre-refresh cadence starvation; do not revert the accepted fail-closed transport-gap semantics.
- Accepted main `433afb3965fb3543dc93a66854841e1d57523d0b` adds an explicit event-loop handoff at every Extended WebSocket message boundary after a live-shaped 45-market regression proved ready message queues could monopolize the loop and starve the due-FULL tick. Independent Chief verification passed `2466` tests with `3` skipped. Fresh `chief10` operational validation observed 67 Extended disconnects and 67 reconnects, persisted 30 deferred gap observations, and completed two due FULL scans over all 58 directions. The first deadline was 21.27 seconds late during the initial wave and its FULL completed 34.05 seconds after the scheduled slot; the second deadline was only 0.68 seconds late and its FULL completed 11.57 seconds after schedule. No blocked or fatal event occurred. Intentional stop recorded `STOPPED_SAFE` 2.61 seconds after request and the process exited in 9.54 seconds; integrity is `ok` and orders/fills/positions are zero.
- Fresh isolated `chief11` passed preflight with all 58 current directions, integrity `ok`, and zero orders/fills/positions, then reached `PAPER_RUN_READY` at `2026-08-27T15:22:12Z` with Top-10 Telegram presentation before the user ended the window for Chief rotation.
- The user ended the `chief11` shadow window to rotate Chief and explicitly authorized a new two-route testnet funding-boundary program for RISEx–Nado and RISEx–Extended. `chief11` did not finish graceful shutdown within 30 seconds; Chief verified SQLite integrity `ok` and exact zero orders/fills/positions, stopped only PID `63121`, and deleted its scheduled follow-up. No venue write or candidate is in flight. The new testnet scope may open the smallest venue-executable matched positions even when modeled economics are negative, hold them through an actual venue funding boundary, reconcile the actual testnet funding behavior before/during/after, then close to authoritative zero orders and exact flatness. This is testnet-only and does not authorize mainnet credentials, real funds, or strategy execution.
- In parallel with the two-route testnet objective, the user authorized one fresh two-hour public-mainnet all-route paper observation and a central outbound-only Telegram lifecycle-notification correction. Fresh `chief12-2h` preflight proved 58/58 directions across 15 assets, all three venues `PUBLIC_MARKET_READY`, SQLite integrity `ok`, and exact zero orders/fills/positions. The paper run started at `2026-08-27T15:35:24Z`, reached `PAPER_RUN_READY` at `15:35:37Z`, and has an exact bounded stop/report gate at `17:35:24Z`. Telegram delivery is temporarily disabled because the prior token was exposed in historical process arguments and is not available as a protected input in the new Chief session; it must be rotated and supplied securely before a bounded restart. This does not delay public measurement or authorize inbound control.
- Fresh testnet read-only discovery found Extended `READY` under its accepted strict REST fallback. Accepted main `7666d1c9b0e592f7746f8a4db682d65c71f77677` bounds only Nado `all_products` at 70,000 bytes while retaining the generic 65,536-byte cap; a fresh authenticated read then `FINALIZED` on 94 products with agreeing rounds and no write intent or dispatch. Accepted RISEx corrections `36b6aada9805216cdb5ee3078c908eb74eb304b3` and `4e83d191fe3de20986b2e58d730a7b632debb8bb` validate the completed legacy/two-account histories with nonce uniqueness scoped to each distinct account; after one transient public validation stop, a fresh two-barrier authenticated read `PASSED` with final agreement, authoritative zero-order/flat proofs, and no write dispatch. Staged Nado and Extended funding-boundary candidates are now in isolated Builder worktrees; no venue write is in flight.
- Accepted main `9704da739790b7a26c8720890157e21f32ae7ac3` adds outbound-only paired lifecycle notifications with explicit `PAPER`/`TESTNET` scope, authoritative two-leg open/close/final-flat gates, actual funding status including zero/negative/unresolved, and paired blocker/recovery semantics. It does not add inbound commands or venue writes. Telegram delivery remains operationally disabled until the exposed historical token is rotated and supplied through protected environment variables.
- The bounded `chief12-2h` public paper run completed from `2026-08-27T15:35:24Z` to its authorized `17:35:24Z` stop and recorded durable `STOPPED_SAFE` at `17:36:14Z` without forced close. It produced 64 FULL scans over all 58 directions (3,712 observations), no eligible entry, no fatal event, SQLite integrity `ok`, and exact orders/fills/positions `0/0/0`. Eleven blocked scans and an observed roughly 10m52s FULL delay were traced to repeated synchronous readiness persistence during an Extended heartbeat burst. Accepted published main `c8bcef0118eb4c613b0c5b8ffb63c87b22dceb1e` makes unchanged heartbeats in-memory and persists semantic readiness transitions; independent verification passed `2512` tests with `3` skipped. The sanitized operational report remains outside Git.
- No paper or venue-write process is running. On `2026-08-28` the user supplied a test-only Telegram token; Chief used one bounded operator `getUpdates` read solely to resolve the user's pending `/start`, identified private chat `738925112`, stored token/chat configuration outside Git with mode `0600`, and delivered one sanitized outbound `sendMessage` smoke test successfully (message `5142`). No inbound polling or command process exists, and continuous Top-10/lifecycle delivery remains coupled only to a future separately started PAPER runtime.
- Fresh cross-route review rejected the first staged funding candidates before integration or dispatch: they did not enforce opposite leg direction, their cross-run attestations were not bound to exact authoritative counterpart journals, and Nado could report account funding cash without requiring agreement with its authoritative funding event. The bounded correction worktrees are preserved and idle at the operational blocker; none of the correction candidates is integrated or accepted. Extended ETH-USD remains independently blocked at the live book with four bids and zero asks. No testnet write is in flight.
- Fresh Chief-owned authenticated reads on `2026-08-28` changed the exact blocker without authorizing a write. RISEx `PASSED`; Extended again returned `READY` through its accepted REST fallback, while public `BTC-USD` and `ETH-USD` books had respectively `1 bid / 0 asks` and `4 bids / 0 asks`; Nado `FINALIZED` on 94 products with agreeing rounds, so the earlier `contracts` HTTP 403 did not recur. Accepted main `62fad2fbb27d937db2faa9d95feca10b21d63b0e` adds a fresh Nado funding-boundary contract and operational gate that binds opposite directions, exact canonical quantity, immutable pre-dispatch route/journal identities, account-to-event funding agreement, durable non-applied blockers, applied-only completion, and two agreeing terminal zero-order/exact-flat rounds. Independent Chief verification passed `2542` tests with `3` skipped. No order preparation or dispatch occurred. Nado now blocks on the absent accepted authoritative live funding-event wire adapter; Extended remains blocked on absent ask-side liquidity.
- A fresh Chief Level-B Nado funding-wire discovery on `2026-08-28` used one attempt per required official testnet read and no writes. Protected owner/key identity matched locally, but the live wire gate ended terminal `SCHEMA`; under the no-semantic-retry rule it was not repeated. The authoritative live funding-event/account-history contract is therefore still unproven, no Builder is authorized, and no order preparation or dispatch occurred. A separate one-attempt Extended public observation again found `BTC-USD` at `1 bid / 0 asks` and `ETH-USD` at `4 bids / 0 asks`; both reported zero current funding and a documented next-funding timestamp of `2026-08-28T00:00:00Z`, already stale at the `05:13Z` observation. Extended remains blocked on both absent executable ask-side liquidity and an unusable current funding schedule.
- Separate bounded diagnostics proved the Nado `SCHEMA` verdicts were local decoder mistakes: they expected a combined catalog and then exact incoming key sets instead of the accepted `spot_products`/`perp_products` split with additive-field tolerance. A final one-attempt gate reused the accepted catalog decoder and observed 94 products, 79 perps, two realized hourly rate rows for product 44, and the documented account-history pagination contract. The exact protected subaccount returned zero funding-payment rows with terminal `next_idx=null`, so no account-scoped applied cash or exact event-to-account agreement exists yet. The precise Nado blocker is now `NO_ACCOUNT_FUNDING_RECORD`; this is unresolved, never zero. No Builder, order preparation, intent, dispatch, or venue write occurred.
- Accepted main `0f8a733f572824724075740c1ba9af4efa982b16` adds a presentation-only whole-minute funding countdown to every Top-10 FULL Telegram route row from its persisted authoritative `target_cycle_start`. Missing, malformed, timezone-naive, or expired boundaries display `UNKNOWN`; ordering, PnL/blockers, deduplication, splitting, cadence, persistence, economics, and outbound-only behavior are unchanged. Independent Chief verification passed 58 focused tests and the clean Python 3.11 full suite with `2552` passed and `3` skipped. The replacement `chief17-countdown` PAPER runtime reached `PAPER_RUN_READY`, its first FULL scan covered all 58 routes, and all ten delivered ranked rows rendered numeric countdowns; write state remained zero.
- Accepted main `03ccb2c8d4a8e2fe0cb50397bbd0818be8643e34` renders only the Top-10 FULL Telegram digest as escaped HTML: a bold scan header, UTC timestamp in code style, and compact two-line route cards with bold rank/asset plus code-styled PnL and funding countdown. Lifecycle notifications remain plain text. Independent Chief verification passed 62 focused tests and the clean Python 3.11 full suite with `2556` passed and `3` skipped. Fresh `chief18-html` reached `PAPER_RUN_READY`; its first FULL scan covered all 58 routes, produced one 1,138-character HTML payload with exactly ten ordered cards and ten numeric countdowns, and retained exact zero orders/fills/positions.
- The `chief18-html` process was later externally interrupted without `STOPPED_SAFE` after 113 scans; it is not a completed acceptance window and its database will not be resumed. Its sole PAPER entry intent was durably `CANCELLED` after 59.09 seconds, final runtime state is `FLAT`, fills/positions are zero, and SQLite integrity is `ok`; no venue write occurred. User-authorized replacement `chief19-2h` ran from `2026-08-28T08:05:41Z` to a late manual stop at `10:16:45Z`; the intended two-hour checkpoint was missed by about 11 minutes, so the exact observed duration is 2h11m03s rather than two hours. It ended `STOPPED_SAFE` with `forced_close=false`, SQLite integrity `ok`, 128 scans, ten scans with two eligible directions (four ZEC and six NEAR), two durably `CANCELLED` PAPER entry intents, zero fills/positions/completed trades/funding settlements, seven disconnects and seven reconnects, and no blocked scan or fatal event. The overdue automation was deleted. A fresh one-attempt Extended testnet observation still found BTC-USD at `1 bid / 0 asks` and ETH-USD at `4 bids / 0 asks`, with zero funding and the same stale `2026-08-28T00:00:00Z` next-funding timestamp, so no testnet write or implementation Builder is authorized.
- Independent Nado funding-gate review accepted the external audit with a material amendment. The prior requirement for an existing account payment before first exposure was a liveness defect: official Nado history is account-scoped, so the observed empty baseline is valid pre-entry evidence rather than proof of an unknown schema. The accepted model also incorrectly required aggregate public `funding_payment.payment_amount` to equal individual account cash and attached account quantity/status semantics to the product-level event. `NEXT_TASK.md` now authorizes one fresh venue-local live-adapter correction that persists the pre-entry high-water/cumulative state and requires a newly attributable account row after the target boundary. Missing post-boundary evidence remains fail-closed. No candidate, order preparation, intent, dispatch, or venue write exists yet; Extended remains externally blocked.
- Accepted main `6364c388ce97d3c4f7cbbfa778af5f610ddc6092` implements the Nado venue-local live funding adapter and corrected evidence contract. It accepts an authoritative empty pre-entry history, persists the exact post-entry signed exposure and cumulative side, bounds the public stream by an absolute target-plus-30-second monotonic deadline, keeps product aggregate cash separate from account cash, and requires the account row's signed balance and amount to equal the exact pinned on-chain cumulative settlement calculation. Archive `rate_x18` and `oracle_price_x18` remain strict persisted metadata rather than inputs to an undocumented derived formula. Independent Chief verification passed `485` focused/adverse tests and the clean Python 3.11 full suite with `2592` passed and `3` skipped. No live access, order preparation, dispatch, or venue write occurred. Nado now requires one fresh bounded authenticated read-only preflight before the authorized funding-boundary lifecycle; Extended remains blocked on absent ask-side liquidity and a stale funding schedule.
- The first post-acceptance Nado one-shot authenticated read-only preflight used fresh invocation `nado-read-1d96961ac732670bb0b60490da9049f2` and performed no venue write. Identity derivation, signature recovery, the signed zero-trigger observation, 94-product catalog, zero regular orders, exact-flat account, and zero isolated-position reads completed, but public round B stopped terminal `HTTP` when its fifth request, `contracts`, returned non-200. The durable report is `UNKNOWN/VALIDATION_FAILED`, SQLite integrity is `ok`, and the semantic HTTP failure was not retried inside the gate. Nado testnet dispatch remains blocked pending a separate fresh bounded read-only gate; this does not invalidate the accepted adapter.
- A separate one-attempt public `contracts` diagnostic passed, but fresh invocation `nado-read-5139bc626697367ecefb00a68b494501` then reproduced the same exact round-B `HTTP` stop with 5 attempts and 4 completions; it is also terminal and must not be resumed. Official Nado query weights explain the deterministic failure: the 94-product catalog has 92 orderable IDs, making round A weight `208`, round B weight `213`, and the unpaced two-round sequence `421` against the IP-scoped `400/10s` limit. Accepted main `8d28a006270a7d98472fb4eccc021dda1b26dbd1` adds one Nado-preflight-local cancellable round-boundary cooldown with no retry, counter, semantic, or write-surface change. Independent Chief verification passed `294` focused/adverse tests and the clean Python 3.11 full suite with `2594` passed and `3` skipped. Nado still requires one fresh one-shot authenticated read-only preflight before any order preparation or dispatch; unrelated traffic sharing the same IP can still consume venue quota and cause a terminal HTTP result.
- Fresh invocation `nado-read-da450a7cc3bcb1fdfc2c7b35f1e8ad18` then `FINALIZED` with 94 products, agreeing round tags, exact 7/7 and 8/8 public counters, all identity/signature/zero-trigger barriers complete, and SQLite integrity `ok`; no venue write occurred. Funding-specific discovery established RISEx market 2 and Nado product 4 `ETH-PERP_USDT0`, exact matched quantity `0.1 ETH`. Accepted main `3df9ae0d0c8f87160020cc4bde6cf0b3fbbca2a8`, `42abbdf5a18e3ac8a92b31b2a98adcb21af3cf2c`, and `e59d088ca9710cf9cf96b570ea9a1dac84777a95` provide the venue-local Nado runner, RISEx hold-through-boundary coordinator, and exact pre-write owner. Fresh Level-B gates passed on `2026-08-28`, but the first exact `19:00Z` owner attempt empirically exposed a real-SQLite thread-affinity defect before observation/preparation: Nado's store/journal were created in the caller thread and used by its worker. The owner safely returned `BLOCKED_BEFORE_WRITE`; Nado and both RISEx accounts have zero intents and zero dispatches, all three SQLite integrity checks are `ok`, and no venue write occurred. Failure persistence hit the same affinity defect, leaving the immutable failed Nado DB at runtime `STARTED` and lifecycle `RUNNING`; it will not be resumed or repaired. Accepted main `9131dfc` moves construction, access, durable failure terminalization, and close of the real Nado SQLite resources onto their single worker thread and retains persistence-unknown state for manual recovery. Independent Chief verification passed `285` focused/adverse tests and the clean Python 3.11 full suite with `2686` passed and `3` skipped. The correction authorizes one entirely fresh RISEx–Nado gate and future-boundary lifecycle; it does not authorize reuse of the failed databases. Extended remains externally blocked on zero asks and its stale funding schedule.
- Accepted main `e4eefd2f0fa29207ba3c4ac453bcd5c1cc65c619` rotated only the three fixed funding-boundary store identities to fresh `v2` names; independent Chief verification passed `182` focused/adverse tests and the clean Python 3.11 full suite with `2688` passed and `3` skipped. Fresh gates then reached Nado `FINALIZED` and RISEx `PASSED`. The one `21:00Z` owner attempt stopped terminal `BLOCKED_BEFORE_WRITE`: Nado durably recorded `BLOCKED/UNEXPECTED_FAILURE/ENTRY_PREPARATION`, both RISEx journals recorded `HALTED`, all intents/dispatches/cancels are zero, Nado baseline count is one and exposure count is zero, and all three SQLite integrity checks are `ok`; no venue write occurred. A separate one-shot authenticated read-only diagnostic proved current route amount `0.1 ETH`, plan amount `0.046 ETH`, step `0.001 ETH`, direction/sign, and both entry/close notionals satisfy the live product contract, while persisted cross-bindings agree exactly. The concrete blocker is therefore the real pre-dispatch preparation-gate coordination/classification path, not liquidity, quantity, funding schema, or account exposure. The failed `v2` databases are immutable and must not be resumed, repaired, renamed, deleted, or copied. Extended additionally regressed to `0 bids / 0 asks` on both BTC and ETH and retains the stale `2026-08-28T00:00:00Z` funding timestamp.
- Fresh Builder `01a049f2-ada9-7bc0-bcd2-204941124938` could not reproduce the accepted live preparation-gate failure without fabricating it. Its actual real-component harness stopped sanitized `STARTUP_FAILED` before either `RISEX` or `NADO` arrived at the barrier, so it yielded no valid transition evidence. The entire harness was removed; the branch is clean at `369417fc47b1bb471d764e1c6230e20b3aef3436`, with no production edit, candidate, commit, `v3` rotation, live access, or venue write. The bounded correction did not converge and is stopped; no further Builder or lifecycle attempt is authorized from this evidence.
- Independent takeover diagnostics on `2026-08-29` reproduced the accepted preparation failure with the real RISEx coordinator, owner barrier, Nado runner wrapper, and fresh worker-owned temporary SQLite, without network access or writes. RISEx passed its venue-local binding object into the owner barrier, which correctly emitted `BINDING_MISMATCH` before the target-time check; both venue wrappers then erased that bounded provenance, leaving Nado `UNEXPECTED_FAILURE/ENTRY_PREPARATION` and owner `NADO_FAILED`. Accepted main `b7d61e2997a1c3e999383b9ae89188407911b932` preserves only the closed allowlisted code with fixed `SAFETY/ENTRY_PREPARATION` through both wrappers, peer wake, durable Nado terminalization, and the owner report; unknown exceptions remain generic and redacted. Independent Chief verification passed `184` focused/adverse tests and the clean Python 3.11 full suite with `2701` passed and `3` skipped. No failed operational database was opened or reused, no runtime/store identity was rotated, and no intent, dispatch, cancel, network call, or venue write occurred. The exact remaining Nado blocker is now the proven RISEx-to-owner binding mismatch; correct only that venue-local call contract in a fresh bounded Builder before any `v3` identity or lifecycle. A fresh Extended read still showed BTC `1 bid / 0 asks`, ETH `4 bids / 0 asks`, and the stale `2026-08-28T00:00:00Z` next-funding timestamp, so Extended remains externally blocked.
- Accepted main `f964cf646359c052e74e5c9523f8555cae094c64` corrects only the proven binding call contract: the RISEx coordinator retains its venue-local wrapper for funding/hold validation but passes the identical nested canonical `FundingBoundaryBinding` to the owner preparation barrier. Real-component regressions prove one arrival per side, shared object identity, barrier passage before observation, unchanged local-wrapper mismatch rejection, and target-time `BARRIER_ABORTED/SAFETY/ENTRY_PREPARATION` propagation through both wrappers, peer wake, owner report, and worker-owned SQLite with zero observations, intents, dispatches, cancels, or venue calls. Independent Chief verification passed `186` focused/adverse tests and the clean Python 3.11 full suite with `2703` passed and `3` skipped. No live access, operational database access, identity rotation, or venue write occurred. The next Nado slice is a separate fresh source-bound `v3` store-identity rotation, followed by new RISEx and Nado Level-B gates; no dispatch is authorized before both gates agree.
- Accepted main `c56f22f36246122715814091d36b7e73798b5deb` rotates exactly the Nado and two RISEx funding-boundary source identities from immutable `v2` to unused `v3` names, with no migration, fallback, or access to any operational database. Regressions reject both `v1` and `v2` as active identities and keep primary/counterparty domains distinct. Independent Chief verification passed `186` focused/adverse tests and the clean Python 3.11 full suite with `2703` passed and `3` skipped. No runtime, network, credential, intent, dispatch, cancel, or venue write occurred. Fresh RISEx and Nado authenticated Level-B gates against the `v3` source identities are now the only next action; Level C remains closed until exact agreement.
- Fresh `v3` Level-B evidence then passed: RISEx invocation `risex-read-02077c62cbb700f6040450455aacfbf2` proved both fixed accounts flat with zero orders and agreeing public/authenticated rounds; Nado invocation `nado-read-fbb373211616f9eafb2ad2c8d6ae0643` finalized on 94 products with zero regular/trigger orders and agreeing rounds. Funding-specific reads bound Nado product 4 `ETH-PERP_USDT0`, the common exact `0.1 ETH` quantity, an authoritative empty account-history baseline, fresh cumulative funding state, sufficient collateral, and the future `2026-08-29T09:00:00Z` target. The one resulting Level-C owner attempt is failed, not accepted: the RISEx counterparty `ENTRY_MAKER` was accepted once and filled externally before the planned primary taker dispatch, while the first exact/list reconciliation returned `RISEx exact order/list disagreement`. The owner halted `ASYMMETRIC_PARTIAL_EXPOSURE`; RISEx primary and Nado never dispatched and remained flat with zero orders, while counterparty held exactly `-0.1 ETH`. Chief performed one durable single-shot reduce-only recovery `BUY MARKET IOC 0.1` under fresh intent `9f0334ad-fe50-4a4b-98c3-77c265a9319c`; accepted order `0x00000000000339ec00000000032736b300000000000000a8` filled exactly `0.1`, and two later REST rounds independently proved both RISEx accounts flat with zero open orders and no unrelated state. No entry was replayed. The three `v3` databases are now immutable incident evidence and must never be resumed or reused. The concrete RISEx blocker is the accepted-order reconciliation path rejecting a newly accepted order that exact lookup already knows but paged order history has not yet published; correct only this observed propagation case in one fresh Builder before any identity rotation or further lifecycle. Extended remains blocked on absent asks and stale funding schedule; no write is in flight.
- Accepted main `9eec63ca26b2bffc7dcc2ec04c8c732c11597dd0` classifies only that missing-list state through the existing exact accepted-order propagation exception, so the existing intent/pair allowlist and durable one-use resample gate can perform one fresh observation. It never returns the incomplete observation; second omission/exhaustion, unrelated identities, immutable disagreement, non-monotonic status/fill, and a maker already externally filled remain fail-closed before primary dispatch. Independent Chief verification passed `260` focused/adverse tests and the clean Python 3.11 full suite with `2705` passed and `3` skipped; dependency, import-source, compile, diff, and Git checks passed. No network, credentials, operational database, identity, or venue write was used. The next bounded slice is a separate fresh source-bound identity rotation away from immutable `v3`, followed by entirely fresh Level-B gates; Level C remains closed.
- Accepted main `0aa69acfb0a2bd5e886e48212b1582d0acd5a863` rotates only the Nado and two RISEx funding-boundary source identities from immutable `v3` to unused `v4`; v1/v2/v3 are rejected as active identities and no migration or compatibility path exists. Independent Chief verification passed `186` focused/adverse tests and the clean Python 3.11 full suite with `2705` passed and `3` skipped. Fresh v4 Level-B gates then passed: RISEx `risex-read-e7ef86b1f1a8ed6753f1831175ea243f` reached `PASSED`, Nado `nado-read-c040a8b7e35fd84e0dfb3ca7813ea5c2` reached `FINALIZED` on 94 products with agreeing rounds, and funding-specific reads proved exact `0.1 ETH`, zero orders/positions, empty-terminal Nado baseline, sufficient collateral, executable books, and common `2026-08-29T09:00:00Z` schedule. The one v4 Level-C run nevertheless failed before funding: the RISEx maker was externally filled while both the first list read and the one immediate resample still omitted its accepted identity, so RISEx halted before primary dispatch. Nado durably reconciled one `-0.1 ETH` entry and one reduce-only `+0.1 ETH` close before the boundary and ended flat; this is no funding evidence. Chief reconciled the RISEx external maker fill and dispatched one fresh durable reduce-only recovery `BUY MARKET IOC 0.1`; exact order/trade evidence and two full REST rounds prove both RISEx accounts flat with zero orders, while two paced Nado rounds prove zero regular/trigger orders and exact flatness. All three v4 databases have integrity `ok`, remain `0600`, and are immutable failed-incident evidence. No write was replayed and no process remains. The newly proven defect is timing, not classification: the allowed resample is immediate although venue history propagation remains incomplete until a later read. Correct only this path by adding one bounded cancellable settle delay before the already-single resample; do not add attempts, return incomplete evidence, or change recovery semantics.
- Accepted main `6fd9dd3b5fa75f50c418ec6d1cf362623fa2b45d` added the bounded cancellable five-second settle before the existing one-use RISEx resample; Chief verification passed `261` focused/adverse and `2706` full-suite tests with `3` skipped. Accepted main `ca7a8bd9be72ec3480d197188de3335153cbc8e0` then rotated only the three funding-boundary stores to unused `v5` identities; Chief verification passed `186` focused/adverse and the same clean full suite. Fresh RISEx and Nado generic/funding-specific Level-B gates agreed on exact zero/unrelated state, executable `0.1 ETH`, sufficient test collateral, two-sided books, and the authoritative `2026-08-29T10:00:00Z` boundary.
- The single v5 Level-C owner failed before funding. RISEx maker and taker both filled exactly `0.1 ETH`, but even the delayed one-use resample still observed the exact accepted order missing from paged history and halted `ASYMMETRIC_PARTIAL_EXPOSURE`; Nado had already reconciled SHORT `0.1` and its reduce-only close, ending terminal `BLOCKED/UNEXPECTED_FAILURE/FINAL_BARRIER`. Chief used only fresh durable reduce-only recovery identities: the RISEx counterparty close filled once, then a primary-only close filled once after the first recovery reader halted on a sanitized JSON-read failure. Subsequent exact order/trade/position evidence and two fresh RISEx REST rounds plus two paced Nado rounds agree on zero regular/trigger/open orders, no unrelated state, and exact flatness. All three v5 databases are mode `0600`, integrity `ok`, permanently frozen, and the route is failed with funding `UNRESOLVED`, not accepted. No runtime is active. The remaining observed defect is strictly the RISEx accepted-order list propagation bound: five seconds was insufficient although later exact/history/trade/position reads agreed. No new lifecycle or identity rotation is authorized before one fresh bounded RISEx Builder correction and independent Chief acceptance. Extended remains blocked on absent ask-side liquidity and stale funding schedule.
- Accepted main `a4e44dd01801304666ef87a7acda226ecb62eeef` increased only the existing cancellable accepted-order propagation settle to 60 seconds; accepted main `e051b8e1b5348e0c2645f4b05cc5a1c789866291` then rotated only the three source-bound stores to unused `v6` identities. Fresh RISEx and Nado Level-B gates passed. The one v6 Level-C owner proved that the longer settle resolves list publication, but both RISEx `0.1 ETH` entry legs filled against distinct external counterparties. Their authoritative quantities and directions matched, yet the coordinator required one shared mutual trade ID and halted before funding. Nado had entered SHORT `0.1`; its first two appendix-2561 reduce-only IOC closes were terminal venue rejections with code `2056`, leaving the position unchanged. This route is failed and funding remains `UNRESOLVED`.
- Official Nado CLI behavior proved the funding-route close price was less aggressive than the venue's own 2% close convention. Accepted published main `59ff1109d44235b61061cd44f357181c131f4ed4` corrects only that Nado funding-boundary close pricing while preserving appendix 2561, reduce-only, IOC, direction, quantity, durable identity, and no-replay gates. Independent Chief verification passed `11` focused/adverse tests and the clean Python 3.11 full suite with `2711` passed and `3` skipped. One fresh third Nado recovery close was durably prepared and dispatched exactly once; it filled `+0.1 ETH`, and two authoritative terminal rounds agree on zero regular/trigger/isolated orders and exact flatness. Both RISEx entries and their fresh single-dispatch reduce-only recovery closes are terminal, exactly filled `0.1`, and reconciled. All three v6 SQLite journals have integrity `ok`, no runtime is active, and the stores are permanently frozen.
- Accepted published main `e7a5b5947e31f0f48dbb9b4457e673e171051be8` corrects only the observed RISEx mutual-fill rule. Each leg is now independently bound to its exact sealed account/intent/order/market/client/nonce, accepted exactly-once dispatch, terminal fill, quantity, direction, trade ID, and price; equal canonical quantity and opposite directions remain mandatory, while one shared cross-leg trade ID or execution price is no longer fabricated when external counterparties fill the legs independently. Entry recovery and final reconciliation preserve each journal's own evidence. Independent Chief verification passed `249` focused/adjacent tests and a clean isolated Python 3.11 full suite with `2725` passed and `3` skipped; compile, import-source, dependency, diff, and Git checks passed. Open exactly one fresh Nado Builder to rotate only the three immutable v6 source-bound stores to unused v7 identities. No lifecycle or Extended write is authorized before that candidate is independently accepted and fresh Level-B gates agree.
- Accepted published main `1a32d7fb406d5120c61d968dcce7f0df82443ab2` rotates only the Nado and two RISEx funding-boundary source identities from frozen v6 to unused v7 names; v1 through v6 are rejected as active and the two RISEx identity domains remain distinct. Independent Chief verification passed `190` focused tests and the clean isolated Python 3.11 full suite with `2725` passed and `3` skipped; dependency, import-source, compile, diff, and Git checks passed. Run entirely fresh RISEx and Nado authenticated Level-B gates against v7 without preparing an order. Only exact identity, zero/unrelated state, market, matched quantity, executable liquidity, collateral, and authoritative future funding schedule/source agreement may open one future-boundary Level-C owner run.
- Fresh RISEx and Nado Level-B gates passed on `2026-08-29`: two RISEx rounds agreed that both fixed accounts are exact flat with zero open orders, unchanged history, positive free collateral, and normal risk; two Nado rounds agreed on product 4 `ETH-PERP_USDT0`, two-sided executable BBO, zero regular/trigger/isolated orders, exact flatness, and a valid account funding-history high-water baseline. Exact `0.1 ETH` is step-aligned and above the live `0.042 ETH` minimum plan. A final pre-dispatch audit nevertheless kept Level C closed: the accepted RISEx wrapper unconditionally records `UNRESOLVED` because it has no applied-funding wire. Separate bounded official testnet reads then proved market 2 publishes a fresh hourly `next_funding_time` and continuous settled `/v1/markets/id/2/funding-rate-history` rows with exact interval, rate, and index price. Open one fresh RISEx Builder to bind that official applied-rate evidence to both exact opposite account exposures and the persisted boundary without claiming account-ledger cash. Missing or contradictory history must remain a blocker but must not prevent reduce-only close and final flat reconciliation. The unused v7 store identities remain valid; no order intent or dispatch exists.
- Accepted published main `6244d7de5eefb37f75c2c9eba62c63c28774bca6` adds only the exact RISEx market-2 applied-rate history wire. It durably binds the persisted hourly boundary, exact `0.1 ETH` opposite exposures, one settled official row plus two cadence anchors, shared row identity/digest, and separately signed derived rate cash under `APPLIED_RATE`; it never claims account-ledger cash. Missing, stale, duplicate, wrong-market, malformed, or contradictory evidence remains terminal `BLOCKED` while the accepted reduce-only close and terminal flat barrier continue. Independent Chief verification passed `287` focused/adverse tests and a clean isolated Python 3.11 full suite with `2736` passed and `3` skipped; 38 module imports, compile, dependency, exact transport-surface, diff, ancestry, and Git checks passed. The v7 stores remain unused. Because this code change invalidates the earlier Level-B evidence, Level C stays closed until entirely fresh RISEx and Nado generic plus funding-specific read-only gates agree on a future boundary and executable state.
- Accepted published main `3e341178b6a9027b0bc81dc37dd3a86991a55631` corrects only the proven RISEx GET chunked-body defect by bounded draining decoded chunks through authoritative EOF under the unchanged hard cap. POST/write behavior, allowlist, deadlines, initial-plus-one retry, funding semantics, signing, execution, and v7 identities are unchanged. Independent Chief verification passed `33` exact transport tests, `295` adjacent RISEx/Nado adverse tests, and the clean Python 3.11 full suite with `2744` passed and `3` skipped; compile, 38-module import, dependency, diff, ancestry, and worktree checks passed. Entirely fresh gates on this SHA then passed: RISEx `risex-read-794af708735d57e4ced39dba41599bad` reached `PASSED`, Nado `nado-read-5bc62261b2bf1f2ca16cdd515e450cf1` reached `FINALIZED` on 94 products, two RISEx funding rounds agreed on zero orders/exact flatness and positive collateral, and the exact 100-row history proved continuous hourly cadence with next boundary `2026-08-29T13:00:00Z`. Two Nado funding rounds for product 4 agreed on zero regular/trigger/isolated orders, exact flatness, stable cumulative funding, executable two-sided `0.1 ETH`, high-water `158816239`, and positive USDT0 collateral. All v7 stores remain absent and no write occurred. Extended remains externally blocked despite fresh private `READY`: BTC and ETH still have respectively `1 bid/0 asks` and `4 bids/0 asks`, with stale funding timestamp `2026-08-28T00:00:00Z`.
- The one v7 RISEx–Nado Level-C owner run is failed, not accepted. The RISEx counterparty maker was accepted once and filled externally before its mandatory resting proof, so the owner halted `ASYMMETRIC_PARTIAL_EXPOSURE` with primary flat/no intent and counterparty exactly `-0.1 ETH`; funding was never reached. Nado independently entered SHORT `0.1` and reduce-only closed in four exact archive fills; Chief reconciled the already-authoritative close locally with no new Nado write, leaving both intents `RECONCILED`, lifecycle `HALTED`, and runtime `BLOCKED/UNEXPECTED_FAILURE/FINAL_BARRIER`. Chief then recovered only the RISEx counterparty through fresh durable intent `f843f0cf-049e-4ad4-ba50-ec389e96ad2a`, one accepted reduce-only `BUY MARKET IOC 0.1` dispatch, and exact terminal order/trade/position evidence. Two later RISEx REST rounds share fingerprint `00293dde14aac5e5d06c736c1d9fa55f6dc01a40f6caf53e95d671ef74509389`; two later Nado rounds share fingerprint `1e52e27d746e4a8a8b47468878bebfeae1ed4c3eb2b618ad517c71f38d811d89`. Both venues are exact flat with zero relevant/trigger/isolated orders and no unrelated state. All three v7 stores are mode `0600`, integrity `ok`, permanently frozen, and no process remains; funding is `UNRESOLVED`. The exact blocker is the accepted RISEx gate requiring an OPEN resting maker even when authoritative exact order, trade, full-fill, zero-open-order, and account-position evidence prove that the just-accepted maker filled externally before the primary dispatch.
- Accepted published main `6fa027173f8ccf931a2026476e807ac5d597f704` corrects only that observed external-maker gate. After one accepted dispatch, the existing fresh authoritative observation may continue only on the exact sealed terminal `0.1 ETH` maker order, one exact trade, matching `-0.1` position, zero open orders, unchanged identity/economics, and no unrelated state. Terminal fill state, `trade:ENTRY`, and `price:ENTRY` are committed atomically before primary preparation; identical restart evidence is idempotent and changed trade identity/price fails closed before any primary dispatch. Partial, rejected, cancelled, ambiguous, stale, duplicate, contradictory, or unrelated evidence remains blocked, and no read or write attempt was added. Independent Chief verification passed `315` focused/adjacent tests and a clean isolated Python 3.11 full suite with `2764` passed and `3` skipped; dependency, compile, diff, ancestry, and Git checks passed. Open one fresh Nado Builder to rotate only the three frozen v7 source-bound stores to unused v8 identities before any fresh Level-B gate.
- Accepted published main `42ab0b3d04d85a1aaf4e3cb24e8bc0b9fe2a9886` rotates only the Nado funding-boundary store and the two distinct RISEx journals from frozen v7 to unused v8 identities; v1 through v7 are rejected as active and there is no migration path. Independent Chief verification passed `201` focused/adverse tests and a clean isolated Python 3.11 full suite with `2764` passed and `3` skipped; dependency, compile, diff, ancestry, and Git checks passed. Entirely fresh Level-B gates then passed: RISEx `risex-read-5ac7f279592a12438d33998e8d0bbcb4` reached `PASSED`, Nado `nado-read-3c8798bbe53fa128f847fc5d258fdbe8` reached `FINALIZED` on 94 products, two RISEx funding rounds agreed on exact flatness, zero orders, stable history, positive collateral and BBO `2427.91/2428.65`, and official history/schedule selected `1788012000000`. Two Nado rounds agreed on product 4 `ETH-PERP_USDT0`, zero regular/trigger/isolated orders, exact flatness and two-sided BBO; the funding baseline high-water is `158816239`, spot collateral is positive, and live smallest executable is `0.042 ETH`, below the fixed matched `0.1 ETH`. All v8 stores remain absent and no write occurred. One exact future-boundary v8 owner run may proceed under the existing Level-C barriers.
- The one v8 owner run is failed, not accepted, and funding remains `UNRESOLVED`. Both RISEx entries filled exactly `0.1 ETH`; Nado entered SHORT `0.1` and reduce-only closed. Before the actual `1788012000000` wall-clock settlement, Nado accepted a public funding event whose embedded timestamp fell in the target interval and emitted its release signal without requiring current time to have reached settlement. RISEx therefore invoked its gate early and correctly blocked `RISEX_BOUNDARY_EVIDENCE_STALE`. Its counterparty reduce-only `EXIT_MAKER` then filled externally before the mandatory OPEN proof and was left durable `DISPATCHED`, causing `FAILED_HALTED_MANUAL_RECOVERY`. Chief reconciled that exact exit fill locally, then dispatched one fresh primary reduce-only recovery `SELL MARKET IOC 0.1` under intent `3c049a81-6e17-44e2-8c81-c23c288f8f77`; it filled once. Two RISEx terminal rounds share fingerprint `1bc3a2332277b404419726cac0c2c4f3b6a9c958fcc6b10e000ce7bff2cbd481`; two Nado rounds share `8eb2d687539b77460b55faa3a58bc0cf2efd3e7c4b95d359a2d7ff2ffedfe7e4`. Both venues are exact flat with zero relevant/trigger/isolated orders and no unrelated state. All three v8 stores are mode `0600`, integrity `ok`, permanently frozen, and no process remains. The two independent blockers are the RISEx external-filled exit-maker proof and the Nado missing wall-clock settlement barrier before event acceptance/release.
- Accepted published main `5cc5d3073ac032579c0a27f452a737d8fb7a04b5` closes the Nado early-release defect: an in-interval public funding event observed before authoritative wall-clock settlement is not cached or released, the same bounded stream connection continues, and the existing absolute deadline fails closed if no post-settlement event arrives. Independent Chief verification passed `242` focused/adjacent tests and a clean isolated Python 3.11 full suite with `2767` passed and `3` skipped; dependency, compile, diff, ancestry, and Git checks passed.
- Accepted published main `378cfdc59b06c5d2ddfc275a4cf7343d8243e6c8` closes the RISEx external-filled exit-maker defect. After one accepted reduce-only `EXIT_MAKER` dispatch, only an exact terminal `FILLED 0.1` order, one bound trade, counterparty exact flatness, zero opens, and unchanged authoritative identity/economics may atomically seal the intent plus `trade:EXIT` and `price:EXIT` before primary exit preparation. Restart evidence is idempotent; changed or non-exact evidence halts before primary dispatch. Independent Chief verification passed the `260`-test coordinator module and a clean isolated Python 3.11 full suite with `2793` passed and `3` skipped; dependency, compile, diff, ancestry, and Git checks passed. Both v8 blockers are corrected in published code, but v8 remains permanently frozen. No lifecycle write is authorized until a fresh Builder rotates all three source-bound stores to unused v9 identities and entirely fresh Level-B gates pass. Extended remains read-only and blocked on absent asks and stale schedule.
- Accepted published main `31677e242e19f7eb91003808fb24b540c22e222a` rotates only the Nado funding store and two distinct RISEx funding-boundary journals from frozen v8 to unused v9 identities; v1 through v8 are rejected and no migration or compatibility path exists. Independent Chief verification passed `204` focused/adverse tests and a clean isolated Python 3.11 full suite with `2793` passed and `3` skipped; dependency, compile, diff, ancestry, and Git checks passed. All v9 stores are unused. Entirely fresh RISEx and Nado generic plus funding-specific Level-B gates must pass before any v9 lifecycle write. Extended remains read-only and blocked on absent asks and stale schedule.
- Fresh v9 Level-B gates passed on `2026-08-29` without opening any v9 lifecycle store or preparing an order. RISEx generic invocation `risex-read-a1e75f075c8fdcb1151eb74eaa0d1b66` reached `PASSED` with identical public/private barrier fingerprints; Nado invocation `nado-read-8edc836d1b89ad9a9adb73621192d420` reached `FINALIZED` on 94 products with matching rounds and the accepted collateral floor. Two RISEx funding rounds share fingerprint `1c81a1614b62716595f9eb4a9a31c4c523484d084b08ca3f4c23c9ae3a18fbc9`: both fixed accounts are exact flat with zero opens, positive collateral and a two-sided market-2 book; 100 continuous official hourly rows agree with future boundary `1788015600000`. Nado funding evidence fingerprint `6491c93c50a222f06dd146bdb0f019ff2718ac18b7259fc85c2fdc068d5f59ef` binds product 4 `ETH-PERP_USDT0`, zero regular/trigger/isolated state, exact flatness, stable high-water `158816239`, a two-sided book, and live minimum `0.042 ETH`, below the fixed matched `0.1 ETH`. One exact v9 Level-C owner run may proceed for that future boundary under the existing no-replay, funding-evidence, reduce-only close, and two-round terminal barriers. Extended remains read-only and blocked.
- The one v9 owner run is failed and permanently frozen. Both matched `0.1 ETH` entries filled and reconciled, but Nado's funding WebSocket handshake failed with HTTP `403` before the boundary, so the owner emitted only a coordination `CANCELLED` and both venues executed their accepted reduce-only flattening paths. Nado entry `-0.1` and close `+0.1` are reconciled in three exact fills; RISEx has four terminal single-dispatch intents and both journals are `COMPLETE`. Two later RISEx REST rounds share fingerprint `7e539bbb9bcc7a4d4ce566753530926b6bf7d598ece417bb2d741ddfc77f9f32`; two later Nado rounds share `f24dee2830df2c13314e7b8d56a890e7409fc409bf0b035674b63d8d3b3c7c7b`. Both venues are exact flat with zero relevant/trigger/isolated orders and no unrelated state. All three v9 stores are `0600`, integrity `ok`, and no process remains. Funding is `UNRESOLVED`; the route is not accepted.
- Official Nado subscription documentation requires `Sec-WebSocket-Extensions: permessage-deflate` on `wss://gateway.test.nado.xyz/v1/subscribe`; a bounded public diagnostic reproduced HTTP `403` without compression and completed the handshake with compression before timing out normally while waiting for a future event. Accepted published main `baf60b7ced1dd5293cc9516f8298cc4a1059ff93` adds only `compress=15` to the existing single bounded funding WebSocket connection; no retry, fallback, funding semantics, or write surface changed. Independent Chief verification passed `124` focused/adverse tests and a clean Python 3.11 full suite with `2794` passed and `3` skipped, plus dependency, compile, diff, ancestry, and worktree checks.
- Accepted published main `fb189a5aaf3cb5af1322a02b57da74ef6eb8ceee` corrects only the observed RISEx timestamp-resolution mismatch. Signal, callback-reference, and terminal-observation timestamps within `[current_second*1000, (current_second+1)*1000)` are no longer misclassified as future; the exact next-second boundary and the existing stale-age cutoff remain fail-closed. Independent Chief verification passed `84` focused/adverse tests and a clean Python 3.11 full suite with `2797` passed and `3` skipped; dependency, compile, diff, ancestry, and worktree checks passed. Both v9 defects are corrected in published code, but v9 remains permanently frozen.
- Accepted published main `3eef78d8a84d283148394dafb940a362c5bebf94` rotates only the Nado funding store and the distinct RISEx primary/counterparty funding-boundary journals from frozen v9 to unused v10 identities; v1 through v9 are rejected and no migration or compatibility path exists. Independent Chief verification passed `208` focused/adverse tests and a clean Python 3.11 full suite with `2797` passed and `3` skipped; dependency, compile, diff, ancestry, and surface checks passed. The first fresh generic RISEx gate then terminalized with an unclassified post-public-B failure; separate bounded public diagnostics passed current schema/identity and freshness.
- Accepted published main `5f1b377a71bcab8b5ed315de527fe0d497435716` durably classifies only failures after all public-B responses complete and before barrier persistence as fixed sanitized `TRANSPORT`/`HTTP`/`SCHEMA`/`AUTH`/`IDENTITY`/`SAFETY` plus exact validation/freshness/fingerprint stage. Raw payloads, URLs, secrets, and exception text remain excluded; old rows remain readable and forged class/stage tokens reject on reopen. Independent Chief verification passed `539` focused/adjacent tests and a clean Python 3.11 full suite with `2810` passed and `3` skipped; dependency, compile, diff, ancestry, and surface checks passed.
- Entirely fresh generic and funding-specific v10 Level-B gates passed without creating a v10 lifecycle store or preparing an order. RISEx invocation `risex-read-1dd450e92f9d1b6e23bebee61fb8cfbe` reached `PASSED`; Nado invocation `nado-read-f82908d9d7fbdfd523feb622b41c0af0` reached `FINALIZED` on 94 products. Two RISEx funding rounds proved both fixed accounts flat with zero opens, positive free collateral, a two-sided market-2 book, and 100 continuous official settled hourly rows with future boundary `1788019200000`. Two Nado rounds agreed on product 4 `ETH-PERP_USDT0`, zero regular/trigger/isolated orders, exact flatness, stable high-water `158816239`, stable cumulative funding, a two-sided book, and live minimum `0.041 ETH`; fixed matched `0.1 ETH` was executable.
- The one v10 owner run is failed, safely recovered, and permanently frozen. All three entries filled and reconciled at the matched `0.1 ETH`, but the owner reached its 900-second orchestration timeout without durable funding completion. Nado remained `RUNNING` with no persisted funding evidence or blocker; the original RISEx journals later sealed `UNRESOLVED/RISEX_BOUNDARY_GATE_INTERRUPTED`. Recovery used exactly one fresh Nado reduce-only close and two fresh RISEx reduce-only closes, each dispatched once and reconciled. Two final authoritative rounds agree on exact flatness, zero relevant/trigger/isolated orders, and no unrelated state: RISEx fingerprint `dbc987711bc62c75e298d76551f5c22d6ed911ef42372323f3a5065ae291ef4e`; Nado fingerprint `ee32904739859209a4ff22272bae8d8792619c64f74919cceded0183fee5f512`. Post-boundary read-only evidence subsequently found the exact Nado account funding row after high-water `158816239` (`idx 158926135`, timestamp `1788019200`, amount `+16535427682794 x18`) and the exact RISEx settled market-2 row (`1788015600000000000` to `1788019200000000000`, rate `-0.00023501248210369`, index `2444.855019301005`), but this does not retroactively satisfy the interrupted durable lifecycle. The five frozen stores are mode `0600`, integrity `ok`; their SHA-256 values are `a535a2f24df6173543d208dfdd6d1bfb31673951db97b338be179e48b0226210`, `da2839d6589854c3dbc5f01ed71f6e14b23d71464e356dc20627c08e2d2ce4ac`, `27d4f284516e162013fa677d2f1f80d38a0b4945133eb024fb02235f39786c92`, `ced52f4c0c14dbd2b895a94aad1f98122bf615e54ecdfd0d98d0a494bbf4dff4`, and `4bc409f25c7d5438f6feebc460bff2e5ce9d800c790ebcfca703cf69b23c5520`. Both Nado runtime identities are terminal `BLOCKED/UNEXPECTED_FAILURE/FINAL_BARRIER`; its lifecycle is `HALTED`. No process remains and no further v10 write is authorized. Extended remains read-only and blocked.
- Bounded read-only v10 provenance established the last durable pre-timeout state as Nado exposure bound plus RISEx callback invocation `STARTED`, with no durable release/cancel signal, funding result, blocker, or close intent. A public-only live probe of the accepted compressed Nado stream exhausted its no-event settlement-plus-30-second deadline correctly in `32.008s`, so the general WebSocket deadline path is not reproduced as defective. The remaining accepted-source gap is concrete and narrower: no durable checkpoint distinguishes public-event wait, accepted event, relay publication, and account-history read. Separately, the operational owner wrapper mishandled the documented non-cancelling 900-second orchestration timeout by exiting instead of continuing `retrieve`; daemon ownership then ended without a terminal checkpoint. Until the venue-local progression provenance gap is corrected and independently accepted, the route may not be retried or rotated.
- Accepted published main `5303c2927d712e2465f13e0c2ce033c00cf622ca` closes that Nado progression-provenance gap. A new tracked exposure atomically binds fixed `REQUIRED` progression semantics inside its immutable digested exposure payload; append-only closed-world tokens distinguish public-event wait/acceptance, relay publication start/acceptance with fixed kind, and account-history read start/completion. Reopen rejects missing, reordered, contradictory, replayed, or markerless non-empty progression and rejects deletion of every optional progression table for tracked exposure; genuine legacy no-marker stores remain readable without migration or rewrite. The orchestration timeout remains deliberately non-cancelling, and a worker-owned continued-retrieval regression proves the owner can retain and later close the worker. Independent Chief verification passed `198` focused/adverse tests, dependency/compile/import checks, and a clean Python 3.11 full suite with `2819` passed and `3` skipped. A byte-copy of the frozen v10 Nado store reopened `HALTED` with legacy empty progression and remained byte-identical; the original SHA-256 stayed `a535a2f24df6173543d208dfdd6d1bfb31673951db97b338be179e48b0226210`. No operational DB, credential, network, or venue write was used by the Builder. v10 remains failed and frozen; no v10 replay is authorized.
- Accepted published main `81e6f48a1e45b5239fa4387866d05425d98db528` rotates only the frozen v10 Nado funding store and the two distinct RISEx funding-boundary journal/store identities to unused v11 names. v1 through v10 are rejected as active and primary/counterparty domains remain distinct; no migration or compatibility execution path exists. Independent Chief verification passed the `3` exact identity tests, `215` affected-module tests, dependency/compile checks, and a clean Python 3.11 full suite with `2819` passed and `3` skipped. The v11 stores are absent and no operational access or venue write occurred.
- Fresh generic v11 prerequisite gates passed sequentially without creating a v11 lifecycle store or preparing an order. RISEx invocation `risex-read-16ecb9e07418d0acb7b3709b385eca6a` reached `PASSED` with complete public/private counters and final agreement; Nado invocation `nado-read-e1a4471032ebebec45afba07b7101c09` reached `FINALIZED` on 94 products with matching round tags. Funding-specific two-round state/liquidity/schedule agreement remains required before any Level-C authorization.

## Venue completion

### RISEx — Level C complete

- The accepted sealed ETH/USDC two-account coordinator uses separate durable identity domains for the primary and the one authorized isolated counterparty. Signed place/cancel writes use REST; public and authoritative read-backs use REST; authenticated private WebSocket evidence supplements but does not replace REST correctness.
- Entry completed with one counterparty `SELL LIMIT+GTC+postOnly` of `0.1 @ 2363.04` and one primary `BUY MARKET+IOC` of `0.1`; exact mutual order, trade, fill, and position evidence agreed. Exit completed with one counterparty reduce-only `BUY LIMIT+GTC+postOnly` of `0.1 @ 2599.85` and one primary reduce-only `SELL MARKET+IOC` of `0.1`; exact mutual evidence again agreed.
- Both journals are `COMPLETE`. Each contains two terminal intents with dispatch count one, total dispatch count two, and zero cancels. Two ordered final REST rounds have the same digest on both accounts. A separate fresh REST plus private-WebSocket observation confirmed zero open orders, exact flatness, no unexplained state, and nonce `(1,7)` on both accounts.
- Active journal SHA-256 values are `04a2957758a6fab646a0b213bd42eb2b4b16304ca6b55c9cec5a9ac5b6353776` for primary and `0f273ed5116e01d9ba9eabf02f5e8d435371e952daaa58726b3ea243c5c4d3fa` for counterparty; both pass SQLite integrity checks. Protected byte-identical pre-recovery backups preserve the post-exit halted state. No trade order was replayed and no recovery venue write occurred.

### Nado — Level C complete

- One entry and one guarded reduce-only close filled. Independent authoritative evidence proves zero regular orders, zero trigger orders, and exact flatness. The durable lifecycle is complete; no new Nado lifecycle is authorized before the strategy gate.

### Extended — Level C complete

- Entry order `2092620936462331904` and reduce-only close `2092632701983932416` filled and reconciled. Two authoritative final rounds prove zero orders, exact flatness, and no unrelated state. The active database is `COMPLETE` with SHA-256 `4c5dacadd83a222cd8cde7297aa4f54cc6f3bda80bd8018d6acfa7adf3e105d1`.
- The accepted strict REST-only fallback is testnet-only while the entire testnet WebSocket ingress is unavailable. Extended private WebSocket proof remains mandatory for any future mainnet Level D claim.

## Bounded 3/3 commonality review

- Common proven safety semantics are: durable unique intent identity before dispatch, one-way dispatch state, no blind replay, exact authoritative order/fill/position reconciliation, unrelated-state rejection, reduce-only flattening, and two ordered agreeing terminal zero-order/exact-flat rounds.
- Common measurement inputs are funding, fees, executable top-of-book depth, spread/slippage, response and execution timestamps, stale-data status, position/order state, and reconciliation outcome. These can share a normalized observation/report boundary without sharing venue signing or write engines.
- Venue authentication, signing, nonce/wire identity, order/cancel/close encoding, pagination, private-event transport, and recovery rules remain materially different and must stay venue-specific. The 3/3 evidence does not justify a generic OMS or a shared execution framework.
- Infrastructure expansion stops here. The next work uses real unauthenticated mainnet market data in the existing read-only paper runtime to establish opportunity frequency, conservative economics, timing, stale-data, leg-risk, reconciliation, and kill-switch evidence before any separately authorized strategy execution.
- Candidate `300362d840141d9ed599d8189ed1d10801fc5256` on `codex/strategy-measurement-foundation` is formally rejected and immutable: it adds 3,359 lines of parallel measurement infrastructure for the superseded testnet-measurement objective without a visible Builder evidence report. None of it enters `main`; the existing accepted mainnet-public paper path is the authorized starting point.

## Remaining gate

- Protected onboarding is complete for the three real mainnet identities. The next action is sequential bounded exact-account read-only gates, beginning with Extended and Nado while RISEx session registration/JWT remains separately blocked. No secret belongs in task/chat. Deposits/transfers, payload preparation/signing, dispatch, and strategy execution remain closed. Any later real-funds canary requires fresh public and private evidence, exact account/private-stream/fee/unrelated-state gates, the already bound `20 USD` deposit and `12 USD` maximum-loss caps, planned matched quantity within both caps, and a separate approval binding one exact route, both exact accounts, direction, quantity, and exactly one manual lifecycle.
