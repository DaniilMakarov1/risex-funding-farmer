# Current status

## Central baseline

- Git `main` is the exact accepted-history authority and is published to `origin/main` after every accepted Chief checkpoint.
- All three fixture lifecycle cores are integrated. Paper remains default; no venue is strategy-ready.
- User permanently authorizes bounded venue-local diagnostics, authenticated testnet access, wallet/account/API setup, faucet funding, product/economic selection, sequential testnet writes, and recoverable local operational-database recovery for RISEx, Nado, and Extended without repeated approval. These wallets are testnet-only; current spreads and minimum notionals are not blockers and have no fixed USD ceiling. Mainnet, real funds, strategy execution, and unrelated accounts remain separately governed.

## Venue readiness

### RISEx

- Accepted private-read evidence has complete counters, authoritative zero orders, and exact flatness; its historical run identities remain immutable.
- The accepted fixture lifecycle now binds `client_order_id` to composite, wide, and resting order identities, matching order/fill records, the exact cancel action/body identity split, and a fresh authoritative market position. It preserves durable no-replay and terminal zero-order/exact-flat barriers.
- The isolated Level C credential/signing/fixed-write-transport binding is accepted with exact signed-action/submitted-body linkage and remains absent from normal startup. A fresh pre-state gate stopped before network/auth on a noncanonical local lifecycle database; its terminal runtime row is immutable and no write was dispatched.
- The observed lifecycle-clear rejection now has an accepted distinct sanitized safety classification that persists before every public, credential, signing, private, and write effect and remains terminal across restart.
- Read-only recovery inspection found one obsolete empty three-column `cancels` table; integrity, identities, and zero intents/cancels were otherwise correct. A protected exact backup was verified before the table was transactionally replaced with the accepted canonical schema. `LifecycleClearBinding` now passes repeatedly, and the consumed private-read runtime database remained byte-identical.
- The fresh Level B run completed all nine first-barrier public requests, then stopped on an unclassified public-sweep semantic failure before source load, credential access, signing, or private dispatch. Its new runtime row is terminal and immutable; no retry or write occurred.
- Separate credential-free diagnostics showed the BTC/USDC BBO was not literally crossed, but its spread was about 18.83%. The user explicitly accepted arbitrary current testnet spread for the first bounded lifecycle because the purpose is execution-contract proof with test assets. The previous 0.30% maximum-spread blocker is removed only for this isolated RISEx testnet path; authoritative non-crossed BBO, exact grids/depth, price bounds, notional, identity, reconciliation, zero-order, and exact-flat safeguards remain mandatory.
- The accepted RISEx correction removes only the isolated testnet maximum-spread rejection; crossed-book, freshness, grid, depth, adverse price bound, notional, identity, reconciliation, zero-order, and exact-flat safeguards remain. Focused review passed 151 tests and the clean Python 3.11 full suite passed 1,923 tests.
- A fresh post-correction Level B passed completely: both nine-request public barriers agreed, every authenticated private-read/signing/capability phase completed exactly once, and authoritative orders/positions evidence was zero/flat. No write occurred.
- The sealed zero-argument Level C runner is accepted and integrated. On its first live invocation it durably dispatched one minimum-size `MARKET+FOK` opening intent once, but received no terminal order event and halted without replay. Two independent authenticated observations plus a full fresh public sweep then proved the nonce consumed, zero open orders, exact position zero, and no unrelated state. The protected local lifecycle database was backed up and recovered through the already-accepted expired/no-identity/zero-flat reconciliation rule to `COMPLETED_NO_FILL_FLAT`; no venue recovery write was required.
- A second sequential invocation again dispatched one minimum-size `MARKET+FOK` intent exactly once and reached the same authoritative no-identity, zero-order, exact-flat state, but a quiet receive timeout bypassed the first eight-frame absence fallback. After a protected backup, Chief used the accepted expired/no-identity recovery rule to record `COMPLETED_NO_FILL_FLAT`; no venue recovery write was required.
- The accepted RISEx correction now treats only an ordinary bounded receive timeout after validated initial zero-order and flat-position snapshots as event absence for the exact dispatched-once opening vector. Disconnect/reset, missing snapshots, identity, nonflatness, stale evidence, and contradictions remain manual recovery. The combined clean Python 3.11 tree passed 2,007 tests.
- The final authorized sequential invocation again dispatched exactly once and reconciled its intent to terminal zero fill/zero position, but a later final-barrier transport failure overwrote the operational outcome with manual recovery. Two fresh independent authenticated observations plus fresh public sweeps then proved nonce consumption, zero open orders, exact flatness, and no contradiction. After a protected backup, Chief restored only the outcome to `COMPLETED_NO_FILL_FLAT`; no venue recovery write occurred. Three safe no-fill invocations prove durable single dispatch and authoritative no-fill reconciliation but do not prove the filled-position cancel/close path; do not repeat this opening unchanged.
- The accepted fill-seeking correction changes only the opening to a price-bounded crossing `LIMIT+IOC` at the current ask-side adverse bound. Minimum size, depth/grid checks, durable identity, no replay, reconciliation, cancel/close, and final zero-order/exact-flat barriers remain. The combined accepted tree passed 2,019 tests in clean Python 3.11.

### Nado

- The fixed identity is securely provisioned; Ink Sepolia gas and test USDT0 collateral are available, with exactly 10 test USDT0 deposited.
- A fresh credential-free official catalog observation completed on its first transport attempt. Its complete uniform spot/perpetual structure exactly matches the accepted parser and fixtures, so no catalog code change is required.
- Runtime run identity is accepted. The latest Level B run stopped on public `all_products` before credential access; its historical runtime row remains immutable.
- The accepted private-read report now durably distinguishes sanitized `TRANSPORT`, `HTTP`, `SCHEMA`, `AUTH`, `IDENTITY`, and `SAFETY` failures without raw bodies, secrets, signatures, or account identity.
- A fresh credential-free `all_products` diagnostic received a valid transport/envelope but stopped because current product IDs are not contiguous. Official documentation treats the returned IDs as the valid set and does not require contiguity, so this is an observed Nado-local parser defect, not a rate limit or credential failure. No credentials, signing, private dispatch, or write occurred.
- The accepted Nado correction now treats unique non-negative product IDs returned by official `all_products` as the authoritative sparse set while retaining exact collateral, duplicate, malformed, account-coverage, cross-response, counter, signing, and no-rearm safety checks.
- The next fresh Level B run passed contracts, status, sparse `all_products`, and linked-signer public checks, then stopped with sanitized `SCHEMA` on public `subaccount_info`. Credential loader, derivation, signing, private trigger dispatch, and writes remained unused; its runtime row is terminal and immutable.
- The first separate credential-free schema diagnostic confirmed the failure is in the public-response prefix but did not retain enough bounded envelope evidence to identify the exact field mismatch. That diagnostic is terminal and will not be replayed; one narrower envelope-shape observation is required before deciding whether code is defective.
- The narrower observation stopped deterministically because the current public `subaccount_info` response exceeded the accepted 65,536-byte transport ceiling before a complete body could be retained. Official documentation confirms this response includes account balances plus complete spot and perpetual product structures. This is an observed Nado-local response-size compatibility defect, not credentials or rate limiting; the basic query has weight 2.
- The accepted Nado correction gives only `subaccount_info` a finite 1,048,576-byte ceiling; all unrelated responses retain 65,536 bytes and strict JSON, fixed-host, timeout, redirect, freshness, and fail-closed protections. Focused review passed 243 tests and the synchronized clean Python 3.11 suite passed 1,898 tests.
- The next fresh Level B run again completed four of five first-round public requests, then stopped on public `subaccount_info`, now as sanitized `SAFETY` rather than response-size `SCHEMA`. Credential loading, derivation, signing, private trigger dispatch, and writes all remained at zero; its fresh runtime row is terminal and immutable.
- The separate credential-free diagnostic classified the predicate as `embedded product catalog disagrees`. Both observations contained the same aggregate 90-product coverage (15 spot, 75 perpetual), complete matching-size balance vectors, and three health rows; the account response had 175 health-contribution rows because official semantics index that vector by sparse product ID rather than by product count. The current check hashes whole product objects across sequential responses, including live state/book/oracle fields, so ordinary market movement creates a false mismatch before the already-visible sparse contribution-vector incompatibility.
- The accepted Nado correction validates each complete snapshot strictly, compares stable identity/kind/coverage/risk/config across responses while tolerating oracle/state/book drift, and validates contributions through the maximum sparse product ID with exact zero triples in unused slots. Independent focused review passed 252 tests and the clean Python 3.11 full suite passed 1,907 tests.
- The next fresh Level B run still completed four of five first-round public requests and stopped with sanitized `SAFETY` on public `subaccount_info`; credential, derivation, signing, private trigger, and write counters all remained zero. Its new runtime row is terminal and immutable, so the next remaining account predicate must be classified separately.
- The separate current-state diagnostic then passed the accepted account validator completely: stable catalog equality, 15/75 complete balance vectors, collateral above floor, zero non-collateral spot balances, zero perpetual amounts/vQuote, three health rows, and 175 sparse contribution slots. The preceding `SAFETY` was therefore transient cross-snapshot inconsistency; current external state has materially changed and no further code defect is evidenced.
- A fresh Level B using the full public sequence nevertheless reproduced the same fifth-request `SAFETY` with every credential/sign/private/write counter still zero. The contrast with the immediately adjacent two-request diagnostic shows a field classified as stable is changing during the longer contracts/status/catalog/linked/account sequence; do not retry Level B until that field is identified.
- The exact full-prefix diagnostic proved product IDs and kinds remained identical while 12 of 90 products changed only live fields: spot/perpetual `oracle_price_x18`, selected `state` fields, and `risk.price_x18`. The validator already tolerates oracle/state drift but still includes the strictly validated positive `risk.price_x18` in its cross-snapshot stable hash, causing the repeated false mismatch.
- The accepted Nado correction keeps `risk.price_x18` mandatory, canonical, and positive in every snapshot but excludes only that observed live value from the stable cross-snapshot hash; risk weights and spot configuration remain stable inputs. Independent focused review passed 268 tests and the clean Python 3.11 full suite passed 1,923 tests.
- The next fresh Level B passed the five-request public account prefix for the first time, then stopped with sanitized `SCHEMA` on the first public `subaccount_orders` request for product 0. Credential loader, derivation, signing, private trigger, and write counters all remained zero; its runtime row is terminal and immutable.
- The separate orders diagnostic stopped earlier than `data` validation with `wire envelope schema mismatch`, proving current `subaccount_orders` uses a top-level envelope different from accepted `{status,data,request_type}`. That observation is terminal; no order content or identity was retained.
- The narrower envelope observation showed this is the venue's explicit error shape `{status,request_type,error_code,error}`, not a successful response-shape drift. The API is rejecting `subaccount_orders` for collateral product 0; the runner currently misclassifies that semantic API rejection as `SCHEMA`.
- The bounded error diagnostic classified venue error code `2015`: no market exists for the supplied product/ticker ID. Product 0 is the fixed USDT0 collateral product and must remain in account/balance safety coverage, but it is not a market and must not receive a `subaccount_orders` market query. This is not authentication or rate limiting.
- The accepted Nado correction retains product 0 in every collateral/account safety vector, excludes it only from market-order queries, and queries each non-collateral product exactly once per public round. Independent focused review passed 266 tests and the clean Python 3.11 full suite passed 1,921 tests.
- The next fresh Level B completed 12 of 13 attempted first-round public requests, then another non-collateral product failed at `subaccount_orders` with sanitized `SCHEMA`; every credential/sign/private/write counter remained zero. Product catalog membership is therefore not sufficient proof that a product has a market. Do not exclude IDs one by one or retry until an authoritative current market-bearing set is observed.
- The authorized credential-free market diagnostic showed the official V2 `pairs` set contains exactly all 89 non-collateral catalog IDs, but product 11 is the special `NLP_USDT0` liquidity-provider vault token rather than an orderbook instrument: its direct official `subaccount_orders` and aggregate `market_prices` queries both return venue error `2015`, while official NLP documentation defines mint/lock/burn vault-share behavior. This explains the 13th-request stop without credentials or quota exhaustion and provides a bounded Nado-local correction: preserve product 11 in complete account safety coverage but exclude it, like collateral product 0, only from regular orderbook-order queries.
- The accepted Nado correction excludes fixed testnet product 11 only from regular orderbook-order queries while preserving complete catalog/account/balance/health coverage; focused review passed 268 tests and the clean Python 3.11 full suite passed 1,923 tests.
- The fresh post-correction Level B completed all 94 first-round public/account/order requests and exact fixed owner capability loading/derivation, then stopped before signing or private trigger dispatch on `server_time` with sanitized `SCHEMA`. A separate credential-free observation showed the valid current time envelope is `{status,method,id,server_time}` with additive `id:null`; the parser requires the older exact three-key shape. This is a narrow Nado-local additive-response compatibility defect, not credentials, rate limiting, or a semantic time failure.
- The accepted time-envelope correction allows only the observed additive `id:null` while retaining exact required semantics and strict rejection of every other addition/value; its clean Python 3.11 full suite passed 1,934 tests.
- The next fresh Level B completed all 94 round-A requests, capability load/derive, fresh server time, signing/recovery, private trigger zero-order read, and all 95 round-B requests. It then stopped with sanitized `SAFETY` because the two sequential 88-market order sweeps took 47,621 ms after round-A evidence against the 30,000 ms temporal barrier; every individual response completed and no write occurred.
- Official current orders documentation defines a multi-product `type: orders` query. A credential-free observation derived the exact current catalog set, excluded only fixed non-orderbook products 0 and 11, and received one successful `query_orders` response covering all 88 requested product IDs exactly once with zero nonempty order lists. This is the authoritative bounded fix for the temporal incompatibility; the earlier manually composed diagnostic that accidentally included nonexistent product 12 failed with venue `2015` and is not contract evidence.
- The accepted aggregate-orders correction performs one exact complete 88-product `query_orders` check per round and preserves complete account/health coverage; focused review passed 290 tests and the clean Python 3.11 full suite passed 1,947 tests.
- The fresh post-correction Level B finalized successfully: round A used 7 completed requests, fixed owner capability load/derive, server time, signing/recovery, and private trigger zero-order read each completed exactly once, round B used 8 completed requests, and both stable fingerprints agreed for all 90 account products. No write occurred.
- The sealed zero-argument Nado Level C runner is accepted and integrated with durable pre-dispatch identity, exact place/cancel payloads, aggregate account/order/trigger evidence, bounded compressed transport, IOC close attempts, and terminal zero-order/exact-flat barriers. The combined accepted tree passed 1,992 tests in the project-compatible isolated Python 3.11 environment.
- Its first live invocation stopped before preparing an intent or dispatching a venue write. A fresh standalone accepted Level B immediately finalized with all counters, proving the key/owner/private trigger capability is valid. The runner now reuses that accepted 30,000 ms signed-read window while preserving the 100 ms write contract.
- The next invocation also stopped before preparing an intent or dispatching a venue write. The complete current 88-market catalog contains auxiliary products whose published minimum amount is not itself divisible by their step; the target product remains aligned. The accepted correction therefore validates catalog minima independently, enforces exact step alignment on the actual target opening amount and clamped closing amount, and preserves every target notional/safety check. The combined clean Python 3.11 tree passed 2,007 tests; the empty intent history permits one sequential retry.
- The retry again created zero intents and sent zero writes. One initial read-only observation had a transient transport failure and its single allowed retry passed; the exact terminal preflight then showed fixed product 2 currently requires a minimum 100-unit order at about USD 78,817 per unit, far above the project-wide USD 500 gate. This is neither credentials nor quota exhaustion. The current official catalog and V2 pair identity show multiple perpetual alternatives below USD 500, including product 20 `MON-PERP_USDT0` at about USD 2.90 minimum notional and product 30 `PUMP-PERP_USDT0` at about USD 0.45. Changing the fixed product/economics requires explicit user selection before a fresh Builder correction.
- The user removed the fixed USD ceiling for the named testnet wallets. The accepted current-market correction binds product 44 `SKR-PERP_USDT0` through V2 identity, signing, opening, reconciliation, cancel, close, and barriers, and computes the smallest step-aligned amount satisfying the venue's USD 5 minimum; the observed amount was 650 SKR at about USD 5.04.
- The first SKR invocation durably prepared and dispatched one post-only entry, but the execute response was not accepted by the runner and the intent halted `AMBIGUOUS`. Two post-window authoritative observations proved zero regular/trigger orders, zero positions, zero fills, and no unrelated state; exact `query_order` returned venue code 2020, order not found. After a protected backup, Chief used the existing completion barrier on a database copy and then the working database to recover the intent `RECONCILED` and lifecycle `COMPLETE`; no replay or recovery write occurred. A fresh visible Builder must preserve the execute failure class and use a taker opening rather than repeat the post-only path.

### Extended

- The fixed identity and API-key capability are securely provisioned. The accepted account-shape witness completed `CAPTURED` with every counter exactly `1/1` and no write.
- The exact `{status, data}` parser correction is accepted. The latest private read completed its first REST pass, then the official account stream returned HTTP 503 before any frame; no write occurred.
- A fresh credential-free handshake confirmed that the old source-bound testnet stream host resolves and completes TLS but returns HTTP 503. Current official SDK configuration instead names `wss://starknet.sepolia.extended.exchange/stream.extended.exchange/v1`; its account path returned HTTP 403 without an API key, proving the current host is available and enforcing authentication. This is an observed Extended-local endpoint drift, not a rate limit; no credentials or frames were sent.
- The accepted Extended correction binds the current official testnet stream host and allocates a fresh durable runtime run ID in a protected multi-row journal while preserving historical rows, account/write identities, direct TLS, header-only API-key authentication, and normal-startup isolation.
- The next fresh Level B run completed credential load and all three first REST reads, then failed during the authenticated stream open before upgrade validation or any frame. The durable result is sanitized `UNEXPECTED_FAILURE`; no signing, account mutation, or write occurred, and the new runtime row is terminal and immutable.
- A separate authenticated handshake classified the current account-stream response as HTTP 403, not rate-limit HTTP 429. Official SDK source sends both `X-Api-Key` and an explicit SDK `User-Agent`; the accepted runner actually supplies only `X-Api-Key` while claiming both header names in evidence. This is a concrete implementation/attestation mismatch requiring one SDK-conformant handshake observation before any correction is opened.
- The SDK-conformant handshake, using the official `X10PythonTradingClient/2.5.0` user-agent form and the existing valid REST API key, still returned HTTP 403 before upgrade and sent no frame. The missing explicit user-agent is a local implementation/attestation defect but not the cause of the 403; the remaining blocker is external account-stream access/ingress authorization, not quota exhaustion.
- The accepted Extended correction now sends the official SDK user-agent explicitly with `X-Api-Key` and derives sanitized header-name evidence from the actual request. Focused review passed 205 tests and the synchronized clean Python 3.11 suite passed 1,898 tests. It does not clear the external HTTP 403 account-stream blocker.
- The user authorized a final detailed independent diagnosis. One exact official Python SDK 2.5.0 testnet account-stream open may be run from an isolated temporary environment with the existing protected REST-valid API key, receiving no application frame beyond the passive first observation and retaining only sanitized status/class evidence. This is to distinguish a local client-stack mismatch from provider-side key/account/ingress authorization; it does not authorize key rotation, reprovisioning, account mutation, or a write.
- The exact published SDK diagnosis is terminal and identifies an upstream configuration split. PyPI's latest official SDK 2.5.0 still configures `wss://api.starknet.sepolia.extended.exchange/stream.extended.exchange/v1`; with its declared unconstrained `websockets>=12` dependency it first fails locally against current `websockets` 17 because the SDK uses the removed `extra_headers` argument, and with compatible `websockets` 12 its exact `/account` open reaches the network but returns HTTP 503. The current official source host used by the accepted runner instead returns HTTP 403 with the same REST-valid key. No frame, account mutation, signing, or write occurred. This is provider/package and account-stream access inconsistency, not quota exhaustion or a remaining evidenced local header defect.
- The testnet-only Extended wallet was generated locally by Chief; it is not a user-provided wallet and will never hold real funds. Its protected L1 owner key was independently matched to the existing Extended client and its protected Stark identity matched the sole subaccount. Official SDK REST reads proved zero orders and zero positions. One authorized testnet claim completed and balance then became readable. One new API key was created and proved REST-valid but received the same account-stream HTTP 503, so it was not activated locally; the existing REST-valid key remains unchanged. Official stream RPC v2 returned HTTP 404. Wallet, balance, stale key, quota, and local headers are therefore excluded; the remaining v1 503/v2 404 is provider-side.
- The accepted Extended correction restores the current official SDK `api.starknet.sepolia.../v1/account` host and accepts optional/additive irrelevant stream fields while preserving required types, sequence, identity, and activity rejection. The combined accepted tree passed 2,019 tests in clean Python 3.11; one fresh Level B may confirm the current provider result.

## Exit condition

- Each venue must independently pass one bounded testnet place/reconcile/cancel/close lifecycle ending in authoritative zero open orders and exact flatness. Only then may a separate strategy-testnet task begin.
