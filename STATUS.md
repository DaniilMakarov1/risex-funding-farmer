# ACK/fan-out audit — verified candidate, live integration pending, 2026-09-26

Current disposition (supersedes the work-in-progress notes below): **READY_FOR_REVIEW**. Implementation and self-review DONE. Final code commit `4af6b0b` on `codex/ack-fanout-audit`, from accepted main `b8f0d55`. Clean isolated Python 3.11.5 full suite: **6244 passed, 3 existing skipped, 2 dependency deprecation warnings, exit 0**, 181.52 s. Source/test hashes before and after are identical. Later documentation-only commits do not change the tested implementation. All changes are confined to active hood_handoff, its tests and the four English documents; AGENTS and frozen modules are unchanged.

Evidence package: owner-only, outside Git, `spread-shadow-runs/hood-ack-fanout-audit-20260926/v1/` in the main checkout. `AUDIT.md` explains defects, counterexamples, limits and provenance; `final-4af6b0b/final-validation.json` and `final-full-suite.txt` bind the final verification; baseline probes reproduce both principal defects on accepted imports. Synthetic three-MARKET benchmark with 50 ms per fsync: five post-ACK waits become one, ACK-to-first-MARKET about 296–303 ms becomes 58–61 ms; total cycle fsync count 59 becomes 18. No live speedup or own-priority guarantee is claimed.

Real evidence: cycles 341–354 have seven SUCCESS/seven PARTIAL results, all complete and confirmed flat. Cycle 355 is separately UNKNOWN after closing-history timeouts; its misleading flatness explanation is corrected for future cycles, without rewriting its old evidence. A separate read-only check at 22:28:56 Moscow proved READY, exact zero positions and zero active orders on all four accounts. Later owner-started series mean that snapshot is not perpetual readiness. Initial read-only readiness failure and earlier validation-environment failures are preserved, not hidden.

Integration/activation: **NOT_RUN**. Main and the live controller have been left at `b8f0d55` because the owner continues real series (including a new seven-cycle series after 355). Preserve the attached worktree and candidate branch. Next action after the owner provides a pause between series: recheck idle state and fresh all-wallet READY, integrate the unchanged verified candidate and apply it without launching a new trading cycle. Server deployment, credential migration and agent financial commands: NOT_RUN / 0. Final reconciliation remains sequential; the separately rejected parallel-readback edit was not applied.

# ACK/fan-out audit — preceding work-in-progress record

Current state: WIP_NOT_ACCEPTED on `codex/ack-fanout-audit`, based on accepted main `b8f0d55`. Work is isolated from the live checkout and performed by one agent with self-review. Current NEXT_TASK/SYSTEM_SPEC/README top sections supersede the older admission and dust claims below. No live financial command, server deployment or credential migration was performed.

Fixed candidates: exact source stream identity after an ACK without order ID; release of closing-preparation nonce reservations before all recovery exits; one durable receiver-intent batch with post-flush local freshness/deadline/book revalidation; bounded ACK diagnostics and batched stream evidence; removal of a duplicate post-MARKET source lookup; receiver_2..16 no-replay detection; implementation identity covering fan-out; actual send timing and rigorous offline proof of an intended-but-unsent MARKET. Owner interfaces and fan-out are ACK only; read-stream transport and historical readers remain. Reconciliation is sequential: automatic approval review rejected a separate parallel-readback edit, and that edit was not applied.

Saved evidence: private `spread-shadow-runs/hood-ack-fanout-audit-20260926/v1/` in the main checkout. `baseline-probe.json` reproduces both principal correctness defects using imports from accepted main. `saved-live-analysis.json` hashes the original inputs and analyzes cycles 310–344; a separate `saved-live-analysis-345-354.json` adds the newly completed cycles. Fan-out cycles 341–354 have seven PARTIAL and seven SUCCESS outcomes, all COMPLETE/CONFIRMED_FLAT. Those live partial fills include outside counterparties; existing ACK semantics do not establish own queue priority. Original journals are unchanged. The bound Robinhood BTC reduce-only dust path already existed; the new regression corrects the earlier overly broad documentation claim without a top-up policy change.

Validation so far: active hood suite 2000 passed (before final report refinements); final targeted engine/fan-out/report set 113 passed. First full-suite attempt used the clean Python 3.11.5 environment but an uncommitted checkout inside the restricted sandbox: 16 failed, 1644 passed, then interrupted at a stalled stop test. Diagnosed failures: legacy evaluation tests require a clean Git source; local HTTP test servers cannot bind loopback in the sandbox. A bounded unsandboxed retry passed the stop/process/socket cases (16 passed); the 10 remaining failures were the documented dirty-source requirement. No production code or frozen legacy tests were changed to accommodate them. Final full clean-checkout validation: NOT_RUN. Candidate is not integrated or activated yet.

Clean-checkout validation at candidate `8f9f746`: 6240 passed, 3 skipped, one failure in the unchanged legacy directory-permission test. Diagnosis: the evidence runner's private `umask 077` was inherited by pytest, so its requested 0755 fixture became 0700. The next run will keep evidence private but give only the test child the normal `umask 022`; no test or production permission rule changes. Self-review also reproduced a controller startup refusal against the actual saved WS default. The simple/control path now maps that legacy default to ACK in memory, with a regression covering unchanged config bytes, local controller validation and a complete fan-out launch. During the audit the existing controller is executing an owner-started fan-out series; its checkout remains untouched.

At `073111c`, the clean Python 3.11.5 full suite passed: 6242 passed, 3 existing skipped, two dependency deprecation warnings, exit 0; all source/test hashes stayed unchanged. The subsequent real cycle 355 stopped its series on closing reconciliation timeouts (three legs consumed their 20 s budgets; total reconciliation 64.11 s). Its late positions are zero but its execution/history proof is incomplete, so UNKNOWN is correct. The contradictory reason "final inventory confirmed flat" was reproduced in both one-to-one and fan-out tests and corrected to agree with the UNKNOWN classification; no trade/retry/stop policy was relaxed. Final targeted set after that text correction: 307 passed. The final full suite must now cover that last correction too. Original cycle 355 and all prior test results are preserved separately.

# One LIMIT filled by several MARKETs (fan-out) — verified and active, 2026-09-26

Owner request: one account places the LIMIT and two or more other accounts fill it. Owner decisions: the bare `/run` asks "1 LIMIT → 1 MARKET" or "1 LIMIT → несколько MARKET", then the cycle count; k is random from 2 to "all ready wallets except the source"; the LIMIT holds at least the receivers' minimum orders with a small margin (implemented as +10 %: Q ≥ ⌈k × minimum × 1.10⌉, 0.00044 BTC for two receivers); the split is random; the MARKETs are sent at the same time; a partial own match closes everything reduce-only at once, as for 🟡. `main` is now `0fcc885` (branch `claude/fanout-limit-20260926` from `ccaa0aa`: `f7c58a6` engine and cycle, `0fcc885` pool draw, launcher, SDK/stream, report/recovery, Telegram, tests and documents). SYSTEM_SPEC.md top section describes the contract; README.md top section the owner's use.

What is new: `fanout_handoff.py` (one attempt: the source's POST_ONLY LIMIT and k IOC MARKETs, all prepared before the LIMIT, the unchanged ACK/WS admission, every receiver intent journaled before the concurrent sends, per-leg reconciliation, reciprocal-identity match per receiver, MATCHED only when every part matched the source) and `fanout_cycle.py` (sizing, random split, leverage, revalidation, opening, hold, mirrored closing, per-account reduce-only recovery over k + 1 accounts); the wallet-pool fan-out draw; `simple --fanout` (ACK/WS only, checked before a slot is claimed; `wallets.json` required); the SDK client and read stream for the drawn receivers; the offline report and operator recovery for `receiver_N` legs and intents; the Telegram mode question, `--fanout` child flag, cards, steps, execution notices and totals over every account. 1 → 1 is unchanged (no extras in its binding, its code paths untouched; the explicit `/run` forms stay 1 → 1).

Same-agent self-review; defects found and fixed before the commit: the read stream rejected the roles `receiver_2…` (a live fan-out would have failed at order binding); `--fanout` without ACK/WS failed only after a slot was claimed; the dispatch resize could shrink the LIMIT below ⌈k × minimum × 1.10⌉ and the fresh floor was not rechecked (counterexample: parts 10 + 10 = 0.20 < 0.22); the fan-out wallet refusal said "нужно минимум два"; the card read receiver parts from the wrong field; the account check used a property that duck-typed test configurations lack (one existing test failed). 51 new test items (39 in `test_hood_fanout.py`: floor and split, bounds refusal, requirements and an exhaustive enumeration of the uniform draw, the selection record, full cycles with 2 and 3 receivers in both directions, an external maker at opening and at closing, an external taker, a below-minimum residual, unknown and rejected receiver sends, admission veto retry, offline report full/partial/residual, recovery intent scanning, the dispatch resize floor and the fresh floor, the launcher end to end with 2 and 3 receivers, too few wallets, no pool file, strict admission refused before the slot, flag scope, SDK allow-lists and read-stream roles, live steps, card, execution notices, leverage step, series totals; 12 in `test_hood_telegram_menu.py`: the mode question, button variants, invalid answers, the count window from the mode answer, a button without a question, the child `--fanout` flag through `serve`), expected numbers computed by hand; the previous menu tests were adapted to the mode step. On base `ccaa0aa` the new modules do not import, a bare `/run` asks the count at once and `simple --fanout` is rejected by argparse. Mac canonical clean Python 3.11.5 suite at `0fcc885`: 6228 passed, 3 skipped, exit 0.

Known limits reported to the owner: a dispatch-unknown or rejected MARKET whose order cannot be proved makes the attempt UNKNOWN with no automatic closing (as for 1 → 1): the known positions stay for `/close` and the series stops. Fan-out parts sit near the 0.0002 BTC minimum, so a partial fill by outside traders leaves a below-minimum residual more often than 1 → 1; reduce-only recovery refuses it (KNOWN_RESIDUAL) and READY (every wallet flat) then blocks `/run` until it is removed — an owner decision is requested (top-up then close, a live check of an oversized reduce-only order, or accept the stop).

Activation: controller PID 35571 (`ccaa0aa`) idle (no child process, no active operation, last cycle-340 FINISHED); precheck READY at 21:10:32 Moscow on all four wallets; SIGINT; fast-forward `ccaa0aa`→`0fcc885`, tested hashes matched; fresh READY in 2.346 s at 21:10:35 Moscow: 27331, 27337, 34019 and 34020 exactly 0.00000, no active orders, 1303 previous intents (5 from saved proofs, none looked up live), 0 unresolved leverage settings, 1222 inputs. Controller PID 48883 started with the instance lock held and an empty log; configuration and controller state bytes unchanged. Branch and `main` pushed to origin. Agent financial commands: 0. Live fan-out cycles NOT_RUN until the owner chooses the mode after `/run`. Evidence: `spread-shadow-runs/hood-fanout-20260926/claude-v1/`.

# Telegram menu, /run question and 24-hour /report — verified and active, 2026-09-26

Owner request: remove `/status` and `/help`, keep `/accounts`, make `/report` total the last day instead of the latest series, show Moscow time everywhere, remove the "USD — номинал USDG без пересчёта курса; фандинг не включён; оборот = …" footnote everywhere, make `/run` mean ACK +1 tick and then ask how many cycles (answered by a plain number), show `/stop` while a cycle runs and `/close` when no series is active. "За сутки" is implemented as the rolling last 24 hours (reported to the owner). `main` is now `d573da8` (branch `claude/telegram-menu-20260926`, from `37167b6`): the keyboard is chosen when each message is sent (`/run` `/close` · `/report` `/accounts` idle; `/stop` · `/report` `/accounts` while an admission, cycle, series step, pause, close or stop runs); `/status` and `/help` get the unknown-command reply with the command list; a bare `/run` asks with ForceReply and keeps an in-memory question for 300 s that only a plain positive integer answers, as exactly `/run ack 1 N` through the unchanged admission (other commands except `/report`/`/accounts` cancel it; a restart drops it; explicit `/run …` forms unchanged); `/report` renders, in the notice worker thread from saved journals only, the cycles claimed in the 24 hours before the request with the window in Moscow time, ✅/🟡/⛔ counts with ⛔ slots named, launch failures, the running cycle excluded and named, turnover, PnL before/after fees and fees (partial sums say "по K из N циклов") and separate closes counted but not summed. Read-only diagnosis of a related display defect: the end-of-series summary required every cycle to use the first cycle's account pair, so the owner's wallet-pool series 318–335 (all COMPLETE, CONFIRMED_FLAT) showed ✅ 0 · 🟡 2 · ⛔ 16 with 16 "PnL ?" and unknown totals; aggregation now judges each cycle by its own two distinct accounts (receipts still deduplicated by account and trade) and gives ✅ 2 · 🟡 16 · ⛔ 0, turnover 2 606.72 $, PnL −0.0408 $. The footnote and the single-cycle nominal-USD line are gone; card, hold and series clock times say "МСК". Trading, admission, series safety, `/stop`, `/close`, persistence schema and the terminal launcher are unchanged.

Same-agent self-review; two defects found and fixed before the commit (a message without text cancelled the question; an admission refusal was sent with the `/stop` keyboard). 56 new tests (54 in `test_hood_telegram_menu.py`: keyboards per state including the stop procedure and a refusal with a pending stop, the question with ten invalid answers, the 300/301 s boundary, foreign/forwarded/group/bot/edited/stale/future answers, duplicate and post-restart answers, cancellation and renewal, busy refusal, unchanged explicit forms, exact launch options and counts, admission refusal; 24-hour window edges, claim-time fallback and scan stop, symlinks, Moscow label, pool pairs, unknown and zero-tariff sums, launch failures, running exclusion, closes, bounded HTML, fixed failure message; 2 in the series report: pool pairs and a one-account cycle) with expected numbers computed by hand; existing tests adapted (direct launches use `/run 1`, fakes accept the keyboard, footnote assertions inverted). On base `37167b6` a probe launches a cycle on a bare `/run` without asking, answers `/status`/`/help`, shows the old keyboard and renders 318–335 as above; the new module does not import there and 3 updated tests fail. An independent Fraction replay of the saved reports gives series gross −0.040784 and turnover 2606.717454, and for the 24 hours to 19:01 Moscow 104 cycles (cycle-232…335; ✅ 48 · 🟡 54 · ⛔ 2: cycle-312, cycle-317), gross −0.2386 over 102 and turnover 16 992.891155 over 103, identical to the rendered report. A pre-commit working-copy run failed only the 10 clean-checkout tests of `test_evaluation_run.py` ("source checkout must be clean"). Mac canonical clean isolated Python 3.11.5 suite at `d573da8`: 6177 passed, 3 skipped, 2 dependency warnings, exit 0 in 180.94 s; 301 tracked src/tests hashes.

Activation: controller PID 24579 (`37167b6`) idle (no child, operator lock free, active none, last cycle-335 FINISHED, state untouched for 35 minutes); precheck READY on all four wallets; SIGINT; fast-forward `37167b6`→`d573da8`, tested hashes matched; fresh READY in 2.217 s at 19:07:30 Moscow: 27331, 27337, 34019 and 34020 exactly 0.00000, no active orders, 1280 previous intents (5 from saved proofs, none looked up live), 0 unresolved leverage settings, 1201 inputs. Controller PID 35571 started with the instance lock held and an empty log; configuration and controller state bytes unchanged; no stop marker. Branch and `main` pushed to origin. Agent financial commands: 0. Live use of the new menu NOT_RUN until the owner presses `/run`. Evidence: `spread-shadow-runs/hood-telegram-menu-20260926/claude-v1/`.

# First four-wallet run errors — fixed, verified and active, 2026-09-26

Owner request: the first runs with four wallets (27331, 27337, 34019, 34020; cycles 310–317) showed several errors; check and fix them. Read-only diagnosis of the journals (copied to the evidence directory): two defects, neither caused by the wallet count. cycle-312: the private stream ended with `transport_error` during the hold, the paired close stopped before its source send (FAILED_PREFLIGHT_BLOCKED, as designed) and reduce-only recovery closed 34019 but refused 27331 (`fallback pre-quote nonce reservation failed: contract_error`), because after a stop before the source send the engine invalidated only the prepared receiver order; the prepared source order kept 27331's nonce reserved in the SDK, and with API nonce management the venue kept returning that nonce. +0.00039 BTC stayed open until the owner's `/close` (close-020). cycle-317: connections to the venue took about 10 s or timed out from 15:55:16 to 15:56:08 Moscow (four closing preparations timed out and were retried; the stream also ended with `transport_error`); on the fifth attempt the reads finished after the pre-quote nonce reservation had expired, order preparation failed before anything was sent, and the engine classified `order preparation failed before source exposure: timeout` as UNKNOWN although both legs reconciled unchanged, so there was no retry and no recovery; ±0.00076 BTC stayed open until `/close` (close-021) and the report shows UNRESOLVED_EXECUTION. cycle-314 (receiver MARKET filled against external account 39, residual closed, flat) is existing market behavior. The cycle-312 card also claimed the residual closed and printed a flat account as "закрыта 0 BTC".

`main` is now `a16567b` (branch `claude/four-wallet-errors-20260926`, from `faea218`): every order an attempt prepared but never sent is invalidated when the attempt ends (a sent order was consumed and is unaffected); a stop before the source send (pre-source stream refusal, order preparation failure, quote expiry) with proved zero mutation on both legs is FAILED_PREFLIGHT_BLOCKED, never UNKNOWN; when its reasons are transient (timeout or connection failure, including an expired reservation, or quote age) the guard records `pre_dispatch` TRANSIENT and the attempt is retried within the unchanged opening/closing budgets after one poll interval, otherwise the existing reduce-only recovery closes the proved inventory; a dead stream is still not retried. Card: "остаток закрыт reduce-only не полностью" unless flat; a flat account reads "позиции нет".

Same-agent self-review. 31 new adverse tests run the real SDK nonce and preparation bookkeeping with a fake signer, fake HTTP and an API nonce model (dead stream at closing ends flat through recovery on both accounts in ACK and WS modes; transient stream refusal retries; an expired reservation at closing retries and pairs with a COMPLETE report; preparation timeout at opening and closing retries; persistent refusal at closing goes to recovery and at opening sends nothing; bounded exhaustion; a sent nonce is never released; every zero-mutation fact required; display lines). On base `faea218` 17 fail — the dead-stream and expired-reservation cases reproduce the live cycle-312 and cycle-317 reasons verbatim — and 14 pass (negative cases and the sent-nonce guard). Cloud Python 3.11.15: base 6084 passed / 1 environment-only failure (macOS home path) / 3 skipped; candidate 6115 passed / the identical 1 / 3 skipped (the S3 uptime fixtures passed this time). Mac canonical clean isolated Python 3.11.5 suite at `a16567b`: 6116 passed, 3 skipped, 2 dependency warnings, exit 0 in 187.63 s; 299 tracked src/tests hashes.

Meanwhile the owner ran `/run ack 1 20` on `b8937b7` (cycles 318–335: 2 SUCCESS, 16 PARTIAL, all CONFIRMED_FLAT; the opening MARKET partly matched external makers quoting inside our LIMIT, or a closing LIMIT stayed unfilled and recovery closed the rest; about 45% of cycles 240–309 were PARTIAL in the same way) and ended it with the first live `/stop` at 17:31:52 Moscow during cycle-335's hold: `HOLD_ENDED_BY_OWNER_STOP` 0.29 s later (77 of 99 s held), the unchanged paired close and reduce-only recovery (closing MARKET matched external account 6611) finished CONFIRMED_FLAT, the stop proved READY on all four wallets, result FLAT at 17:32:06, marker removed, series 18/20; all 18 offline reports are COMPLETE/CONFIRMED_FLAT, cycle-335 without a hold-evidence issue. Activation: controller PID 14233 (`b8937b7`) idle (no child, operator lock free, active none, last cycle-335 FINISHED); pool precheck READY; SIGINT (standard shutdown line only); fast-forward `faea218`→`a16567b`, tested hashes matched; fresh READY in 2.315 s: all four wallets exactly 0.00000, no active orders, 1280 previous intents (5 from saved proofs, none looked up live), 1201 inputs. Controller PID 24579 started with the instance lock held and an empty log; configuration and controller state bytes unchanged; no stop marker. The Mac script pushed the branch and `main` to origin. Agent financial commands: 0. Live behavior of the fixes NOT_RUN until the owner's next `/run`. Evidence: `spread-shadow-runs/hood-four-wallet-errors-20260926/claude-v1/`.

# Owner /stop — verified and active, 2026-09-26

Owner request: a Telegram `/stop` that stops the project, including a running cycle, and closes positions; owner decisions: a running cycle is closed early at a safe point (an order being sent is never interrupted) and the bot stays online (`/run` works afterwards). `main` is now `b8937b7` (branch `claude/stop-command-20260926`, from `60eccdb`). `/stop` passes the usual owner checks and is never replayed; it writes the owner-only marker `stop-request.json` next to the configuration. The controller then starts no new cycle or series step and ends a pause or check-retry wait early; the launcher refuses to claim a slot while the marker exists. A running cycle observes the stop only after selection (before leverage) and after preparation (before the first order) — both end FAILED_PREFLIGHT_BLOCKED with nothing sent — before another opening attempt, and during the hold (1 s poll; `HOLD_ENDED_BY_OWNER_STOP`, then the unchanged paired closing and residual recovery). Afterwards: CLOSE_READY on every wallet, one existing reduce-only close under a durable close record when positions remain (must be CONFIRMED_FLAT), and a fresh READY proving exact zero with no orders before the barrier is cleared; the result is saved as `last_stop` and shown in Telegram and `/status`; the marker is removed at the end and before every `/run`. The offline report accepts a shorter hold only with that persisted row between the hold anchor and the closing plan.

Same-agent self-review. 31 new adverse test cases (engine safe points with no mutation after an observed stop except closing, hold interruption and report acceptance, forged or misplaced stop rows, a failing stop hook, launcher refusal, marker lifecycle, views; controller `/stop` idle flat, idle with positions, during admission, a cycle, a series step, the pause, a check-retry wait and a close, unconfirmed close, refused check, bounded check retries, duplicate/foreign/replayed `/stop`, `/run` removing or refusing a leftover marker). On base `60eccdb` the new test module does not import (no stop API), and a probe shows the base answers «Команда не распознана» to `/stop`, writes no marker and still launches. Cloud Python 3.11.15: base 6049 passed / 5 environment-only failures / 3 skipped; candidate 6080 passed / the identical 5 / 3 skipped (existing test fakes in two files gained the `stop_requested` keyword). Mac canonical clean isolated Python 3.11.5 suite at `b8937b7`: 6085 passed, 3 skipped, 2 dependency warnings, exit 0 in 182.33 s; 298 tracked src/tests hashes.

Activation. The first run (16:22 Moscow) pushed the branch and deferred: the new pool-aware precheck (every `wallets.json` wallet, under the operator lock) refused with `positions remain; use /close before /run`. The owner's series had stopped at 4/6 with cycle-317 leaving 34020 +0.00076 / 34019 −0.00076 BTC (UNKNOWN/KNOWN_RESIDUAL after closing-preparation timeouts around 15:55 Moscow); the controller was not touched. The owner sent `/close`: close-021 CONFIRMED_FLAT at 16:27:48 (reduce-only; a first 34019 attempt filled nothing, then 34020 and 34019 each filled 0.00076), all four wallets 0.00000. Second run (16:29): same commit and hashes, cached green suite; controller PID 6191 (`f024853`) idle (no child, operator lock free, active none, last close-021 FINISHED); precheck READY on all four wallets; SIGINT (its log ends with the standard shutdown line); fast-forward `60eccdb`→`b8937b7`, tested hashes matched; fresh READY in 2.22 s: 27331, 27337, 34019 and 34020 exactly 0.00000, no active orders, 1203 previous intents (5 from saved proofs, none looked up live), 1134 inputs. Controller PID 14233 started with the instance lock held and an empty log; configuration and controller state bytes unchanged; no stop marker. The Mac script pushed the branch and `main` to origin. Agent financial commands: 0 (the owner sent `/close`). Correction: the series-stop activation's READY check (15:21 Moscow) read only the configured pair 27331/27337; `wallets.json` was last written at 15:28, so whether pool wallets already existed then is NOT_PROVED (the controller's own checks always read the whole pool); this activation's checks read every pool wallet. Live `/stop` NOT_RUN until the owner first uses it. Open: why cycle-317's closing left a residual is not analysed here. Evidence: `spread-shadow-runs/hood-stop-command-20260926/claude-v1/`.

# Series stop between steps — fixed, verified and active, 2026-09-26

Owner report: `/run ack 1 30` stopped after 11 cycles (cycle-299…309). cycle-309 ended CONFIRMED_FLAT at 12:28:01 Moscow; step 12 announced "checking positions and old orders" and stopped with `ошибка READ_TIMEOUT` before cycle-310 existed; no order was sent. Read-only diagnosis: every history check (controller CLOSE_READY and READY, and again the cycle child) re-looked-up five old intents without terminal proof in their own journals (cycles 003, 121, 126, 158, 193) through the exact read and deep inactive-order pagination, typically 7–8 s and up to 14.1 s of the 15 s per-lookup and 20 s total bounds, growing with history. The failing step took about 181 s from cycle end to the saved stop, including the 5–30 s pause; its exact composition is NOT_PROVED. Mac sleep and Keychain locking were ruled out (pmset log, login keychain without timeout). The controller logged nothing, and one failed read ended the whole series.

`main` is now `f024853` (branch `claude/series-stop-20260926`): exact old-order proofs are saved once in `recovery-intents.jsonl` and reused only for identical journal bytes, plan and order ID under the live match rules (conflicts block; separate file, so a rollback is not blocked); a between-cycle read-only check that failed only on an incomplete read is repeated (3 attempts, 10/20 s, Retry-After up to 60 s) while refusals still stop at once; a stopped run saves `last.stop` (stage, category, class, attempts, elapsed) and logs one stderr line, and the Telegram stop line names the stage. Same-agent self-review. 51 new adverse tests (proof reuse, refusal, tampering, other versions, partial progress, lookup bound, shared journal untouched; retry bounds, non-retry of 11 refusal/local classes, post-close READY, cancellation, Retry-After, stop record; end-to-end series with one stalled check); on base `23ecd76` 48 fail and 2 pass (they assert unchanged behavior); the retry-cancellation test cannot run there because it waits for a retry the base never makes. Cloud Python 3.11.15: base code 5998 passed / 5 environment-only failures / 3 skipped; candidate 6049 passed / the identical 5 / 3 skipped. Mac canonical clean isolated Python 3.11.5 suite at `f024853`: the first run gave 4 failed / 6050 passed because the frozen S3 envelope fixtures need host monotonic uptime above 2700 s and the host showed about 2488 s (restart around 14:31 Moscow); the rerun after 45 minutes of uptime gave 6054 passed, 3 skipped, exit 0 in 169.75 s, 297 tracked src/tests hashes.

Read-only live check with the tested source under the operator lock: CLOSE_READY 13.263 s with 5 venue lookups (their proofs saved), then CLOSE_READY 1.216 s and READY 1.319 s with no lookup (5 from saved proofs); both accounts exactly 0.00000; 1170 previous intents. Activation: no controller process was running (the restart had ended PID 36215); controller state idle (active none, last NOT_LAUNCHED, unchanged since 13:50 Moscow); operator lock free. Fast-forward `23ecd76`→`f024853`, tested hashes matched, fresh READY passed in 2.173 s; controller PID 6191 started with the instance lock held and an empty log. Configuration bytes unchanged; `recovery-checks.jsonl` untouched by the new proofs. The Mac script pushed the branch and `main` to origin. Agent financial commands: 0. Live series behavior after the fix NOT_RUN until the owner's next `/run`. Evidence: `spread-shadow-runs/hood-series-stop-20260926/claude-v1/`.

# Wallet pool with a random pair per cycle — verified and active, 2026-09-26

Owner request: add an unlimited number of wallets and let every cycle pick its wallets at random; owner decisions as recorded in NEXT_TASK (uniform pair of ready wallets, skip and report unready wallets, no cycle with fewer than two, configured API key index 4 for every wallet with a proved key). `main` is now `4b073e5` (branch `claude/wallet-pool-20260926`: `782b895` core, `deab5c7` complete feature, `4b073e5` bounded typed-429 retry for the selection reads). Without `wallets.json` behavior is unchanged; no pool file exists yet, so the system still trades 27331/27337 until the owner runs `./wallet add`.

Same-agent self-review of the full diff. New adverse tests (68): exhaustive-enumeration uniformity of the pair draw, eligibility and skip reasons, independently computed requirement arithmetic, unsafe/malformed pool files, unchanged legacy launch/proof shapes, pool READY/CLOSE_READY including paused wallets and active orders, bounded concurrent fresh reads, pool history and leverage proof on non-configured wallets, pool closure pairing with pair-bound mutations and stop at the first unproved pair, credential gate before any read, cycle child draw/reservation/credential scope, wallet-add refusals with nothing stored, Telegram views, later pool close matching. Cloud Python 3.11.15: base `a444dc8` 5930 passed / 5 failed / 3 skipped; candidate `4b073e5` 5998 passed / the identical 5 environment-only failures / 3 skipped (one existing Telegram accounts test fake gained the new `operator` argument). Mac canonical clean isolated Python 3.11.5 suite at `4b073e5` (detached worktree, pinned requirements): 6003 passed, 3 skipped, 2 dependency warnings, exit 0 in 186.23 s; 296 tracked src/tests hashes recorded.

Read-only live check with the tested source before activation: native Keychain presence query found both configured keys (no key value read); public account reads 27331 flat, 21.808829 available, 27337 flat, 23.273254 available; dry-run draw: both ready, requirement 4.524 quote; nothing persisted. Activation: the old controller PID 14103 (`a444dc8`) had no child and a free operator lock; its saved series (`/run ack 1 30`) had stopped after cycle-309 at step 12 (11/30 completed, BLOCKED, state unchanged for 79 minutes; the stop reason was only sent to Telegram and is NOT_PROVED here). The first run refused on that saved marker; the script was amended to accept only a BLOCKED marker quiet for at least 10 minutes and reused the identical commit's green suite. SIGINT, fast-forward `a444dc8`→`4b073e5`, fresh READY: both accounts exactly 0.00000, no active orders, 1170 previous intents (5 re-resolved), 1100 inputs. Controller PID 36215 started with the instance lock held and an empty log; its startup closed the stopped series as not launched (never resumed). Configuration bytes unchanged. The Mac script pushed the branch and `main` to origin (ordinary push; the cloud git proxy is not authorized for this repository). Agent financial commands: 0. Live cycles with a pool NOT_RUN. Evidence: `spread-shadow-runs/hood-wallet-pool-20260926/claude-v1/`.

# Every step in Telegram, zero-fee display — active, 2026-09-26

The Mac rebooted at 07:30 Moscow and the controller stopped with it; at 07:03 it had rejected one stale Telegram command (timestamp outside the allowed window, not executed). It was restarted at 07:49 on accepted `0fd2278` after fresh READY (PID 4670). The owner then ran `/run ack 1 3`: cycle-234 and cycle-235 own-account paired SUCCESS, cycle-236 opening MARKET filled against external account 16969, residual closed reduce-only, all CONFIRMED_FLAT (gross −0.0081 USD). The owner saw no progress between acceptance and the card, which the card-only design caused.

`main` now `5fe320c` (branch `claude/zero-fee-display-20260926`): owner-confirmed 0% Robinhood fee in views (`9f0439f`; only settled cycles whose every fill has an empty fee), brief live phase steps (`4b7c8f3`) and every controller/trading step in journal-time order through one FIFO send path (`5fe320c`); library exception text never reaches Telegram. Same-agent self-review. Targeted Telegram/controller/report tests: 425 passed. The clean isolated suite for `9f0439f` gave 5924 passed, 4 failed, 3 skipped: the four frozen S3 envelope fixtures need host monotonic uptime above 2700 s and the host had just rebooted (documented before; unrelated code). By explicit owner decision the full clean suite for `5fe320c` is NOT_RUN. Activated as controller PID 5968 under both locks after the guarded script refused nothing (idle, no series): fresh READY, both accounts exactly 0.00000, no active orders; configuration unchanged; startup log empty. Agent financial commands: 0. Live display of the new steps NOT_RUN until the owner's next `/run`. Evidence: `spread-shadow-runs/hood-live-steps-20260926/claude-v1/` (activation) and `spread-shadow-runs/hood-zero-fee-display-20260926/claude-v1/` (failed-uptime suite, restart on `0fd2278`).

# Human Telegram cycle cards — verified and active, 2026-09-25

Owner-reported problem: about eight mixed pushes per cycle (18 for the two-cycle series cycle-232/233, reconstructed from their real journals). Candidate `59af127` replaces routine pushes with one acceptance, one card per newly created cycle slot (verdict, BTC size, USD turnover, own/external/mixed phases with external account IDs, LIMIT→MARKET gap, attempts, durations, PnL, fees or a labelled cap estimate, next pause) and one compact series summary; the same series now yields four messages. Rare one-line alerts remain for spread wait, HTTP 429 cooldown, opening resize, residual recovery and pre-cycle close. Rendering runs in the controller's worker thread from saved journals; the trading child, sequencing, retries and persistence are untouched. Robinhood receipts in cycles 201–233 carry no fee fields, so net PnL stays unknown; the estimate is display-only. Fixed in passing: the opening-resize push introduced by `9a8991a` called a missing `views.amount` and was silently dropped (and suppressed later stage alerts in that loop).

Same-agent self-review. Adverse card tests cover unsafe/nonflat/incomplete/unresolved, missing report, foreign denomination, escaping, an unlaunched step not reusing the previous card, independent fee-bound arithmetic and alert filtering; aggregate/unknown/subtotal series tests retained with new wording. Final clean isolated Python 3.11.5 suite at `59af127`: 5922 passed, 3 skipped, 2 dependency warnings, exit 0 (157.27 s). Integrated unchanged and activated as controller PID 11884 under both locks (old PID 8310 idle, stopped by SIGINT). Fresh read-only READY: both accounts exactly 0.00000, no active orders, 797 inputs. Configuration and controller state bytes unchanged; offset 227121330; startup log empty. Agent financial commands: 0. Live display of a new series NOT_RUN until the owner's next `/run`. Evidence: `spread-shadow-runs/hood-telegram-cards-20260925/claude-v1/`.

# Cycle stops 194 / 223 / 231+ — verified and active, 2026-09-25

Read-only diagnosis of the owner series after `da13053` (cycles 194–231, close-017..019, copied operator snapshot; no venue access):
- Since 16:57:57 Moscow every `/run` is refused at READY: the shared `recovery-checks.jsonl` reached 35,110,061 bytes and crossed the 32 MiB per-journal read bound (first over at the READY check before cycle-231; cycle-231 child then failed PREFLIGHT_REFUSED before its journal). CLOSE_READY skips that read, so `/close` still worked (close-018/019 flat, no orders). Growth came from each CURRENT_STATE_VERIFIED row repeating ~789 input paths/hashes (~183 KB per row, two per cycle). Reproduced with the actual code on the snapshot.
- cycle-194: after both leverage writes the fresh executable price moved −137.5 at an unchanged mark; source adverse entry loss became 0.113535 and 0.00087 BTC failed the 0.02 dispatch reserve by 0.033; the non-retryable stop ended the series.
- cycle-223 (same class as cycle-137): closing attempt 2 was refused before its source send (`begin_ws_admission`: the stream still showed attempt 1's cancelled LIMIT as active; in 137 the stream had ended with transport_error). ACK mode appended "ACK not proved" although nothing was sent, making the attempt UNKNOWN; reduce-only recovery was skipped and ±0.00055 stayed open until close-017.
- Latent: the 2000-journal scan cap (789 used) would block again after roughly 350 more cycles.

Candidate `claude/cycle-stops-20260925`: separate finite 512 MiB streamed bound for the shared journal (32 MiB per trading journal unchanged), compact persisted proofs (exact input count + SHA-256; 183 KB → ~5 KB per row), file cap 10000 (snapshot scan 0.4–0.5 s for 789 files / 37.7 MB), explicit HISTORY_LIMIT refusal/launch code; opening resize as defined in NEXT_TASK; ACK reason only after an attempted source send; typed transient pre-source refusals retried after one settle, others go to reduce-only recovery. Counterexamples on base vs candidate: snapshot checkpoint read PreflightBlocked → 1 checkpoint in 0.09 s; synthetic READY over 32 MiB PreflightBlocked → READY; margin cycle FAILED_PREFLIGHT_BLOCKED at 0.40 → SUCCESS/CONFIRMED_FLAT at 0.37 with the same two setting writes; ACK dead-stream closing UNKNOWN/KNOWN_RESIDUAL ±0.20 → PARTIAL/CONFIRMED_FLAT via two reduce-only orders; ACK transient closing UNKNOWN → SUCCESS.

Same-agent self-review. Cloud Python 3.11.15 environment built from the project's own venv packages with test-only shims for macOS-only binaries (keccak, brotli, AES/scrypt, ckzg, bitarray, regex; documented in evidence): base `da13053` 5850 passed / 5 failed / 3 skipped; candidate 5902 passed / the identical 5 failed / 3 skipped (4 s3-envelope tests need host monotonic > 2700 s; 1 private-read test needs the Mac home path). One older assertion changed by policy: the provenance test that required a stop on a proved shortfall now requires the reduction. Owner-authorized run of `verify_and_activate.py` on the Mac (launched by the agent through Finder at the owner's request): canonical clean isolated Python 3.11.5 suite at `96fb4ad` (detached worktree, pinned requirements): 5907 passed, 3 skipped, 2 dependency warnings, exit 0 in 165.81 s; 291 tracked src/tests hashes recorded. No controller process was running beforehand; operator lock free. main fast-forwarded `da13053`→`96fb4ad` with hashes matching the tested tree. Fresh read-only READY with the new code passed at the shared journal's 35.1 MB size: both accounts exactly 0.00000, no active orders, leverage checkpoint resolved, 789 inputs. Controller started as PID 8310 (instance lock held, empty startup log); configuration and controller state bytes unchanged. Agent financial commands: 0. Live post-fix execution NOT_RUN. Historical journals and configuration untouched (hash list saved). Evidence: `spread-shadow-runs/hood-margin-resize-20260925/claude-v1/`.

# Reconciliation rate limits and inter-cycle pause — verified and active, 2026-09-25

Cycle-193 (sixth of twenty; five completed) stopped after a source LIMIT was cancelled and the ACK receiver was not sent. Account HTTP 429 was incorrectly converted into a permanent identity/read barrier; order and history reads also failed. Read-only current recovery independently proved READY, both accounts exactly zero with no active orders. Original journals are unchanged. The accepted-base synthetic counterexample returns UNKNOWN; candidate returns CONFIRMED_FLAT with one cancellation and no mutation replay.

Typed SDK/plain-HTTP 429 now permits bounded reconciliation read cooldown; genuine account identity, authorization and malformed evidence still block. Independently sampled integer 5–30 second pauses occur only between safe completed series cycles, followed by fresh readiness. Background Telegram notices show the pause and one reconciliation rate-limit notice per attempt. Successful trading reads gain no network request or sleep; reporting arithmetic and external-fill accounting are unchanged.

Same-agent self-review and 422 focused checks passed. Earlier focused failures exposed real waits in one server fixture and an external-full-fill fixture that injected only after cancellation; fixtures now inject the new sleep dependency or observed fill without weakening safety assertions. Final clean isolated Python 3.11.5 suite at source/tests `3b5751a`: 5855 passed, 3 skipped, 2 dependency warnings, exit 0 in 165.28 seconds. Same-agent self-review completed. Tested code integrated unchanged and activated as controller PID 69442 under operator/instance locks. Fresh READY at 2026-09-25 14:51:44 Moscow proved both accounts exactly zero and no active orders. Only administrative active and last_recovery fields changed; offset 227121311 and the old series result (5/20, stopped cycle-193) remain preserved. All 636 historical inputs and configuration are unchanged; all 288 source/test hashes match. Startup log empty, no trading child. No agent financial command or automatic replay; fresh owner-triggered live execution NOT_RUN. Evidence: `spread-shadow-runs/hood-reconcile-cooldown-20260925/solo-v1/`.

# Accepted command without launch — diagnostics verified and active, 2026-09-25

Owner command update 227121308 was accepted for eight cycles at 13:45 Moscow, but no cycle-188, active operation or current-recovery checkpoint was created. Telegram refusal delivery failed at 13:46; the controller discarded its underlying exception. Exact historical cause is NOT_PROVED. Read-only probes now pass the exact production CLOSE_READY and READY checks; getMe/getWebhookInfo return HTTP 200, no webhook, no pending updates. No positions/orders were changed. Current readiness does not retrospectively establish why the earlier request failed.

Candidate persists only fixed admission failure categories, stage, command ID and timestamp before notifying; /status exposes the saved refusal after restart, historical series results remain intact, and no consumed command is replayed. Telegram errors retain only fixed HTTP/timeout/connection categories, never response bodies, URLs or credentials. Existing trading/recovery/retry policy is unchanged. First focused run: 1 failed, 49 passed, interrupted because old integration waits for the established refusal heading; also preserve the specific existing nonflat explanation. Corrected focused run: 116 passed with old tests unchanged. Same-agent self-review complete at source/tests `09ad010`. Final clean isolated Python 3.11.5 suite: 5822 passed, 3 skipped, 2 dependency warnings, exit 0 in 153.84 seconds. Inert eight-cycle admission with real readiness and a no-op launch callback passed CLOSE_READY/READY and reached the callback once; no trading subprocess or Telegram send. Integrated unchanged and activated controller PID 65732 under both locks. Configuration/state bytes, offset 227121309, prior series results and all 611 historical inputs preserved. All 285 source/test hashes match; no child and empty startup log. Post-activation read-only READY at 2026-09-25 14:15:32 Moscow confirmed both positions zero and no active orders. No financial command or replay. Historical root cause remains NOT_PROVED; the lost-refusal diagnostic defect is fixed, not retrospectively diagnosed. Fresh owner-triggered financial execution NOT_RUN. Evidence: `spread-shadow-runs/hood-telegram-connectivity-20260925/solo-v1/`.

# Initial spread wait — verified and active, 2026-09-25

Cycle-187 stopped before selection or any mutation with PRICE_OFFSET_NO_ROOM after 11 of 20 series cycles. Current read-only recovery returned READY, both accounts exactly zero and no active orders. Initial quote selection had no retry; the exact no-room ContractError also bypassed existing paired preparation retry classification. Candidate adds bounded initial read-only observations (existing max_poll_count and reconcile_timeout_seconds), validates fresh metadata/book/accounts on every observation, and selects quantity/hold only after readiness. Explicit tick offset and mutation budgets remain unchanged; successful initial preparation still reads metadata/book/two accounts once with no sleep. Existing paired opening/closing budgets now recognize only the exact no-room error. Background Telegram lifecycle shows spread waiting once per cycle. Permanent no-room or any other unsafe fact remains blocking; no result is rewritten or series resumed.

19 new adverse/integration checks and 296 focused checks passed. Same-agent self-review completed at source/tests `3d8d4fb`. Final clean isolated Python 3.11.5 suite: 5816 passed, 3 skipped, 2 dependency warnings, exit 0 in 164.14 seconds. Both long/short counterexamples fail on accepted base and pass candidate. The first base harness attempt lacked pytest in production venv; retained and rerun in isolated test venv. Integrated unchanged and activated as controller PID 62995 under both locks. Fresh READY at 2026-09-25 13:31:53 Moscow proved both positions zero and no active orders; only administrative active and last_recovery changed. Configuration, offset 227121308, previous series results (11/20, cycle-187 stopped), and all 611 historical input files are preserved. All 284 source/test hashes match the tested checkout. Startup log empty and no trading child. No agent financial command or automatic replay; live post-fix execution NOT_RUN. Evidence: `spread-shadow-runs/hood-spread-wait-20260925/solo-v1/`.

# Launch after cycle-165 — verified and active, 2026-09-25

Read-only diagnosis from published main `a6843d0` found two independent blockers. The shared recovery journal has 167 legitimate run IDs, but the leverage checkpoint reader applied a single-run invariant. After correcting that path, Robinhood returned the exact prepared transaction with status 3, millisecond executed_at, zero L1 commit/verify timestamps, and an execution event matching account 27337 / market 1 / fraction 4528 / cross mode / empty error. A new venue-bound version-3 proof requires that full event plus consumed nonce and fresh matching flat accounts, preserving version-2 and trading-journal constraints. Candidate read-only recovery returned READY with both positions exactly zero and no active orders and appended one validated checkpoint; original cycle history was not rewritten. No financial command.

123 focused recovery/hash/controller checks passed after correcting three new fixture mistakes (missing require_flat argument, timestamp boundary fixture, and constructing an invalid venue config). Final clean isolated Python 3.11.5 suite at source/tests `aaf358f`: 5797 passed, 3 skipped, 2 dependency warnings, exit 0 in 148.43 seconds. Same-agent self-review completed. Actual saved-history counterexample fails on accepted base and passes on the candidate. Integrated unchanged and activated as controller PID 59898 under both locks. Fresh READY at 2026-09-25 12:48:01 Moscow confirmed both positions exactly zero and no active orders using the new checkpoint; cleared only the administrative active marker and refreshed last_recovery. Offset 227121306 and the blocked prior series result (2/10, cycles 163–165) remain preserved. All 530 historical cycle/close journal files and the recovery journal bytes recorded before activation are unchanged. Configuration and all 283 tested source/test file hashes match; startup log empty, no child or financial command. New owner-triggered live execution remains NOT_RUN. Evidence: `spread-shadow-runs/hood-launch-unblock-20260925/solo-v1/`.

# Cycle-165 HTTP 429 correction — verified and active, 2026-09-25

Published main `5a84bf1` completed cycle-163/164 before cycle-165 stopped in leverage preparation, with no order creation intent. Four SDK account/accountActiveOrders HTTP responses were 429 immediately after the second leverage transaction. Generic exception mapping incorrectly called this an account identity read failure, and leverage readback had no rate-limit recovery. A separate owner-authorized read-only observation found both accounts exactly flat and no active selected-market orders; historical journals are unchanged. The prior cycle-158 delayed-order cleanup is not this failure.

Candidate adds typed credential-free 429 classification, bounded increasing read-only cooldown with Retry-After, fresh validation and simultaneous-error precedence. No setting replay, order-policy change or successful admission delay. Initial 73-check run exposed a test hook bypass when the first implementation moved the raw account boundary; preserve the existing _accounts boundary and wrap only leverage/preparation callers instead. The corrected 296 focused checks and 28 rate-limit regressions passed. First full clean suite: 1 failed, 5768 passed, 3 skipped; a one-argument account-read test hook rejected an unnecessary optional None argument in the new wrapper. Remove that unused argument and keep the existing test unchanged. After the signature correction, 269 related checks passed with existing tests unchanged. Final clean detached Python 3.11.5 / lighter-sdk 1.1.2 full suite at `866b963`: 5769 passed, 3 skipped, 2 dependency warnings, exit 0 in 141.90 seconds. Same-agent self-review completed; no independent review claimed. Identical tested source integrated into main and activated as controller PID 57418 under operator/instance locks with no trading child and an empty startup log. Configuration/state bytes are unchanged, including blocked cycle-165 series (2/10 completed), membership and offset 227121303. Historical cycle inputs retain their hashes. No agent financial command or series replay; real post-fix venue execution remains untested. Evidence: `spread-shadow-runs/hood-rate-limit-20260925/solo-v1/`.

# Cycle-158 delayed maker cleanup — verified and active, 2026-09-25

Candidate from published main `0166a29` fixes the single-read cancellation gap after ACK receiver refusal. Historical cycle-158 accepted the closing LIMIT without a venue ID, refused the receiver on a better L2 price, missed the exact source on its immediate read, then stopped with an active order. A later separate read proved that source filled and left the other account short; the historical journal remains unchanged. The correction polls exact source identity within existing time/count bounds, stops on conflicts, reuses freshly validated evidence for one-shot cancellation, and preserves all terminal trade/position and series gates. Both initial publication and disappearance during cancellation recheck are covered. No receiver admission path or numeric policy changes.

Self-review completed (same agent, not independent review). Initial focused runs passed 390 integration checks and all 45 new regressions. All 28 adverse publication counterexamples fail on the accepted base and pass on the candidate. First clean full run: 4 failed, 5737 passed, 3 skipped; four variants of an older related regression required exactly two history requests even though earlier cleanup now resolves the order before history and only one is needed. Replace that incidental count with exact-ID history and actual external trade identity; retain quantity, history completeness, flatness and mutation-count checks. The resulting 79 focused close-reliability/new regressions passed. Final clean detached Python 3.11.5 / lighter-sdk 1.1.2 full suite at source/tests `5d8ce1d`: 5741 passed, 3 skipped, 2 dependency deprecation warnings, exit 0 in 157.94 seconds. Tested code integrated unchanged into local main and activated as controller PID 54063 under operator/instance locks with no trading child and an empty startup log. Configuration and saved state are byte-identical, including blocked cycle-158 series and offset 227121301. Historical cycle-158 inputs retain their hashes. No agent financial command, automatic closure or replay; live behavior remains untested by the agent. Evidence: `spread-shadow-runs/hood-ack-cleanup-20260925/solo-v1/` in the original workspace.

# USD report and executed volume — verified and active, 2026-09-25

Candidate from published main `650570d` adds nominal USD labels for configured Robinhood BTC cycle/series PnL and final executed USD turnover across both accounts. Price-times-quantity uses exact Decimal products/sums. Same-account trade duplicates cannot inflate volume; both sides of a mutual trade count separately. Unknown currencies/history/fields remain unknown, while complete empty histories prove zero. Gross/net fee and funding semantics and pre-cycle exclusions are preserved. No trading engine, risk, settings, persistent-state schema or network changes.

209 focused tests passed, covering USD binding, paired/external/residual volume, missing fees, empty histories, malformed/missing receipts, same-account duplicates within/across cycles, unsupported denominations, exact small digits, pagination and existing controller safety. The first saved-journal replay found that the old bounded report projection discarded venue/domain fields, so the new USD gate correctly returned unknown. The projection now preserves only the exact public Robinhood constants for a matching BTC binding; arbitrary endpoint strings remain omitted, covered by a credential-redaction regression. Rerun independently proved 350.411633 USD turnover across example cycles 142–144 using rational quantity-times-price arithmetic; gross PnL is -0.009701 USD and net remains unknown. This example does not assert one historical command membership. Final clean isolated Python 3.11.5 full suite: 5696 passed, 3 skipped, 2 dependency deprecation warnings, exit 0 in 153.98 seconds. Source/tests `eb584de` integrated unchanged into local main and activated as idle controller PID 48273. Operator/instance locks, no trading child and empty startup log verified; configuration/state bytes, offset 227121299 and last series membership (cycles 152–154) preserved. Self-review only; no agent live order. Evidence: `spread-shadow-runs/hood-series-usd-volume-20260925/solo-v1/` in the original workspace.

# Uncapped finite series and summary — verified and active, 2026-09-25

Candidate from published main `f9cad71` removes the five-cycle parser/state ceiling without changing tick limits or execution safety gates. New multi-cycle commands durably retain unique cycle membership across terminal/error/restart handling. The background sender streams per-cycle own/external/mixed/unknown execution and exact aggregate gross/net execution PnL, with in-cycle residual fills included and unknown fees, incomplete results, funding and pre-cycle closures explicitly distinguished. `/report` repeats the saved series summary; legacy membership is not invented. Numeric slot sorting covers the cycle-999/1000 boundary.

187 focused checks passed, including real controller sequencing for 6/10/12 synthetic cycles, restart membership, unsafe-result stops, missing economics, mixed execution, exact negative/zero sums and delivery of a 5000-cycle synthetic report without enqueuing every page. The first expanded run failed only an arbitrary test expectation of more than 20 pages for 700 rows (the valid bounded layout used 15); replaced it with the actual contract: multiple pages, each within the limit, every row exactly once and one aggregate. Final clean isolated Python 3.11.5 suite: 5674 passed, 3 skipped, 2 dependency deprecation warnings, exit 0 in 149.23 seconds. Source/tests `be2f073` were integrated unchanged into local main and activated as idle controller PID 43042. Both locks, no trading child and an empty startup log were verified; configuration/state bytes, offset 227121298 and last completed cycle-151 series were preserved. Saved replay of cycles 142–144 independently sums fill cashflows to -0.009701 quote gross; this is a reporting verification example, not a claim that those three cycles belonged to one historical command. Self-review only; no live trading by the agent. Evidence: `spread-shadow-runs/hood-series-results-20260925/solo-v1/` in the original project workspace.

# Cycle/close audit — verified and active, 2026-09-25

Self-review from accepted local main `6f9652e`. Saved cycles 142–144 are COMPLETE/CONFIRMED_FLAT; every saved paired-close/residual-close plan is reduce-only. Cycle 143 recovered an external opening fill, and 144 needed a second residual order after a proved zero-fill slippage cancellation. No opening order inside their closing paths was found. These are historical proofs, not current account observations.

Confirmed defects: `/close` reused an acknowledgement promising a new real cycle; per-cycle notifications lacked series identity and transition messages; closing preparation and early residual evidence could retain stale HOLD/PREPARING display. Candidate corrects these views and sends progress/final notifications through a bounded background queue that cannot gate the next cycle. No trading/risk settings changed. Initial focused execution lacked pytest in the runtime; an isolated test environment was provisioned. First 157-check run passed with display-task teardown warnings; explicit test teardown and in-task timeout corrected the harness, and the next 157 passed without warnings. Final expanded focused run: 158 passed without warnings. Clean isolated Python 3.11.5 full suite: 5645 passed, 3 skipped, 2 dependency deprecation warnings, exit 0 in 149.39 seconds. Tested source/tests `43b331b` were fast-forward integrated unchanged into local main and activated as idle controller PID 36141. Both locks and absence of a trading child were checked; configuration/state bytes, offset 227121296 and last completed series were preserved, with no startup errors. Evidence: `spread-shadow-runs/hood-cycle-close-audit-20260925/solo-v1/`. No live order, replay or remote publication.

# Bounded Telegram cycle series — active 2026-09-25

A fresh authenticated `/run ack 1 5` now requests five sequential BTC cycles; `/run 5` uses saved mode/ticks, and omitted count remains one. The controller persists the total/current step before each child, holds one exclusive owner command across the series, requires a complete flat prior cycle with no unresolved order state and a fresh current recovery check before each next cycle, and uses the existing one-use reduce-only close when newly proved inventory appears. Unknown/partial results, failed checks/configuration/persistence and restart stop without automatic continuation or mutation replay. Telegram acknowledgement, status and final result show requested/completed progress. No opening size, leverage, price, fee, retry or admission policy changed.

Self-review passed 240 focused checks and the final clean isolated Python 3.11.5 suite: 5636 passed, 3 skipped, 2 dependency warnings, exit 0 in 131.99 seconds. The first full run had six failures in unchanged localhost/process-inspection tests because the restricted sandbox denied those operations; the same full suite passed with those local capabilities available. Both logs, tested-source manifest and activation proof are in `spread-shadow-runs/hood-telegram-series-20260925/solo-v1/`. This was the same agent's review, not independent review.

Integrated unchanged tested code `f5a03e8d205df008d7064b60bbe86bfa9f639283` into local main and activated idle controller PID 31258 under the operator and instance locks. Config and saved controller state remained byte-identical, including offset 227121294, active=null and last cycle-141; startup log was empty and there was no trading child. No agent live series, order, cancellation, settings change, command replay or remote push occurred. Future fresh owner commands use the new syntax; no live five-cycle outcome is yet measured.

# Fresh /run pre-cycle close — active 2026-09-25

The active local runtime was `main` at `518175d51bcd51a41cde45c341e64f5ab57e6a24`; the older HCR-43 worktree was not the running source. Remote `origin/main` remained at `d6ac3aa969dd6789ae7f747a84b3e46402df8fe4`. The controller defect was that a fresh `/run` called flat-only recovery and refused proved existing BTC positions before the available `/close` operation could reduce them. The correction uses one persisted pre-cycle close intent for a fresh owner `/run`, the existing bounded reduce-only close path, one unambiguous terminal `CONFIRMED_FLAT` close, and a fresh exact zero-position `READY` check before opening. Missing/unknown/partial closure, changed configuration, missing account proof and interrupted persistence never launch or replay a cycle. Restart after a completed close but before cycle creation explicitly reports that no cycle started.

Self-review passed 162 focused checks and the final clean isolated Python 3.11.5 suite: 5608 passed, 3 skipped, 2 dependency warnings, exit 0 in 156.88 seconds. The first full run had one unchanged permissions-test failure because the harness inherited umask 077; that test passed under normal umask 022, and the final full run used a second clean checkout with umask 022. Source/tests match tested commit `c9281fe3d6ae32aab688ce08f91cc2352cc0926f`; only README wording changed afterward. Evidence: `spread-shadow-runs/hood-auto-close-before-run-20260925/solo-v1/`.

Integrated at local main `14c497533ea77a277e74781aafbea9004c3dfdff` and activated idle controller PID 20644. Operator and instance locks, no active child, clean startup log and exact configuration/state bytes were verified; offset 227121292 and active=null were preserved. No real order or cancellation was sent during this change. The last fresh account observation before activation found both selected BTC positions exactly zero with no active BTC orders; a nonflat live auto-close has not yet occurred. Remote publication was not performed.

# Receipt time reconciliation — verified

Cycle 138 filled both 0.00029 BTC legs, but an early clock-ahead receipt error remained sticky after a valid exact reread. Candidate tracks unresolved exact future receipts within existing read bounds, independently of order/position/fee checks. The same defect in close-013 is addressed in residual-history reconciliation. No timestamps are relaxed or rewritten, no mutation replay or new trading settings. 55 focused checks passed. Prior engine-only full snapshot was superseded when the fallback defect was found; final clean Python 3.11 combined run passed. Evidence: spread-shadow-runs/hood-receipt-time-20260924/solo-v1/.

Self-review complete: 55 focused checks passed. The final combined clean isolated Python 3.11.5 suite passed 5577 tests, 3 skipped, 2 dependency warnings, exit 0. Source/tests match the final-v2 installed snapshot and original cycle138 inputs are unchanged. Base reproduction failed the two repaired cases as expected. No real orders or automatic startup configuration.

Integrated unchanged tested source a2d6175 into local main and activated controller PID13598 under idle locks. Config unchanged, including 15s request timeout; instance lock held, no child and no startup errors. Saved operation state was preserved; historical journals were not rewritten. No financial command or remote push. Activation proof: spread-shadow-runs/hood-receipt-time-20260924/solo-v1/final-v2/activation.json.

# Telegram timestamp handling — verified 2026-09-24

Telegram API was reachable with no pending updates/webhook, but its HTTP Date led the host clock by about 2 seconds. The old owner-command gate silently consumed all future-dated messages. Fixed with a bounded 5-second allowance, explicit timestamp refusal and safe delivery-failure logging; identity, freshness, restart and no-replay checks remain. Historical discarded message dates are unavailable, so this is a reproduced defect consistent with the observed silence, not proof of every lost message.

Self-review complete. All 49 focused controller checks and the final clean isolated Python 3.11.5 suite passed: 5566 passed, 3 skipped, 2 dependency warnings, exit 0. The installed source matches the workspace src/tests manifest. No live orders. Evidence: spread-shadow-runs/hood-telegram-clock-skew-20260924/solo-v1/.

Activated on local main2354675 as controller PID 11509; instance lock held, no trading child, startup log empty and working config unchanged (request timeout 15s). No real trading command; no remote push. Activation evidence: spread-shadow-runs/hood-telegram-clock-skew-20260924/solo-v1/activation.json.

# Interrupted Telegram close recovery — active 2026-09-24

Code integrated into local main at `61f666159c9cd7f31b4cb4127f8bd3a8cfaedc3d` now reconciles an inherited `/close` with the existing bounded `CLOSE_READY` check on startup. Controller PID 9097 is connected to Telegram and holds the instance lock; the operator lock is free and no trading child exists. Its administrative `active` marker is clear, while `close-011` remains historically `BLOCKED` because its journal has no terminal record. Fresh startup proof resolved all 428 prior creation intents (three by current exact read), found no active selected-market orders, and observed positions -0.00025 BTC on account 27331 and +0.00025 BTC on account 27337. The original cycle/close journals and configuration hashes are unchanged; no agent order, cancellation, setting or Telegram trading command was sent. `/run` remains correctly refused until a fresh check proves both positions exactly zero; the owner can send a new `/close` to attempt reduce-only closure.

Five new adverse startup/run-guard checks and 189 related checks passed. The clean isolated Python 3.11.5 / lighter-sdk 1.1.2 full suite ran without deselection: 5552 passed, 4 failed, 3 skipped, 2 dependency warnings. All four failures are in unchanged `test_s3_envelope_evidence.py`: its synthetic 2.7e12 ns event is later than the host's monotonic clock at finalization, so the old test reports time travel. The same four failures reproduced on the untouched base `main`; the initial dirty-tree run also triggered ten evaluation-run clean-checkout failures, absent in the clean run. No unrelated source or tests were changed. This is self-review with a known pre-existing full-suite limitation, not an all-green claim. Owner-only evidence: `spread-shadow-runs/hood-telegram-close-recovery-20260924/solo-v1/`. Remote main was not pushed.

# Controller activation — 2026-09-24

Controller started as PID 4416 with request_timeout_seconds=15. Process alive and instance lock held; configuration unchanged and startup log empty. No trading command sent. Verification: spread-shadow-runs/hood-request-timeout-15-20260924/activation-v1/verification.json.

# Request timeout — configured 2026-09-24

The working operator random-cycle.json now explicitly sets request_timeout_seconds=15 (previously 5). Actual local parser validation passed; all other config fields unchanged. Telegram binding updated under operator/instance locks, preserving saved history/offset/active state. Controller was not running and was not started. Applies to subsequent runs using this configuration; no source change, no financial action. This does not resolve the cycle 137 residual-close defect. Evidence: spread-shadow-runs/hood-request-timeout-15-20260924/solo-v1/verification.json.

# Attempt limits 6 / 15 — integrated and active

Owner requested up to6 opening attempts and15 paired-closing attempts, including the first. Shared preparation/zero-fill budgets now differ by phase; confirmed pilot and separate residual recovery remain unchanged. Updated CLI limits and execution notices beyond attempt3. Tests cover exhaustion, unique order IDs, success at attempts6/15, preserved quantity/hold and final-attempt messages.317 targeted plus43 related checks passed. Final clean isolated Python3.11 full suite:5551 passed,3 skipped,2 dependency warnings,exit0. Self-review complete; no agent real trading. Evidence:spread-shadow-runs/hood-attempt-limits-20260924/solo-v1. Integrate identical tested source and activate an idle controller with config/state preserved.

Activation verified on local main793af63, controllerPID24950: tested source identical, config/state byte-identical, no active child, no trading command. Remote publication not performed.

# ACK cancellation/recovery fixes — integrated and active

Owner-run audit112–126 identified an ACK cancellation nonce reservation conflict, sticky provisional source absence, delayed residual-close receipts/positions and misleading terminal reporting. Sole implementation on codex/ack-recovery-fixes from accepted main5e557f1 corrects these paths without changing pricing, admission modes, quantity, leverage or ambiguous-write policy.22 new adverse regressions cover actual SDK nonce ownership, signing failure, ambiguous cancel without replay, both cycle phases, delayed/missing/contradictory histories, account identity and reporting. Saved-journal replay preserves54 original files and now reports119's external accounts18227/2302 while retaining its historical UNKNOWN;122 retains SUCCESS without an old retry reason.

Final clean isolated Python3.11.5/lighter-sdk1.1.2 suite:5548 passed,3 skipped,2 dependency warnings in136.91s, exit0. The unchanged frozen paper runtime emitted its known pending-task teardown diagnostic.39 targeted isolation/regression checks passed. Initial failures and their test-harness/contract diagnoses are retained, not hidden; no unrelated frozen code changed. Self-review complete. Evidence:spread-shadow-runs/hood-ack-recovery-fixes-20260924/solo-v1. Integrate identical tested source and activate only under idle operator/instance locks, preserving config/state. No agent real trading; live recovery effectiveness remains to be observed in owner runs.

Activation verified at local main537a6d0, controllerPID22651: operator/instance locks checked, no active child, tested source identical, config and saved controller state byte-identical before/after restart. Historical offsets and journals were preserved; no trading command was sent. Remote publication was not performed.

# Tick/ACK switch — integrated and active

Sole implementation on codex/ticks-ack-switch from accepted main239e498. Implemented explicit1..5 tick selection, ACK admission with retained local L2/known-event veto and post-send exact-source cleanup, per-run Telegram/terminal switches and bounded public BBO evidence. Activated default:5 ticks/ws_confirmed. ACK remains an explicit per-run selection; no agent real orders. Final source8a9fe17 passed161 focused checks and clean isolated Python3.11.5/lighter-sdk1.1.2 full suite5526 passed/3 skipped/2 dependency warnings in158.22s, exit0. Self-review complete. Controlled offline removal of an injected200ms wait measured202.173ms→0.552ms; this is not venue latency. Narrow-close evidence confirmed no invalid LIMIT and residual recovery. Evidence: spread-shadow-runs/hood-ticks-ack-20260924/solo-v1. Initial fixture failures included an inconsistent terminal cache, an expectation of automatic closure under existing ambiguous-close barriers, and synthetic BBO crossing after ten updates; corrected the models/expectations, not product safety policy. Actual ACK speedup and own-fill probability remain unmeasured.

Integrated unchanged tested source8a9fe17 through local maind6fce47. Idle controllerPID18931 activated under operator/instance locks; no child/active operation. Only config price_improvement_ticks=5 and derived controller binding changed; all other config, saved history and offsets preserved. Source identity verified; no startup traceback, operator lock free and instance lock held. Commands /run ws5 and /run ack5 are entered with spaces (`/run ws 5`, `/run ack 5`); selections apply to one cycle including paired closing. No remote push or live agent test.

# Paired-close HTTP warmup — integrated and active

Sole implementation on codex/close-http-warmup from7eb5066. Seven focused regressions passed, including failures, cancellation, local HTTP pool reuse and server-forced reconnect without replay. Final clean isolated Python3.11 suite at3597f05:5467 passed,3 skipped,2 dependency warnings in154.10s, exit0. The unchanged paper runtime also printed a pending-task teardown diagnostic. Full diff self-review completed. A bounded pair of public Robinhood reads confirmed connection reuse; this is not a live order-speed measurement. No real trading. Evidence: spread-shadow-runs/hood-close-http-warmup-20260924/solo-v1. ACK remains request acceptance only; current ws_confirmed source-order gate remains unchanged.

Accepted main had advanced with an unrelated reporting correction before activation; the initial activation stopped on its base assertion before touching the controller. Both changes were preserved in merge8f63030, with each source byte-identical to its tested predecessor. The final combined clean isolated Python3.11 suite passed5476/3 skipped/2 dependency warnings in148.56s, exit0. Integrated unchanged in local main8f63030 and activated idle controllerPID12517 under locks. Config/state bytes preserved, no child/active operation, instance lock held and operator lock free. No remote push or agent financial action. Live paired-close latency improvement remains NOT_MEASURED. Combined evidence is solo-v1/merged-v1.

# Three real BTC diagnostic runs and reporting correction — 2026-09-24

The owner's three authorized Mainnet diagnostic cycles are complete and all one-use claims consumed. At exactly 0.00040 BTC and 5000-bps (2x) selected-market cross initial-margin settings, cycles 109/110 sent real source LIMITs and canceled at proved zero fill after L2 veto. Cycle 111's source externally filled 0.00040 BTC during exact REST cancellation recheck; its one in-cycle reduce-only recovery canceled at zero fill. Necessary separate `close-006` then sold the exact residual reduce-only, with a terminal result and later fresh read confirming both configured BTC positions zero and no active selected-market orders at the observation time. No receiver MARKET or paired close occurred, so there were zero own reciprocal matches in three cycles. Gross closed execution PnL is -0.010440 quote; fees and funding are unknown, so net and funding-inclusive results are unknown. The tested diagnostic source `350042f` remained isolated and did not change the ordinary controller. Its clean full suite passed 5469 tests, 3 skipped, 2 warnings. Source journals, manifests, causal timing, account readback and self-review are in owner-only `spread-shadow-runs/hood-three-live-diagnostics-20260924/solo-v1/`. No fourth opening is authorized.

The saved-result defect reproduced from cycle 111 is corrected at reporting-only source `6f1b4e6`: `OPEN_INVENTORY` is shown explicitly, and the latest later close for the same account pair and market is shown separately with its own timestamp. An incomplete, partial or mismatched close cannot assert flatness, and saved results never claim current state. Read-only replay on the actual journals selected `close-006` and left cycle 111 historically open. Seventy-one focused checks passed; the final clean isolated Python 3.11.5/lighter-sdk 1.1.2 suite passed 5469, 3 skipped, 2 dependency warnings in 148.24 seconds. This is self-review, with no trading/risk/transport changes. Local main `7a0c19f` integrated the tested source unchanged; the idle controller restarted as PID 11270 with instance lock held, no child, config/state byte hashes and offset 227121222 unchanged. A runtime-source read-only replay confirmed the corrected view. No activation trading command or remote push occurred. Reporting verification and activation evidence is in the packet's `reporting-fix-v1/` subdirectory.

# Race latency verification — integrated and active (2026-09-24)

Parent works alone on codex/ws-race-measurement-20260924 from main a080bbe. Bounded safe Robinhood probes observed exact-ID unsigned WS validation rejection21602, 12 warmed HTTP/WS comparisons (medians298.894/279.383ms; ping278.455ms), and subscription acknowledgements for both account_tx channels with zero transaction events. These do not establish accepted mutation timing or earlier resting-order proof. Preserve HTTP and current admission. Candidate fixes the inactive sender rejection parser and retains raw venue order/transaction diagnostics in the bounded background journal. Focused210 tests passed after final self-review isolated transaction diagnostic errors from admission counters. An initial clean suite at1313d01 passed5459/3 skipped/2 warnings; the final changed source requires a new full run. One fixture first hit the frame-size rather than transaction-count bound, one summary fixture needed the new diagnostic counters, and initial runtime Python lacked pytest. Final clean isolated Python3.11.5 suite at37b240d passed5460/3 skipped/2 dependency warnings in152.03s, exit0. It also printed a pending-task teardown message from unchanged PublicPaperRuntime. The ten-second candidate read session reached readiness and both transaction subscriptions without malformed frames or gaps; it observed no own order/transaction. Tested source/tests37b240d are integrated unchanged in local main through e7cebf1. ControllerPID89828 was activated under idle locks; no child/active operation, config/state/offset/history unchanged. Ordinary writes remain HTTP; next owner cycles collect passive transaction timings. No remote publication or agent real execution occurred. Evidence: spread-shadow-runs/hood-ws-race-20260924/solo-v1. No agent real order, cancel, tier change or relocation.

# Owner WS runs086–089 — verified and active

All four saved cycles completed with PARTIAL pairing and CONFIRMED_FLAT inventory. Each source limit was canceled with zero execution; no receiver MARKET, HOLD or paired close occurred. Every refusal was the old combined L2 price/quantity mismatch. The old refusal omitted book values, so public publication lag versus actual competition cannot be resolved historically. Do not relabel the historical stops as proved false positives.

Real local opening intervals: source ACK median310.086ms (306.080–441.220); ACK→exact observation median204.328ms; source intent→guard decision median520.305ms (518.073–529.165), versus931.133ms across082–085 opening decisions (~44.12% lower). Different samples/conditions, not controlled causal proof; new source→MARKET and paired-close timings are NOT_MEASURED because those sends never occurred. Guard→cancel intent median298.575ms, cancel acknowledgement307.237ms. Sources and hashes: spread-shadow-runs/hood-ws-postruns-20260924/solo-v1/final-measurements.

Candidate codex/ws-postruns-086-089 from main76e9695: treat L2 as a negative veto instead of requiring a second positive source publication, preserve fresh exact private source admission, journal bounded diagnostic source/best-level values on both outcomes, separate decision from send timing, restore existing bounded retries only after proved zero-fill canceled liquidity vetoes, and explain stops in Telegram/terminal. Four private-before-public counterexamples reproduce on the accepted base; focused391 checks pass. Final clean isolated Python3.11.5/lighter-sdk1.1.2 suite at9041a3c:5432 passed,3 existing skips,2 dependency warnings in147.46s. First full run had one test-harness umask077 failure; file-mode reproduction confirmed it, the same test passed with normal022, and the whole suite was rerun without product/test changes or skips. Self-review complete. Activated via local main9b29a39/controllerPID84493 with tested source unchanged, identical config/binding/state/offset227121200, last089 FINISHED and no active operation. Instance lock held, operator lock free, no child/startup traceback;58 original input files unchanged. No remote push or agent real financial action.

# WS-confirmed admission — verified and active

Candidate branch codex/ws-confirmed-admission from accepted main c52ff46. An explicit mode removes repeated REST account/book/final-source admission in paired opening and closing; exact fresh private WS plus local L2 veto replaces that admission. Strict remains the omitted default. Full account and owner/FIFO equivalence is deliberately not claimed. Transactions, authoritative final reconciliation, recovery and ambiguity/no-replay rules remain unchanged.

Controlled offline comparison with identical injected delays and six opening/closing observations per arm: median LIMIT→MARKET 931.588ms strict versus 530.472ms ws_confirmed, 401.116ms/43.06% reduction (1.756x). This is a synthetic mechanism measurement, not real network/matching performance. The earlier failed expired-account cleanup test modeled old creation timestamps as fresh REST timestamps; corrected to actual SDK observation semantics and retained a negative stale-response control. A CLI fixture initially supplied an invalid null margin reserve; no product workaround was added. Final clean isolated Python3.11.5/lighter-sdk1.1.2 suite at b535a4b:5411 passed,3 existing skips,2 dependency warnings in154.69s. Final source controlled rerun:923.837ms→524.102ms,399.735ms/43.27% reduction (1.763x). Self-review completed. Activated via local main cc608f3:controllerPID81259 uses source identical to tested b535a4b. Only config receiver_admission=ws_confirmed and its state binding changed; offsets/history/last085 FINISHED and no active operation were preserved. Instance lock held, operator lock free, no child/startup traceback. No remote push or agent trading. Evidence: spread-shadow-runs/hood-ws-confirmed-20260924/solo-v1.

## Post-run correction — verified and active

Integrated locally viaef44451; idle controller restarted asPID73820 with unchanged tested src/tests. Config/binding preserved; startup report reconciliation changed cycle081 BLOCKED→FINISHED and cleared its administrative active state through Controller.finish, with no manual reset/replay. Instance lock held, operator lock free, no child. No remote push or agent financial action.

From accepted main35f0a9d on codex/postrun-report-latency: fix stream diagnostic trailers that invalidated every saved cycle report; extend narrow terminal source-only classification to proved canceled zero-fill; avoid redundant REST start when a validated exact WS observation is already ready; add bounded redacted numeric GET diagnostics to both actual transports. Focused399 tests passed. Historical076/077/078/079/081 now report COMPLETE/CONFIRMED_FLAT with original files unchanged;080 retains its historical explicit UNKNOWN, while new synthetic reproductions in both directions/opening/closing resolve safely. Final clean isolated Python3.11.5 / lighter-sdk1.1.2 suite at05bbcf5:5368 passed,3 skipped,2 dependency warnings in140.35s. Initial full run exposed3 early-admission diagnostic regressions; fixed before acceptance,46 focused regressions passed. Self-review completed; no independent review claimed.

Bounded read-only Robinhood orderBookOrders comparison:16 alternating public reads (8 per transport), first cold request of each excluded: plain median83.943ms, SDK93.631ms. Four interrupted public reads followed by new requests produced median286.807ms (~202.864ms above warmed plain). This demonstrates potential connection-loss cost, not attribution of old accountOrders latency or live LIMIT-to-MARKET improvement. Remaining race cancellation is unchanged if WS arrives after REST starts. No private credentials or real orders were used. Evidence: spread-shadow-runs/hood-postrun-fix-20260924/solo-v1. Conditional old/new code comparison (six observations per arm, synthetic ready WS + actual public GET): median observation plus next read274.859ms→82.609ms,192.250ms saved (~69.94%);GET count2→1. This measures only already-ready WS, not a live trading cycle. Original full-cycle live intervals remain1.469–2.367seconds; faster live matching is unmeasured.

# Telegram /close WS journal correction — active

Source 412b87558a53ae391ae4013fac4ea1ca1ccb5a30 fixes a reproduced blocker: historical recovery treated stream-events.jsonl as a DurableJournal and refused /close before account checks. The reserved diagnostic file is now hash-retained but excluded from mutation-event parsing; other journals and symlinks retain strict validation. Nine new regression cases include actual Telegram command routing through synthetic recovery/closing with a saved active barrier. Focused suite: 87 passed. Final clean isolated Python3.11.5/lighter-sdk1.1.2 full suite: **5343 passed, 3 skipped, 2 dependency warnings**, 145.33 seconds. Self-review of full diff and actual source imports complete.

Actual immutable journal readback now succeeds: 209 creation intents/191 files. A bounded read-only current admission check returned CLOSE_READY, no unresolved leverage settings; at that observation account27331 was zero and account27337 held +0.00089 BTC. No agent order/cancel/setting was sent. Historical journals/config/controller state were not reset; only the existing normal recovery checkpoint was appended. Owner sends a fresh /close after activation; the consumed failed command is not replayed. Evidence: spread-shadow-runs/hood-close-ws-journal-fix-20260924/solo-v1. Activated on local main at 9a6e501 with unchanged tested src/tests. Old idle controller67775 stopped under launch-lock exclusion; new PID69366 alive, connected to Telegram, instance lock held, operator lock free, no child or startup error. Config/binding and saved active cycle-075 state unchanged. Explicit /close can perform its own fresh recovery admission despite the preserved historical barrier. No remote publication claimed.

# Cycle-075 corrections — active in local Telegram controller

Tested source 45ef19cdca388467eb021cb80fb6b9664f97bd66, branch codex/ws-cycle-recovery, based on local main 545a234. Final clean isolated Python 3.11.5 / lighter-sdk 1.1.2 full suite: **5334 passed, 3 skipped, 2 dependency warnings**, 147.55 seconds. Actual imports and clean source were verified. Twenty-three new regressions cover the observed source-only fill with empty early account snapshot during opening/closing and both directions, adverse reconciliation and nonce failures, WS/REST ordering, exact identities and read-task cleanup. The prior full run also passed; the final rerun includes the corrected diagnostic field name. Self-review only.

Missing source evidence still blocks the receiver but no longer masquerades as a conflicting active order. Complete exact source-only reconciliation allows existing reduce-only recovery; ambiguous mutation/identity/history still blocks it. Initial/terminal visibility now races WS against REST; final active-source REST and public owner/queue proof stay in place. Cancel nonce acquisition starts after accepted source ACK, overlaps visibility, and uses the existing one-use registry. Cancellation readiness/wait timings are now honest. No owner test-count quota is introduced; no saved active intent or historical position is reset.

Controlled offline timing with injected REST 297ms and WS 200ms returned after approximately 202ms instead of 299ms (three samples); this demonstrates the waiting mechanism, not a venue latency or fill-rate result. No real order, cancellation or account-setting write was made. Evidence: spread-shadow-runs/hood-cycle075-fix-20260924/solo-v1, including final-full-suite.log, final-source-provenance.json, review.md and controlled-read-race.json. Runtime activation: local main integrated 07bce42; old idle controller 63977 stopped under launch-lock exclusion, new PID 67775 runs the identical tested source. Config and binding are unchanged; original saved active cycle-075 state remains BLOCKED, with no manual reset. Instance lock is held, launch lock is free, no child and no startup log error; established Telegram connection observed. Owner can request `/close` for the historical residual, then use normal sequential `/run` tests. No remote push claimed.

# Current status

## WS version activated for owner-run Telegram tests — 2026-09-24

The owner requested activation and will launch real tests through Telegram. Local main integrated 9ffe8fd; src/ and tests/ are byte-identical to tested 7f52812 (5311 passed, 3 skipped, 2 dependency warnings). The documentation merge preserves current main rules. Old controller PID 91534 was stopped only after acquiring the operator lock and proving no child or active saved intent. New PID 63977 runs from the local main checkout with Python 3.11.5 / lighter-sdk 1.1.2 and the same protected config/owner. Config, binding, saved state and offset were unchanged. Verification found the instance lock held, operator lock free, no child/active intent, empty error log and an established Telegram TCP connection. No trading command or financial action was performed by the agent. Source order final recheck remains enabled; transaction submission remains HTTP; the WS read lifecycle begins inside the next owner-run cycle. This activation supersedes older statements that the candidate is not active. Local integration is complete; no remote publication is claimed. Evidence: spread-shadow-runs/hood-ws-activation-20260924/solo-v1.

## Parent-owned WS read acceleration — implemented and self-verified

Chief confirmed stopped writes; its WIP at 44472ce is preserved untouched. Parent implementation and self-review completed on codex/ws-read-acceleration. Tested source: 7f528128bc8c56cd2f23cd477e16ec672a5594bf. Final clean isolated Python 3.11.5 / lighter-sdk 1.1.2 suite: **5311 passed, 3 skipped, 2 dependency warnings**, 146.64 seconds. Actual imports were verified against the isolated source. First full run had three reserved-route cleanup failures; these were corrected and the failed log remains preserved. The final route/WS/cycle focused group passed 237 tests.

Ordinary random cycles now use continuous WS L2 for pricing and full validated positive private-order events for discovery/terminal lookup, with freshness, identity, gap and disconnect invalidation plus REST fallback. Prepared cancellation starts on first active visibility while public-book retrieval is pending. Final active source and public owner/queue checks remain REST. Account/margin/history reconciliation remains REST. The inherited warmed WS transaction sender remains inactive pending actual Robinhood response evidence. Standalone emergency close does not start a new stream; paired closing uses the cycle stream.

Final live read-only comparison at e5d28e1 (book algorithm unchanged in tested source): five REST book reads min/median/max **88.584/94.767/534.599 ms**; local WS book materialization **0.770/0.784/0.820 ms**, receipt age **62.882/73.803/281.050 ms**. Startup 1920.620 ms before dispatch. Best bid/ask matched in four of five sequential comparisons; one bid differed. No book gap/malformed event, but zero private order events. Direct cache access was timed; integration tests cover SDK/cycle routing. This is neither an atomic snapshot comparison nor proof of faster LIMIT submission, live cancellation, LIMIT-to-MARKET or mutual fills. No order, setting, cancellation, deployment or controller restart occurred. Main/runtime remain unchanged.

Owner-only evidence: spread-shadow-runs/hood-ws-read-acceleration-20260924/solo-v1, including review.md, final-read/live-read-comparison.json, full-suite-v2.log and final-source-provenance.json. This is self-review, not independent acceptance. Earlier ownership and candidate descriptions below are historical; this section supersedes them.


## Confirmed single BTC pilot — completed, paired execution failed

On 2026-09-24 the owner confirmed the concrete single-cycle limits and then separately authorized at most one exact 10000-bps (1x) cross-margin setting per configured BTC account. The dedicated one-use pilot command fixes 0.00020 BTC, 40.00 quote per-account cap, proved 20-second hold, one paired attempt per phase, one exact residual recovery attempt per account, 0.10 modeled fee ceiling and read-only stream readiness. At tested source `c8231f7`, 224 relevant focused checks and a clean isolated Python 3.11.5 suite passed **5271 tests, 3 skipped, 2 dependency warnings**. Fresh preflight was flat with zero active orders and observed 5227/5712 bps. The one-use pilot claim was consumed; one setting transaction per account was confirmed effective at 10000 bps. One source POST_ONLY LIMIT was dispatched, then canceled at zero fill when a fresh public book showed better-priced volume. The receiver was not dispatched. The saved result is `PARTIAL`, paired execution `FAILED`, inventory `CONFIRMED_FLAT`, economics `KNOWN` with zero execution fees and zero closed execution PnL; funding remains unknown. Fresh postrun readback proved both positions exactly zero, no active orders and both settings 10000 bps. The pilot is complete and cannot be replayed. The ordinary Telegram controller and configuration were unchanged; no code deployment or second cycle occurred. Owner-only evidence is in `spread-shadow-runs/hood-confirmed-pilot-20260924/chief-v3/`; this is self-review, not independent review or evidence of the full product objective.

## Owner-led five-part development — Chief owns implementation and integration

The owner transferred ongoing project development, self-review and integration ownership to Chief task `01a0cf55-1311-7213-9d5a-905404eb46e1` on 2026-09-23. Parent auditor task `01a0c9cd-c8bc-75b0-92b3-04b75270a65e` is no longer a management or acceptance gate. The complete five-part objective, each current state, next action, result criterion and evidence path are at the top of NEXT_TASK. The accepted HCR-43 milestone and current HCR-44 connector remain development evidence, not proof of live mutual-fill improvement. Actual main/runtime identities must be checked before integration or activation; this handoff itself changed neither. System/tool and AGENTS safety boundaries remain in force.

## HCR-44 bounded read stream — tested read-only candidate

At source commit `0ae0d976e9d6ce2c79fcf6b85b7a34d1729fb3c0`, the branch adds an optional in-memory, read-only cycle stream with strict frame, total-input, duration and freshness bounds. It accepts book and two private subscription snapshots for readiness; a book gap removes book readiness and a disconnect clears all channel and order hints. A matching terminal order event may wake one earlier exact REST lookup. REST alone remains the execution and admission proof. Focused stream/engine/SDK checks passed 114 tests. The clean isolated Python 3.11.5 full suite passed **5255 tests, 3 skipped, 2 dependency warnings** in 151.84 seconds; actual imports resolved to the tested source. One bounded read-only validation connection consumed the chief-v6 gate: 142 frames/195,847 bytes in the 10-second window, two private subscription controls, subscription readiness observed, zero book gaps, zero malformed frames and zero own-order events. Stop invalidated state. This does not prove private event completeness or trading admission. The socket is not started by either live entrypoint; no trade, cancellation, main integration or runtime activation occurred. Owner-only chief-v6 packet contains code checkout, suite log and safe connection summary. The actual diff was self-reviewed; no independent review is claimed.

## HCR-44 read-only stream measurement — incomplete live evidence

Chief-v5 at source `3c80d3e3a85ac7bfe0ea8090532332f51ec19bb0` confirmed safe private-order subscription snapshots for both configured accounts during one bounded 10-second read-only connection. It processed 150 frames/213,637 bytes, including one book snapshot, 146 continuous book updates and two account-bound private snapshots; no malformed frame, gap, transport error or recognized own-order event. These snapshots do not prove complete private history, fills, account balances or admission. The final clean Python 3.11.5/lighter-sdk 1.1.2 suite passed 5236 tests, 3 skipped, 2 warnings; 35 focused tests passed. Evidence and hashes are in owner-only `chief-v5`. The chief-v5 gate is consumed. The next development milestone is bounded persistent state and Robinhood-specific transaction/identity validation, without changing production admission or trading transport prematurely.

Chief-v4 at source `57eb1b6f5e3e6a07efdc323955b636b6375b9331` resolved the observed 64 KiB transport incompatibility for one bounded session. It processed 456 frames/348,107 bytes in a 30-second receive window, with a book snapshot and 452 contiguous book updates, zero book gaps and no transport error. Two frames were not classified by the existing safe counters; no private order event was recognized. Private subscription readiness, account snapshot semantics and live speed improvement were unproved at that checkpoint. The isolated Python 3.11.5/lighter-sdk 1.1.2 full suite passed 5234 tests, 3 skipped, 2 warnings; 33 focused checks passed. Owner-only `chief-v4` evidence records the result and hashes. No trading or controller action occurred.

Chief-v3 at source `aff409437341131e81673d87132face3c854e9f3` isolated the connection failure: one bounded read-only session locally sent WebSocket close code **1009** after three controls (250 bytes), proving at least one incoming decompressed frame exceeds the configured 64 KiB limit. The specific channel and frame contents are unknown; none was retained. No snapshot, private order event or live speed improvement was established. This gate is consumed. The final clean isolated Python 3.11.5 suite passed 5230 tests, 3 skipped, 2 warnings, and 29 focused adverse tests passed. Owner-only chief-v3 evidence and hashes are in `spread-shadow-runs/hood-hcr44-stream-measurement-20260923/chief-v3/`. The owner subsequently authorized the larger chief-v4 resource limit; no trading, controller change or activation occurred.

The owner authorized one bounded read-only Robinhood BTC/two-account stream session after offline instrument checks, with that consumed gate recorded in NEXT_TASK. Chief works alone on `codex/spread-v1-hcr44-stream-measurement` from independently accepted bounded HCR-43 candidate `8bafe306d5f4e413be5dcdaa43bb6f401f9f0036`. At instrument commit `387aec06d24850c833a33414adfcbfd75e4dcf32`, 125 relevant focused checks and the clean Python 3.11.5 / lighter-sdk 1.1.2 full suite passed 5227 tests, 3 skipped, 2 dependency warnings. The one read-only session processed three frames (250 bytes) over 1.718 seconds and stopped with an unclassified connection/protocol error. It saved no book or order projection, so stream completeness, order timing and any live speed improvement remain unproved; zero observed orders is not zero fills. A further offline-only diagnostic correction at source commit `981d08d042e379ec3d9631b9cfc45c2cad93d59a` adds safe error categories; 126 relevant focused checks and its final clean suite passed 5228 tests, 3 skipped, 2 dependency warnings in 149.23 seconds. This diagnostic change was not live validated. Evidence is in owner-only `spread-shadow-runs/hood-hcr44-stream-measurement-20260923/chief-v1/`. The bounded trading pilot remains a proposal without concrete owner-approved numeric execution authority. No trading-path shortcut, main integration or runtime activation occurred in HCR-44. This candidate has not received an independent review.

## HCR-43 observer correction — READY_FOR_REVIEW

Auditor v2 rejected chief-v2 HEAD `2280619779a5b9e36cf46aa9b46a32a6d27dee1f` for boolean/float inbound identity and epoch equality and regressing local receipt time. The rejected branch and chief-v2/auditor-v2 packets are preserved. The isolated branch `codex/spread-v1-hcr43-observer-correction` starts at accepted main `d6ac3aa969dd6789ae7f747a84b3e46402df8fe4` and ports intended HCR-43 changes with strict integer identities/epoch and persistent monotonic receipt chronology. Relevant focused checks passed 194 tests; the final clean detached Python 3.11.5 / lighter-sdk 1.1.2 suite at source commit `bf0a930c197f3263d6260b9064e8139dababe049` passed 5201 tests, 3 skipped, 2 dependency warnings in 141.23 seconds. The combined diff and actual imports were self-reviewed; owner-only evidence is in `spread-shadow-runs/hood-hcr43-low-latency-20260923/chief-v3/`. Auditor v3 independently accepted only this bounded offline correction, with 87 focused tests; its evidence is in `spread-shadow-runs/hood-hcr43-low-latency-20260923/auditor-v3/`. No live trading result or full strategy success follows from that acceptance.

## HCR-43 v1 low-latency candidate — historical checkpoint

The predecessor v1 branch `codex/spread-v1-hcr43-low-latency` began from accepted main `d6ac3aa969dd6789ae7f747a84b3e46402df8fe4`. It added numeric local HTTP mutation and cancellation-stage diagnostics and removed one duplicate exact source lookup after receiver terminal observation before exact residual cancellation, while preserving source/receiver proof gates and the paired closing route. Its 209 focused integration passes were an earlier checkpoint; the current corrected v3 candidate and completed full-suite result are recorded above. Independent auditor v3 accepted the two observer defect corrections for the bounded offline milestone after 87 independently run observer and SDK-interface tests; the review is in `spread-shadow-runs/hood-hcr43-low-latency-20260923/auditor-v3/`. This does not accept the full latency objective or authorize collection, trading, activation, main integration or publication. Source-to-receiver live improvement, WS superiority, stream admission equivalence and improved mutual matching remain unproved.

## HCR-42 selected-reserve release — active controller

The owner selected an exact per-account opening reserve of 0.10 quote at planning and 0.02 quote at fresh dispatch after fees and adverse entry loss. The independently accepted offline HCR-42 v2 candidate is `494f5815b5a38ca15505c79eb5777da5c70c8f5a`; the auditor's v2 packet is `spread-shadow-runs/hood-hcr42-provenance-20260923/auditor-v2/`. Chief bound the values through the terminal/Telegram shared operator parser and child configuration, planning and fresh budget. Focused relevant tests passed **301**. The final clean isolated Python 3.11.5 / lighter-sdk 1.1.2 full suite at code commit `12cba339d1bddafbcfdf2b30afdbbf2e94508cd7` passed **5162, 3 skipped, 2 dependency warnings** in 140.47 seconds; it also printed one pending-task teardown message in an unchanged paper-runtime test. On the owner's subsequent explicit restart request, the old controller was stopped after proving no trading child or operator-lock holder. Local main fast-forwarded to verified remote main `90cfbdc0b7e1783011af507e0ae8767e9659cadd`; the protected operator JSON gained only the selected reserve, and the saved controller state changed only its config-derived binding before startup. New controller PID 91534 holds the instance lock; operator lock is free, with no child observed. Startup's bounded read-only recovery marked the preserved active `run` intent READY at 2026-09-23 15:25:26 Moscow time, without replay, and cleared only that administrative barrier. This is a timestamped check, not a future flatness guarantee. Actual imports are from the runtime checkout with Python 3.11.5 / lighter-sdk 1.1.2. No agent `/run`, `/close`, order, cancellation or leverage write occurred. Restart evidence and exact before/after hashes are in `spread-shadow-runs/hood-hcr42-controller-restart-20260923/chief-v1/`; earlier release evidence remains in `spread-shadow-runs/hood-hcr42-reserve-release-20260923/chief-v1/`.

## HCR-42 v2 — accepted offline, not activated

The v1 candidate was returned by the independent auditor for one reproduced P2 diagnostic provenance defect: pre-setting numeric balance was labeled with a later setting-readback timestamp, and retries could similarly relabel original mark/book inputs. The preserved audit is in `spread-shadow-runs/hood-hcr42-margin-latency-20260923/auditor-v1/`; original v1 branch and Chief packet remain unchanged. A separate v2 correction branch retains original account/metadata/book identities and observation times with each initial numeric budget and labels later preparation references separately. It suppresses deltas when corresponding baseline provenance is missing or conflicting. No reserve arithmetic, transport, runtime or numeric policy change is part of v2. Focused adverse checks passed **12 tests** after the final source edit; the final clean detached Python 3.11.5 / lighter-sdk 1.1.2 suite at source commit `c7f03c9b13dc2c38c91651047b45f92e637b9d07` passed **5148 tests, 3 skipped, 2 dependency warnings** in 137.04 seconds. Independent auditor accepted v2 offline at commit `494f5815b5a38ca15505c79eb5777da5c70c8f5a`; evidence is in `spread-shadow-runs/hood-hcr42-provenance-20260923/chief-v2/` and `auditor-v2/`. It was not activated by that acceptance.

Owner cycles 051–062 on published HCR-41 produced nine cycles stopped before the first order by fresh margin revalidation, plus one such refusal after a zero-fill retry. Five source LIMIT orders in the remaining cycles yielded four proved zero-fill cancellations after lost price priority and one external fill. The only receiver also filled externally; own reciprocal matched quantity was zero. Historical flat terminal states in 052/053/059 are not current account evidence. The independent read-only review and verified 27-input manifest are in `spread-shadow-runs/hood-postrelease-proposal-review-20260923/auditor-v1/`.

Chief completed a separate clean HCR-42 branch from published main. That historical candidate added both-leg fresh margin diagnostics and an explicitly parameterized quote-currency reserve mechanism across sizing, leverage selection and final admission. Its numeric reserve was then unselected and inactive; the owner selected it in the later release assignment above. A reproducible five-attempt timing report corrects the older mistaken public-visibility interpretation. Existing reads already overlap; no safely removable wait or equivalent Robinhood stream proof was established, so no transport change or live speed/fill improvement is claimed. The focused relevant suite passed **311 tests**. The final clean detached Python 3.11.5 / lighter-sdk 1.1.2 full suite at source commit `0bde5403d48d7b724935a45550364ab5dded1c62` passed **5145 tests, 3 skipped, 2 dependency warnings** in 140.26 seconds. Its checked source and test files are unchanged by this documentation-only status update. No agent live order, cancellation, account read or runtime change occurred. The owner-only HCR-42 packet is `spread-shadow-runs/hood-hcr42-margin-latency-20260923/chief-v1/`.

## HCR-41 — accepted, published and active for owner-triggered runs

The independently accepted offline code commit `22296ebfeedd6e382321270e1685f6a861b3cc81` is in local and remote main through the fast-forward integration `97692e16c54f544dde8436d86570cad97823b2a8`. The previously idle Telegram controller was stopped after proving no active child or operator lock, and restarted from this checkout as PID 66692. Its instance lock is held, operator lock is free, and saved state has no active operation. The release packet at `spread-shadow-runs/hood-hcr41-release-20260923/chief-v1/` records exact identities, checks and the final publication. No agent live order, cancellation, leverage write, `/run` or `/close` occurred.

HCR-41 corrects residual-close evidence deadlines, independent account progress after proved local preparation failure, supported terminal margin-cancellation reporting, private/public book diagnostics, mark-based two-account opening budgets and bounded ambiguous-setting reconciliation. Pre-transport leverage cancellation records NOT_SENT; after possible transport the result remains UNKNOWN without replay. Recovery relies on sequencer soft finality, not L1 finality. The clean isolated Python 3.11.5/lighter-sdk 1.1.2 suite passed **5136 tests, 3 skipped** in 145.76 seconds; independent D4 regressions passed 111 tests. Earlier v6 work and the independent review packet remain preserved. Venue-specific reserve/admission behavior and live reciprocal fill performance remain unproved; no guaranteed-fill claim follows from offline acceptance.

The independent read-only review verified all 13 source-manifest files for cycles046–050 and HCR-40's full-suite evidence. It reproduced a missing fallback freshness deadline, near-zero modeled margin headroom, terminal-status drift causing false UNKNOWN in cycle050's report, and a historical leverage barrier without later resolution. Additional focused checks passed 140 tests. Evidence: `/Users/daniilmakarov/Desktop/RISEx Spread Shadow/spread-shadow-runs/hood-chief-review-20260923/solo-v1/`. No original journal or production setting changed.

The owner's later sample has eight source attempts, one receiver submission and zero own reciprocal fills. Three cases prove lost price priority; three lack the exact public source level despite private open-state evidence. Five residual closes in 047/050 include three slippage cancellations before cleanup. Raw final reconciliation shows flat inventory for those completed cycles; the older HCR-40 cycle050 report incorrectly downgraded it because its terminal set omitted canceled-margin-not-allowed. HCR-41 corrects that classification without rewriting the original journal. cycle048 stays launch-only/incomplete. These historical facts do not establish current positions or successful normal paired closure.

## HCR-40 — accepted and deployed

The implementation `ca10b80d7811e49d842a884099169faef451c805` is integrated into local and remote main. The same agent implemented, tested and self-reviewed it; no independent review is claimed. A clean isolated Python 3.11.5 suite passed **5074 tests, 3 skipped**. A focused hood run passed 833 tests before the final sizing-bound refinement; the final full run covers that refinement. The first dirty-checkout full run had 16 failures: ten tests require a clean Git checkout, two reflected old margin expectations and were corrected, and four frozen S3 fixture tests depended on host monotonic uptime. No unrelated frozen code was changed or failures skipped. The final clean run passed in full.

The 2026-09-23 cycle-038…045 audit found a local cross-margin admission error: already reserved `cross_initial_margin_requirement` was compared with remaining free `available_balance`. This blocked closing/recovery before order dispatch in 039/040/041/045 and preflight in 042/043. Removing that false comparison does not alter identity, active-order, reduce-only, venue minimum exception, price or reconciliation gates. cycle-044 independently showed two reconciled zero-fill close attempts before a filled third and confirmed flatness. The precise content of the owner's failed `/close` commands is unavailable; the same false check could refuse admission before a close slot, and at 07:25 Moscow the Telegram controller was also absent. Both are reported as separate findings, not collapsed into a single historical claim.

At 07:23 Moscow a read-only public venue observation showed both configured BTC accounts flat: account 27331 reported 41.66% initial margin fraction (about 2.4x), account 27337 100.00% (1x). This was a timestamped observation, not a current-position guarantee. The new cycle draws one exact BTC quantity with paired notional capped by the smaller fresh available balance times four and any stricter venue leverage limit. It then selects each account's lowest modeled supported 1x–4x leverage, configures changed settings once and requires exact fresh readback before an order. Unclear setting outcomes block new `/run` without blind resend; `/close` can still reduce proved inventory. HOLD is 20–180 seconds. Venue fee/risk admission remains authoritative; no live setting/order/closure was performed by the agent.

All **33 original handoff input files** retain their recorded SHA-256 and sizes. Evidence is in `spread-shadow-runs/hood-margin-leverage-20260923/solo-v1/`: `audit-report.md`, `input-verification.json`, `read-only-observation.json`, `full-suite.log`, `full-suite-result.json` and deployment records. The desktop runtime imports its own source, Python 3.11.5 and lighter-sdk 1.1.2; all 20 hood package files match the tested checkout. At activation the old controller was absent, no trading child or operator lock holder was present, and a new idle controller started without clearing state or launching an operation. Remote main was verified at the implementation commit before the documentation status update.

Owner-triggered live validation is **NOT_RUN**. Any new trading campaign, hypothesis or real agent action needs a new owner decision. Historical UNKNOWN runs remain immutable and are not reclassified by this release.

## HCR-39 — accepted and deployed

Tested implementation: `dd03c9144ec217bb8103133b29d76eec50835cc0`, based on HCR-38 `f4a976ece6bd85f5d706d98533ae9c196ac8776b`. Integrated into main; final remote publication is recorded in `publication.json`. The same agent implemented, tested and self-reviewed the full actual diff; no independent review is claimed.

Recovery now submits exact on-grid BTC market 1 residuals below ordinary opening minimums using reduce-only MARKET/IOC. Read-only venue history confirmed fully filled initial quantities 0.00004 and 0.00007 BTC on orders 844424841898572 and 844424841891552, despite catalog minimums 0.00020 BTC / 10 quote. The exception is restricted to full-residual closure on this venue/market and journaled per attempt. Opening, paired, other-market and grid checks remain; no upward sizing or price widening.

Previously observed source order IDs survive temporarily missing lookups even when the accepted response omitted the ID. Opening and closing reconcile against that retained identity; conflicts/missing history still block dependent writes. Recovery account reads retry only transport/time-out failures within existing bounds, preserving simultaneous identity failures. Delayed pre-send reads require fresh quotes. Terminal reports preserve closure refusals before the first intent; old opaque errors explicitly lack a precise saved cause. Unknown remaining positions no longer carry recycled preflight timestamps.

Final clean isolated Python 3.11.5 suite: **5064 passed, 3 skipped**, exit 0. Targeted/integration checks: **517 passed**. The 34 new regressions cover identity retention in both phases/directions, missing/conflicting receipts, tiny long/short residuals, partial/zero fills, exact grids/quantities, other markets, rejects/ambiguous sends, bounded read recovery, cancellation/deadlines, identity conflicts, expired quotes and diagnostics. Existing credential, funding, no-replay and exact-flat checks passed. Twelve relevant counterexamples fail on the accepted base and pass on the candidate. All 90 selected original files retain their hashes. Saved Telegram HTML for available selected cycles and four close results is valid and below 4096 characters.

Earlier verification is preserved: initial new-test run 21 passed/9 failed due to test assertion errors (inventory string shape and a never-reached injected read); corrected run 322 passed. Integration initially had 191 passed/1 failed because retaining the source ID adds a read of that original ID. Its assertion now checks the intended read plus UNKNOWN, original identity, no cancellation and no pair retry. No failure was skipped.

Idle controller PID 18778 was replaced by PID 23784. Activation verified no active child, null active intent, both locks, clean accepted root, production imports, Python 3.11, SDK 1.1.2 and all package hashes against the tested checkout. After activation the instance lock was held and operator lock available. State/journals were not reset. The agent issued no /run, /close, real order or cancellation.

Evidence: `spread-shadow-runs/hood-close-reliability-20260922/solo-v1/`: `audit-report.md`, `saved-run-readback.json`, `read-only-diagnostic.json`, input manifests, `self-review.json`, `release-before.json`, `release-result.json`, `release-suite.log`, `deployment-*.json`, `publication.json`.

## Findings from selected runs

- close-001/002 refused -0.00004 BTC on 27331. close-003 closed 27331's +0.00020 but refused 27337's -0.00007. These were local minimum refusals; no corresponding order reached the venue. close-004 closed -0.00024 on 27331 and proved both accounts flat.
- cycle-032 ended flat after three zero-fill opening attempts. cycle-033 lost an already observed source ID when a lookup disappeared; no trade history was requested by that known ID. It left an observed -0.00024 source position with UNKNOWN execution.
- cycle-034 source 27331 BUY 0.00020 matched external account 26085; receiver unsent. Fallback 1 filled zero (canceled-too-much-slippage); fallback 2 closed the residual. Gross execution PnL -0.002580; fees/net unknown.
- cycle-035 source 27337 SELL 0.00020 matched external account 21432; receiver unsent. The next account check stopped closure before any fallback intent. Its generic `contract_error` cannot distinguish transient transport from a local state refusal. Historical final inventory remains UNKNOWN.
- 032/033 identify Telegram; 034/035 identify terminal. In 034/035 source visibility took 1.696/1.764 s, submission responses 0.297/0.305 s and signing roughly 0.002 s. These overlapping local intervals are not a controlled channel benchmark. Each separate close in 034 took about 2.97/2.98 s from intent to final evidence, including reconciliation.
- Bounded comparison 024…031: 024 has no cycle journal; 030 lacks a terminal cycle result. Neither proves success. In 027 a partial close left 0.00004 BTC, explaining later /close refusals. Other comparison outcomes remain in the offline readback.

## Remaining limits

Read-only observations at **2026-09-22 23:27:11 Moscow time** showed both accounts flat with no active selected-market orders. This is historical, not a current-position guarantee.

The owner requested agent live tests, but higher-priority tool-use restrictions prohibit conducting real financial transactions. Candidate live execution is **NOT_RUN**; owner-triggered validation remains necessary. Simulated 0.00001 BTC closure establishes code behavior only; actual below-minimum venue acceptance was observed for 0.00004/0.00007, and venue rejections remain authoritative.

The public book cannot reserve counterparties; separate residual closure uses public liquidity. This correction does not establish faster live matching or exclude outside fills. Missing fees/unverified units leave net PnL and funding unknown. One LIMIT consumed by several MARKET fragments and all frozen research remain outside this task.
