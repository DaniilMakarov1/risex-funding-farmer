# Current status

## HCR-38 — accepted and deployed

Tested implementation: `db2e502ccd59270347dbaa9e280fbeae228f4a0c`, based on accepted HCR-37 `a006abf8afeaac9218a4da74aff9eb3b75fca368`. Integrated into main and verified on origin/main. The same agent implemented, tested and self-reviewed the full diff; no independent review is claimed.

Final reports now identify proved external source fills even when the receiver was unsent or its IOC filled zero. Opening/closing messages retain external account IDs, own/external/unproved quantities and explicit unattempted phases. Full strategy failure remains separate from confirmed residual closure and missing fees. Counterparty identities must be valid integers; malformed boolean/float values cannot prove either own or external matching.

Only the public book is refreshed when initial independent account observations already identify the exact resting source and unchanged receiver. Original timestamps and the final exact-source/freshness checks remain mandatory. Missing account evidence still requires the full refresh; contradictions remain sticky. Telegram's hidden child disables discarded terminal progress rendering; its separate controller still publishes notices. New per-component timings and an allowlisted interface label support later comparisons. No transport, timing threshold, size, route, hold, price, minimum or fee policy changed.

Final clean isolated Python 3.11.5 suite: **5030 passed, 3 skipped**, exit 0. Adverse checks include source-only external, receiver-only external, reciprocal own, mixed, zero and malformed/unknown execution; stale retained accounts, wrong public owner and disappearing source on opening/closing; unchanged no-replay/credential/funding/exact-flat checks. All 24 selected original input files retain their hashes. Short/full Telegram HTML is valid and below the 4096-character limit for all five audited reports. Evidence: `spread-shadow-runs/hood-report-latency-20260922/solo-v1/` (`audit-report.md`, `self-review.json`, `final-readback.json`, `release-suite-2-result.json`, `release-suite-2.log`).

Earlier verification is preserved honestly: focused first pass 456 passed/2 failed (missing timing projection keys and an obsolete PARTIAL wording assertion), corrected affected pass 271 passed, first isolated suite 5028 passed/3 skipped. Self-review then found numeric equality could accept malformed boolean/float counterparty identities; its correction passed 85 focused checks and the final suite above. Initial dynamic audit outputs accidentally included owner-created unfinished slots; only `selected-inputs-before.json` and the fixed cycle-019…023 readback support this audit.

The idle previous controller PID 10502 was replaced by PID 18778. Activation checked the old process identity, no active child, both exclusive locks, clean Git state, actual production imports, pinned SDK 1.1.2 and all deployed package hashes against the tested checkout. After activation the instance lock was held, the operator lock available and durable active intent null; state was never reset. No fresh private reconciliation was needed, so this deployment makes no current-position claim. The agent sent/canceled no real orders and invoked neither /run nor /close. Details: `deployment-*.json`.

## Findings from the selected cycles

- Cycle-020: external account 10196 filled source LIMIT 0.00020 BTC before receiver dispatch. Cycle-022: external account 20245 filled source LIMIT 0.00021 BTC; our IOC filled zero. Comparison cycle-019 similarly matched external account 22437 for 0.00024 BTC with our IOC zero.
- Cycle-021: opening 0.00025 BTC matched our two accounts; on closing, attempt 1 lost priority and attempt 2 source matched external account 26085 while our IOC filled zero. All selected cycles eventually prove flatness through saved terminal histories; that does not make the paired strategy successful.
- Cycle-023: three terminal zero-fill attempts. In the first two propagation refreshes, account evidence already contained the source and only the public level was absent: the candidate removes four redundant account/order HTTP reads per eligible refresh. Attempt 3 still lacked source-account evidence and needs the full refresh. The original refresh windows were 0.479/0.436/0.385 s; none is a proved live time saving.
- Controller state positively attributes cycle-021 to Telegram. Other selected cycles lack an explicit interface label, so they are not a controlled channel benchmark. LIMIT-send to MARKET-send intervals were 0.792225 s for cycle-021 opening, 0.908964 s for its closing and 0.746840 s for cycle-022 opening. Source responses plus coalesced private/public reads dominate; final checks and small local work follow. Both phases sign before exposure and use the same admission path. Telegram-specific slowness is unproved.

## Remaining limits

The public book cannot reserve counterparties. External fills remain possible after observation; normal separate residual MARKET closure uses public liquidity. Offline correctness and fewer HTTP requests do not establish a faster live cycle or a higher own-match rate. The next owner-requested run can evaluate the added measurements; no autonomous run is assigned. A stream-based optimization requires equivalent Robinhood account/order identity, freshness and continuity evidence before changing transport.

Fee completeness remains separate from execution. Missing fees and unverified nonzero venue integer units remain unknown; net PnL and funding cannot be invented. Saved flatness is historical, not a new account check. One standing LIMIT consumed by several MARKET fragments remains deferred and each fragment must meet venue minima. Frozen research/scanner/Funding Farmer/old Telegram/testnet modules remain outside this task.
