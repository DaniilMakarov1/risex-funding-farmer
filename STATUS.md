# Current status

## Accepted baseline and ownership

- Current accepted implementation source before this governance record is `4f83f8dea9f7a5deea4902f0c5cc6443e28004c1`; it is published on `main`.
- Transition preflight on `2026-09-02` independently verified local `main`, local `origin/main`, and GitHub `refs/heads/main` at `554b6c9c5e2b60eb13c9f33c6c2184e6932c84f0` (`Refine conservative PAPER economics gate`). That SHA remains the historical transition baseline; later accepted work is recorded below.
- The active project root is `/Users/daniilmakarov/Desktop/RISEx Spread Shadow`, created as a clean checkout of the existing `DaniilMakarov1/risex-funding-farmer` repository. This is not a new GitHub repository.
- The historical checkout `/Users/daniilmakarov/Desktop/risex-funding-farmer` contains a pre-existing modified `README.md`, six untracked operational artifacts, and old dirty worktree residues. They were not changed, accepted, or used as a base. Old unaccepted candidates are abandoned/rejected residue.
- One Chief owns `main`, governance, acceptance, push, and operational decisions. No old Chief or Builder task is active.
- During the `2026-09-03` acceptance preflight, one frozen legacy PAPER process was found under launchd label `com.risex.paper.chief49`, still holding its historical database. The Chief stopped it with `launchctl bootout` and disabled the label. The database and plist were preserved; subsequent process, label, and open-handle checks were clear. No unfinished venue write intent was found and no operational run is authorized.

## Product state

- RISEx Funding Farmer remains in the repository as a frozen legacy benchmark. Its funding-boundary profitability path and all old PAPER/testnet/mainnet operational work are frozen/replaced.
- RISEx Spread Shadow is the active public-only research contour.
- Active product question: does a repeatable positive fee-adjusted execution edge exist when each hypothetical RISEx maker fill is evaluated against delayed executable exact-q Lighter Standard taker hedge books?
- Primary horizons are `0/300/500/1000 ms`; the whole curve is reported. `500 ms` is diagnostic, not presumed actual end-to-end latency.
- Points equal `$0`. Funding is diagnostic and separate. Future maker fills, maker exits, and basis convergence are not recognized as earned entry income.
- Private endpoints, credentials, signing, order preparation, dispatch, testnet/mainnet writes, real funds, transfers, withdrawals, and strategy execution remain prohibited.

## Current implementation state

- SS-001A is independently accepted at `f7251387f0ffe684ed5f3d61f4ea4601b6156183` and is part of `main`. It contains only the pure `risex_spread_shadow` economics/evidence contracts and focused tests.
- Chief acceptance evidence on the exact committed tree: 35 focused tests passed; an isolated Python 3.11 full suite passed `3694` tests with `3` skipped; dependency, compile, import/surface, diff, scope, and Git checks passed. Production size is `2289` lines, within the frozen `2300`-line upper bound.
- SS-001B is independently accepted and published at `9ac7b73941b9f0217cfa6a2ef68b21d6040fd015`. Final evidence includes `54` focused tests, an isolated Python 3.11 full suite of `3713 passed, 3 skipped`, clean dependency/import/diff/Git checks, and `2696` new production lines against the `3200` cap.
- The Chief's exact-SHA public-only BTC smoke used fresh run `5AuNTOVaN842jMZZ0Oss87ov`: `95` RISEx books, `254` Lighter books, `3` RISEx trades, `8352` active quote records, no fatal or real transport/schema gap, deterministic offline report, clean shutdown, and owner-only `0700/0600` storage. No natural strict fill occurred in that 15-second acceptance smoke; deterministic tests prove the event-driven four-horizon path without lookahead.
- External governance review accepted the transition. Before any SS-001A candidate acceptance, target-margin formulas, deterministic quantity sizing, conservative/optimistic fillability interpretation, exact hedge-failure outcomes, and the bounded SS-001A/SS-001B complexity limits were frozen in System Specification 2.1.
- The prospective real-public discovery gate `DG-001` is frozen in System Specification 2.3 before any discovery sample. It fixes the `BTC/ETH/SOL` universe, `25 s` freshness, fee inputs/provenance, numeric fillability and edge thresholds, completeness/fatal rules, a `60 s`/`250,000`-record/first-`50`-strict-evidence bound, and exact seven-verdict precedence.
- `DG-001` ran once on exact measurement source `9ac7b73941b9f0217cfa6a2ef68b21d6040fd015` with fresh run `RNLvQGjXjcT8xkAD48OoVq6h` and exact `BTC/ETH/SOL` admission. Its owner-only store retained `54,493` records (`253,441,888` bytes, SHA-256 `507f15f52926dabf77befa8bb8116d77944edb0bbe6468a795fffb8685438d6c`) below the `250,000`-record cap.
- The run recorded `52,176` active quotes, `2,195` accepted books, `6` RISEx trades, `114` explicit gaps, `0` strict would-fills, and `0` horizons. It exceeded the frozen duration without a clean `RUN_STOP`; the exact hung process was terminated after its store stopped advancing. The offline report was deterministic (SHA-256 `786b9630b66f53d80fabc362eb60ce599695fcccf0c8f2a09c8059b3342ffd4a`), with all `288` groups degraded.
- Under the predeclared first-precedence rule, the immutable `DG-001` verdict is exactly `DATA_INSUFFICIENT`, classified as `DG-001 MEASUREMENT PIPELINE FAILURE / DATA_INSUFFICIENT`, not an economic or strategy-failure verdict. Quoteable snapshot evidence existed, but missing clean termination and repeated public-frame/socket gaps prevent any fillability or delayed-edge conclusion.
- The `2026-09-03` read-only postmortem proved three bounded measurement defects: RISEx `PING` handling falls through to `RISEX_PUBLIC_FRAME_INVALID`; closing an empty ingress does not wake a consumer already blocked on `queue.get()`; and the offline report globally degrades a market for any non-terminal market gap while also requiring at least one strict fill for `COMPLETE`. The evidence contains no `QUEUE_OVERFLOW`, checksum/sequence-recovery, planned-stop, history-capacity, or store-failure gap. Heavy persistence is observed (`253,441,888` bytes; full books plus `52,176` quote rows; synchronous fsync per observer append), but it is not proven to have caused or amplified the recorded gaps or hang, so storage redesign is not authorized.
- SS-001C is independently accepted and published at `b4f2822327fc0f7b50a02d7aabfc2d6e61b453a4`. It corrects only the proven RISEx PING fall-through, ingress close/wake/drain, bounded terminal shutdown, and overlap-aware report completeness defects. Chief evidence on the exact SHA: `65` focused tests; isolated Python 3.11 full suite `3724 passed, 3 skipped`; clean dependency, compile, import, private/write-surface, diff, scope, and Git checks.
- SS-001D is independently accepted and published at `10cc7be7b58c536fc8edf65309b13e9a9d8d819b`. It adds the explicitly labelled at-or-through optimistic upper bound beside the unchanged strict lower bound, model-separated horizons, unique eligible-trade and first-stop counters, a frozen-sample/Lighter-horizon drain, silent-market wall stop, deterministic bounded reporting, and enforced `2,500,000`-record/`12 GiB` evidence caps without changing JSONL representation. Chief evidence on the exact SHA: `56` targeted tests; isolated Python 3.11 full suite `3751 passed, 3 skipped`; dependency, compile, import, private/write-surface, diff, scope, Git, and immutable DG-002B replay checks passed. DG-002B still reports `96,717` records, `288` strict-complete groups, zero strict fills, and optimistic `NOT_IMPLEMENTED`.
- Prospective public-only `DG-002A` passed on exact accepted source with fresh run `YuKfhtuQBGntdCxNUM91n8pv`: exact `BTC/ETH/SOL`, configured `60 s`, unassisted return in `65.46 s`, `112,055` records below cap, `4,484` books, `107,544` quotes, `18` RISEx trades, only six terminal `PUBLIC_SMOKE_STOPPED` gaps, exactly one clean `RUN_STOP`, no `RUN_FAILED` or fatal, and all `288` report groups `COMPLETE`. The owner-only `0700/0600` store is `491,837,573` bytes with SHA-256 `abd291bcff37fc5a3110bee3c75d25d45f99c402f621b90e12cdaf7fa9ea97e1`; two canonical reports agreed at SHA-256 `1acd6cc085a725c6aaf0075d09f83f3bb91655bb217d448cf9e2cb76dce5186c`. Its zero-fill observation is stability-only and did not select the next gate.
- Prospective public-only `DG-002B` completed on exact accepted source with fresh run `mjWY59nkjlLzRzghrGOVkyuX`: exact `BTC/ETH/SOL`, configured `60 s`, unassisted return in `65.35 s`, `96,717` records below cap, `3,871` books, `92,808` quotes, `29` RISEx trades, only six terminal `PUBLIC_SMOKE_STOPPED` gaps, exactly one clean `RUN_STOP`, no failure/fatal condition, and all `288` report groups `COMPLETE`. Every policy had quoteable-time share `1` with `1,070–1,501` active quote observations, but the run produced `0` strict would-fills and `0` horizons; the optimistic model remains `NOT_IMPLEMENTED`. The owner-only `0700/0600` store is `445,382,939` bytes with SHA-256 `9ae2050a362b5e23b308b16db3c6b238277b561e4546ef7f7505141f66cab861`; two canonical reports agreed at SHA-256 `17612b7e73d93b2f6b5162aa0195f9618be9f5f6fe1f3f60204b775505e3fd18`.
- Under the unchanged seven-verdict precedence, the terminal Entry Viability verdict is `FILLABILITY_INSUFFICIENT_EVIDENCE`: snapshot edge was materially present and the corrected evidence was complete, but strict fillability was below the materially-positive threshold and no optimistic model exists. This neither proves a viable repeatable entry edge nor proves unfillability. `SS-002` and `SS-003` remain closed.
- SS-001A candidate `a3ac545a78a687788595470c1e1e1e91a501ec74` was independently rejected and was not merged. Its green tests did not cover fail-open missing economics, invalid cross-clock exchange-monotonic ordering, missing explicit would-fill detection time, or recovery-generation displacement. The original Builder is released; correction requires a fresh visible Spread Builder from current accepted `main`.
- Correction candidate `7b3e370865de28bc4a4566445c676772dc0727cc` was also independently rejected and not merged. It failed a required repeating-decimal multi-level exact-q case, accepted sizing evidence with the wrong direction/target notional, and lacked venue identity on gap evidence. System Specification 2.2 freezes these adverse contracts before the next fresh candidate.
- The rejected candidates remain historical non-authoritative evidence and were not merged. `SS-002` and `SS-003` remain closed.
- The first SS-001B Builder attempt from `2d67811757994deef8523ae384c5e09cd1b2502f` was formally rejected and released without a commit. Its unaccepted worktree duplicated implementation paths, reached about 4,600 new production lines, and produced no final tests or smoke evidence; it remains non-authoritative history.

## Completed prior Chief mission

- The measurement defects are independently corrected and accepted, `DG-002A` passed, and the separately frozen `DG-002B` produced a valid non-integrity-failure product verdict.
- The Entry Viability Stage is complete with `FILLABILITY_INSUFFICIENT_EVIDENCE`. That historical mission is closed; the separately defined fillability-resolution mission remains active. `SS-002` and `SS-003` remain closed.

## Current Chief mission

- Resolve RISEx–Lighter fillability uncertainty: determine with prospectively valid public evidence whether profitable hedge-anchored RISEx maker quotes are genuinely unfillable or whether the current zero-fill result is caused by the conservative strict would-fill model; do not open `SS-002` until fillability is resolved and delayed hedge evidence exists.
- Read-only reconstruction of all `29` immutable DG-002B trades found `348` correct-side active quote interactions. Five trade events reached at least one quote; four traded exactly at a quote; one ETH event traded one tick through three quote policies. That is `22` at-or-through interactions (`19` exact-at, `3` through/strict-price), but no strict or optimistic cumulative quantity reached exact `q`. Twenty-four trades reached no quote. Quote-to-post-only-bound distance across interactions had median `262 ticks / 3.920 bps` and p95 `538 ticks / 7.365 bps`; touches were concentrated in RISEx-sell/Lighter-buy ETH policies.
- The short evidence distinguishes neither practical unreachability nor strict-model conservatism. The required explicit at-or-through optimistic upper bound is now accepted alongside the unchanged strict lower bound.
- DG-002B volume was `223 MiB` BOOK plus `202 MiB` QUOTE at about `7.42 MB/s`. With `527 GiB` free at preflight, the projected bounded capture is supportable without changing storage representation; long-run reporting is memory-bounded.
- The one frozen DG-003 run `52CoS0twvF0wTwMPf9ZXgP-E` on exact source `10cc7be7b58c536fc8edf65309b13e9a9d8d819b` is immutable `DATA_INSUFFICIENT / MEASUREMENT_THROUGHPUT_FAILURE`. It stopped prospectively on `STRICT_EPISODE_LIMIT` with diagnostic counters `53` strict, `63` optimistic, and `114` unique eligible trades, then ended with one `RUN_FAILED` (`TimeoutError`, `INGRESS_DRAIN_TIMEOUT`) and no clean `RUN_STOP`. Its owner-only store has `684,092` records, `4,249,767,593` bytes, SHA-256 `5438beb45be93ed96056a5f7027488e055d0090da33f5068e822922618010644`; two canonical reports agreed at SHA-256 `f9f191eb2c4872ba88688f2ce3613d309a0b50c7c5a504c780b4ab8698ac72e7` with all `288` rows per model degraded.
- The failure evidence contains `643,392` quote rows, `39,877` books, all expected `464` model-scoped horizon rows, `96` queue-overflow gaps (`73` Lighter, `23` RISEx), and `27` unexpected socket-disconnect gaps. This proves public evidence throughput/backpressure and terminal drain are not reliable for the long sample; it does not support an economic verdict. SS-001E is the only authorized correction. No replacement discovery gate is frozen.
- The first SS-001E candidate was formally rejected before commit or acceptance after three delayed task registrations and the replacement Builder concurrently edited the same `runner.py`, `store.py`, and focused-test worktree. All four involved tasks were stopped and released. The combined uncommitted tree is preserved only as non-authoritative rejected residue on `codex/spread-ss-001e-rejected-concurrent`; none of it may be reused or reviewed as a candidate. SS-001E correction must restart with one fresh visible Builder and branch from exact accepted `main`.
- The fresh post-rejection SS-001E candidate is independently accepted and published at `cd741e2a46e874f1e77feebac2aba5c80a96455d`. It activates the existing `128`-record/`0.25 s` lossless observer batching contract without changing the store format, queue size, timeout, economics, fill models, stop logic, horizons, or caps. The deterministic load test processes `39,877` three-market events and `717,786` evidence-row equivalents at a fixed offered rate far above the observed DG-003 rate, without capacity feedback, overflow, unbounded pending evidence, ordering loss, or drain failure. Chief verification passed focused/adverse tests, deterministic DG-002B report SHA-256 `fbffb04e4e8d1fabf5a111b25a5d8c8e30678a572d7c1cf33fac7bcff674c538`, deterministic DG-003 report SHA-256 `6f1a92aafbb6bfbc16547e1192e66da8c353ff5f271185a7c3189f2218307ec6`, and an isolated Python 3.11 full suite of `3756 passed, 3 skipped`; dependency, compile, import, scope, private/write-surface, diff, and Git checks were clean.
- `DG-004 — Fillability Bounds Recovery Discovery` was prospectively frozen in System Specification 2.9 on exact measurement source `cd741e2a46e874f1e77feebac2aba5c80a96455d` and opened exactly once. `SS-002` and `SS-003` remain closed.
- The one DG-004 run `LR5YlVKd9zNseK3TuiyitD3j` is immutable `DATA_INSUFFICIENT / TERMINAL_SERIALIZATION_FAILURE`. It stopped after about six minutes on `LIGHTER_PUBLIC_FRAME_INVALID`, then raised `INGRESS_DRAIN_TIMEOUT`. Its owner-only store has `166,291` physical records, `727,561,746` bytes, and SHA-256 `77f795be487224634e806e3b7c546de8c4378b2c98334f21f59c623ba3ecebfa`; it contains `49` eligible trades, `24` strict episodes, `57` optimistic episodes, and all `324` diagnostic horizons, but none is economic verdict evidence.
- DG-004 exposed a separate terminal-integrity defect: a cancelled drain left an in-flight thread-backed append overlapping the direct `RUN_FAILED` write. Physical rows after the terminal reused/out-of-order record indices `166287..166289`, the terminal was not last, and the fatal protocol gap was absent. Existing reporting did not explicitly reject this corruption. The exact unsupported Lighter frame kind was not retained, so its protocol meaning is not yet classifiable.
- `SS-001F — Terminal Serialization and Protocol-Failure Evidence` is the only authorized next correction. It is limited to append/terminal serialization, explicit corrupt-replay rejection, and bounded sanitized protocol-failure evidence that survives full/closing ingress. It may not change economics, fills, stop rules, queue capacity, shutdown timeout, storage representation, protocol acceptance, venues, or strategy. No replacement discovery gate is frozen.
- SS-001F is independently accepted and published at `cdbc95c67adaf9df120c3ff07bb990dc37542ae3`. The final candidate serializes append/terminal operations, waits for thread-backed write quiescence under cancellation, preserves a single physically-last terminal, rejects corrupt physical replay explicitly, and retains bounded sanitized protocol-frame evidence without changing queue size, timeout, storage representation, economics, fills, or protocol acceptance. Chief evidence: `108` focused/surface tests; isolated Python 3.11 full suite `3767 passed, 3 skipped`; deterministic DG-002B and DG-003 reports; DG-004 rejected as `DECREASING_RECORD_INDEX`; dependency, compile, import, private/write-surface, diff, scope, and Git checks clean.
- Official Lighter WebSocket material specifies JSON messages and the current official Python SDK parses asynchronous messages as JSON with JSON application ping/pong. A separate bounded public `600 s` transport diagnostic observed `34,309` text frames and four ordinary CLOSE frames across five connections, with no binary, continuation, or other unsupported frame. The historical DG-004 frame kind cannot be reconstructed, so unknown frames remain fatal; SS-001F will classify any recurrence rather than silently accepting it.
- `DG-005 — Fillability Bounds Integrity Recovery Discovery` is prospectively frozen in System Specification 3.1 on exact source `cdbc95c67adaf9df120c3ff07bb990dc37542ae3`. No sample has been opened. `SS-002` and `SS-003` remain closed.
- The one DG-005 run `Aahs_oqMeLcvUpsiqKQv1Fks` is immutable `DATA_INSUFFICIENT / MEASUREMENT_THROUGHPUT_FAILURE`. It ended cleanly on `WALL_CLOCK_LIMIT` with contiguous indices and one final `RUN_STOP`, but recorded `1,117` gaps: `921` Lighter and `166` RISEx `QUEUE_OVERFLOW`, `27` unexpected Lighter disconnects, and three planned terminal gaps. Its `40` eligible trades, `17` strict episodes, `22` optimistic episodes, and `156` horizons are diagnostic only; all `288` policy/horizon groups per model are degraded.
- The retained owner-only store has `130,877` records, `4,986,247,063` bytes, SHA-256 `5de295aaf7b8b7f63c71a518e3da3e718c89b154faa1efc4143735c61a8c3611`; two reports agreed at SHA-256 `a9721dc76ad96286bed1e97af7bd7db40168983844e96cab2c1e1e15ba54555f`. Full BOOK rows consumed `4,831,137,744` bytes (`96.89%`). Lighter BOOK rows had median `74,410` bytes and repeated median depth of `1,081` bids plus `751` asks per revision.
- DG-005 therefore proves batching alone is insufficient and lossless book-state repetition is the current throughput bottleneck. `SS-001G — Lossless Book-Delta Evidence` is the only authorized next correction: one full normalized snapshot per identity chain, exact normalized deltas with predecessor/revision binding, exact quote/horizon revision references, and bounded deterministic reconstruction. Store/caps/queue/timeout/economics/fills/stops/protocol/venues remain unchanged. No replacement discovery gate is frozen.
- SS-001G is independently accepted and published at `4f83f8dea9f7a5deea4902f0c5cc6443e28004c1`. It adds full-plus-delta canonical BOOK chains, exact QUOTE/HEDGE_HORIZON revision witnesses, bounded reconstruction/history, legacy full-BOOK replay, and fail-closed chain/reference audit without changing economics, fill bounds, eligibility, queues, timeouts, caps, protocols, venues, or private/write surfaces. Chief verification passed `120` focused tests, a fresh isolated Python 3.11 full suite of `3780 passed, 3 skipped`, compile/import/dependency/scope checks, deterministic DG-002B/DG-003/DG-005 legacy replay, and explicit DG-004 rejection. The independent six-chain `1,900`-level stress persisted `720` ordered revisions in `920,592` bytes and completed end-to-end in `10.13 s`, at least `71` books/s including repeated offline reports, above DG-005's approximately `54.8` books/s.
- The one DG-006 run `KNuynAYhIMdXkAx38OXIaRri` on exact source `4f83f8dea9f7a5deea4902f0c5cc6443e28004c1` is immutable `DATA_INSUFFICIENT / EPISODE_LOCAL_COMPLETENESS_AND_STOP_MISMATCH`. It stopped cleanly on `STRICT_EPISODE_LIMIT` at `305` eligible trades, `56` strict episodes, `87` optimistic episodes, and all `572` horizons. Its owner-only store has `860,952` records, `2,485,056,934` bytes, SHA-256 `dc6f5a9727ef82a4905f402cbf79daf99cd8672628841a102f286199b4643eee`, contiguous indices, `15` full BOOK anchors, `34,827` deltas, and one final `RUN_STOP`; two reports agree at SHA-256 `c8f3d1d88422ba3e67b5babaf3fdec315b32bd8aaa12123c087cd03c537d5376`.
- DG-006 contains three ordinary Lighter CLOSE/reconnect rounds approximately `121 s` apart, recorded as nine market-scoped disconnect gaps, plus three planned stop gaps. The current report degrades a whole policy if any episode overlaps a gap. Episode-local read-only analysis finds `31` strict episodes with all four clean horizons and `25` overlapping a disconnect; the largest policy has `9` raw and `5` clean strict episodes, below the frozen `10`-episode material threshold. No valid entry-edge verdict can be issued, and the active mission is not complete.
- `SS-001H — Episode-Local Completeness and Material Stop` is the only authorized correction. It is limited to episode-local valid/contaminated reporting, graceful CLOSE versus unexpected transport-failure evidence, and replacing the future aggregate strict stop with the existing material-policy threshold of `10` valid all-horizon strict episodes across `5` detection timestamps. Economics, fills, eligibility, horizons, queues, caps, timeouts, venues, private/authenticated access, writes, `SS-002`, and `SS-003` remain unchanged. No replacement discovery gate is frozen.
- SS-001H is independently accepted and published at `6e03195fe1c45e076cbe4cd20a2a02b178cc40e1`. It preserves raw evidence while classifying valid/contaminated model episodes only after the fill interval and all four horizons are present and clean, keeps horizon contamination local, distinguishes explicit normal WebSocket close from abnormal/unknown transport failure, and stops future samples only at a stable ingress watermark on the frozen per-policy threshold. Chief verification passed `135` focused Spread tests and a fresh isolated Python 3.11 full suite of `3794 passed, 3 skipped`; dependencies, compile/import, scope, private/write-surface, diff, worktree, and Git checks were clean.
- Two independent canonical reports of immutable DG-006 on the accepted SS-001H source were byte-identical at `3,525,943` bytes and SHA-256 `728bbd704f20c617a25ad69246726127ebb7c71ae5ac8e6446364ebcd7b27c96`. They preserve strict raw `56` and optimistic raw `87`, and classify strict `31` valid / `25` contaminated and optimistic `58` valid / `29` contaminated, with all `572` raw horizons retained. DG-006 remains immutable `DATA_INSUFFICIENT / EPISODE_LOCAL_COMPLETENESS_AND_STOP_MISMATCH`; this corrected diagnostic does not change its verdict.
- `DG-007 — Fillability Bounds Resolution Discovery` is prospectively frozen in System Specification 0.17 on exact source `6e03195fe1c45e076cbe4cd20a2a02b178cc40e1`. No DG-007 sample has been opened. `SS-002` and `SS-003` remain closed; all private/authenticated/write activity remains prohibited.
- The one DG-007 run `TBP1G0fJZWW29zh0B7vZ2n9d` opened exactly once from the frozen source and ended cleanly on `STRICT_EPISODE_LIMIT`. Its owner-only store has `799,537` contiguous records, `2,309,058,701` bytes, SHA-256 `20a782f8838d69acdf5b0e3545a438a22a8f6ee185f2f851e9128782595ef19b`, `15` full BOOK anchors, `32,338` deltas, `79` raw strict episodes, `81` raw optimistic episodes, all `640` horizons, twelve gaps including three planned stops, and one physically-last `RUN_STOP`. The stop signal records `181` eligible trades and `10` valid strict episodes across `10` timestamps for `BTC|RISEX_SELL_LIGHTER_BUY|100|1`.
- Two reports are byte-identical at `3,503,620` bytes and SHA-256 `340dc899610b27eb8b23c5be3cff9530fdfffb5e80d806a975ef450faa991112`, but expose a report-only mismatch: they classify the stop policy as `1` valid / `9` contaminated and the run as strict `6` valid / `73` contaminated. The cause is proven: offline null-ended gap handling falsely marks evidence completed before a later same-session graceful CLOSE, while the accepted runtime correctly treats the gap as starting only at its recorded start. DG-007 is provisionally `DATA_INSUFFICIENT / ONLINE_OFFLINE_VALIDITY_MISMATCH`; no economic verdict has been issued and the sample will not be reopened or extended.
- `SS-001I — Open-Ended Gap Replay Parity` is the only authorized next correction. It is report-only and must make null-ended gap overlap exactly match the accepted runtime interval/identity semantics, with deterministic immutable DG-006/DG-007 replay and no changes to collection, economics, fills, stop logic, storage, caps, protocols, venues, private/authenticated access, writes, `SS-002`, or `SS-003`.
- The first SS-001I setup is formally rejected without a commit or test evidence: an asynchronous task left uncommitted changes before the separately visible Builder entered the same worktree, making authorship ambiguous. The visible task stopped without editing, testing, committing, or accepting those files; its worktree was released and none of that residue may be reused. SS-001I must restart from current accepted `main` in one fresh branch and visible Builder.
