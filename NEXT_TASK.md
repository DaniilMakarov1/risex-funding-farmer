# SCAN-002 — Bounded Offline Scanner Evaluation

Status: ACTIVE / ONE FRESH VISIBLE SPREAD BUILDER. Verification level A.

Objective: implement only a trustworthy offline scanner evaluation of the frozen two-arm public entry-edge question. Reuse the accepted evidence and arithmetic, with the smallest report extension; no new architecture. Base: exact published main containing this task, supplied by Chief at creation. Fresh codex/spread-scan-002-* worktree/branch; report root, branch, HEAD and clean status before edits.

Required outcome:

- Compute the section-0.19 numerical evidence/economic/selection contract with the prospective section-0.20 clarifications, from full final validity rather than raw horizon rows. Retain compact bounded trade/arm/episode/unit state, not every quote/book record. Missing capacity or required evidence fails explicitly. Reuse existing final gap semantics and book reconstruction. Historical core report fields/verdicts stay unchanged; the new scanner decision is separately labelled and legacy malformed/absent stage metadata cannot qualify.
- Build transitive maker/taker-order dependence units across all eligible trades, including cumulative-fill bridges. Both arms must have active context on the common eligible population. Collapse each arm/unit/horizon to its worst clean strict edge and episode-relative markout; preserve negatives and permit zero only for observed clean no-fill. Mixed/contaminated/inactive/unresolved units remain visible and fail admission instead of losing their bad members.
- Emit raw/clean/contaminated/inactive/unresolved population counts; per-arm filled units, venue clusters and detection timestamps; fixed unit-based concentration; actual same-event price/tick separation, collisions/distinct/reversed/unresolved pairing; all four exact-q full-hedge shares; exact edge/markout distributions, positive share, sum/mean/minimum and gross gains/losses; common-denominator 300ms selector and named failed thresholds. Scores are conditional unit entry-edge scores, never executable PnL or confidence estimates.
- Independently verify exact-q notional/fees/edge against persisted BOOK witnesses and the latest received-before-deadline selection; recompute markout from each episode's h0 before collapsing. Tampered arithmetic or an older otherwise-valid book reference cannot pass. Do not trust stored legacy quote-based markout.
- Expose policy/stage provenance material and validate supplied required metadata without inventing it. FIXTURE remains explicitly synthetic and can never receive a public-stage/candidate verdict. The exact launch configuration/CLI, metadata production, durable stage admission and live CAL/HOLDOUT gate are a later slice; report missing prerequisites as DATA_INSUFFICIENT. A concise offline numerical result is required, not a dashboard.

Allowed: Spread calibration.py/report.py/book_chain.py, at most one small dedicated offline evaluation module if that is simpler than expanding legacy diagnostics, a minimal export in __init__.py if needed, and direct tests/spread_shadow tests. Builder owns the implementation within these bounds. Escalate concrete evidence/schema conflicts instead of expanding runtime scope.

Forbidden: feed/runner/config/store/CLI/economics/fill/stop/protocol changes; governance edits; legacy strategy changes; other repositories; public/private requests or samples; credentials/signing/orders/trading; generic graph/event/storage frameworks; services; paper trader; threshold fitting. Do not make a historical run into CAL/HOLDOUT. Do not require inaccessible historical runtime artifacts as a substitute for deterministic contract fixtures; any unavailable DG replay must be reported honestly.

Required tests cover distinct risks: transitive order graph and cumulative-fill bridge; quote-version dependence and two arms; late/null-ended gaps and mixed clean/contaminated units; clean no-fill versus unknown/inactive; negative score preservation and the audited rare-loss/negative-sum counterexample; paired collision/distinct/reversed/mixed/unresolved levels without cross-product matching; unit concentration and exact percentile/threshold boundaries; missing/duplicate/partial horizons; exact-q arithmetic witness tampering and stale-selected-book rejection; h0-based markout; common denominator/tie/one-arm qualification; bounded history; malformed provenance and FIXTURE separation; legacy core verdict preservation. Run focused/adverse tests, then freeze/commit and run one fully isolated Python 3.11 final full suite plus dependency/import/compile/surface/scope/diff/Git checks. No system-site-packages environment. Reuse unchanged-tree evidence rather than repeatedly running the full suite.

Report exact final SHA, changed evidence, limits and next action. Never self-accept, merge/push main, or spawn agents. Chief alone accepts. CAL/HOLDOUT and all network observations remain closed after this slice.
