# Active bounded tasks

One slice per venue. Secrets stay outside Git and reports. Private reads and writes run sequentially and never retry an ambiguous operation.

## RISEx — prove official order identity

Status: `BLOCKED — OFFICIAL PLACE/CANCEL IDENTITY MAPPING NOT PROVED`.

- Using only official SDK/API/UI-contract sources, prove how one submitted client identity maps to the authoritative order, resting order, fill, cancel, and position records required by the accepted lifecycle. Do not access credentials or dispatch a request.
- If proved, prepare one isolated Tier C fixture candidate from the then-current `main`. If official sources remain insufficient, preserve the blocker; do not guess or browse support channels.

## Nado — resume public preflight diagnosis

Status: `BLOCKED — OFFICIAL PUBLIC GATEWAY RETURNED HTTP 403`.

- After a genuine external-state change or cooldown, perform exactly one credential-free official public catalog query from a fresh Builder session. If successful, reproduce and correct the remaining op003 catalog mismatch with fixtures; if still forbidden, stop without retry.
- Do not read credentials or create op004 until the public contract passes.

## Extended — accept captured account envelope

Status: `READY FOR TIER B CORRECTION START GATE`.

- From current published `main`, use the captured redacted `{status, data}` descriptor to add one genuine RED and correct only the Extended private-read account parser/fixtures. Preserve fail-closed unknown-field/type handling, isolation, redaction, and all durable one-shot behavior.
- After candidate acceptance and publication, create a fresh temporary Architect and Builder for one new fixed private-read operation. Success requires complete counters, fresh REST/stream agreement, authoritative zero orders, and exact flatness.

## Completion

- After private readiness, each venue receives one separate minimum-notional Tier C lifecycle (`<= USD 500`). Strategy work begins only after all three finish with authoritative zero orders and exact flatness.
