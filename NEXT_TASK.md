# HCR-16 — bounded pre-receiver read latency improvement

## Objective and authority
Owner accepts existing limit-first/resting-check/market-second logic and asks to improve speed and test autonomously. Optimize only independent source/receiver account rechecks after source resting confirmation; keep final exact source-order lookup after both completed. Read overlap may use at most two concurrent account_snapshot operations, each retaining its existing timeout; unchanged total calls/page/poll/resource bounds otherwise. No policy, price, quantity, fees, freshness/delay thresholds, mutation concurrency or nonce changes. No wait-for-fill strategy, auto retry, flattening, new campaign or guarantee of matching own accounts. System/tool restrictions prohibit agent real financial transactions despite owner authorization; offline tests and saved actual-response replay only in this slice. No new live collection required.

One Chief (scope/docs/independent review/full suite/integration) and one explicit-role Luna/max Builder in new isolated codex/spread-v1-read-latency worktree. Chief Astra/medium USER_SELECTED, Luna/max VERIFIED by role when assigned, Standard requested/actual speed UNKNOWN. Builder owns only hood_handoff/engine.py and bounded latency/adverse tests; no other writer. No main merge/push or subagents. Preserve all old candidates/evidence.

## Acceptance
Accepted base c50b2340421b3e6e771d216b35dfda9ea4bd5486. Original owner003 observed source-open to source-recheck1.713775seconds and receiver submit/response0.945468seconds; per-request attribution unavailable. Source and receiver account reads are presently sequential. Demonstrate bounded overlap vs accepted base under identical controlled delays; report modeled saving separately from real venue latency. Preserve limit confirmed active first; both account reads complete before final source order lookup; all identity/position/margin/freshness/resting checks unchanged before receiver dispatch. On either read failure/timeout/cancellation stop and clean pending sibling tasks before subsequent writes/cancellation handling; no orphan tasks or receiver dispatch. Preserve deterministic error reporting and no replay. Test both directions, slow/error/stale/identity/position/early-fill, task cancellation, real-response timestamp/receipt regressions. Full actual diff review, relevant adverse checks and one final clean isolated Python3.11 full suite; source/import/config hashes bound to evidence. Exact unchanged implementation/config from prior checks needs no duplicate suite. Chief can run narrow external synthetic diagnostic probes; production fixes remain Builder-owned.

## Actual state and evidence
Owner003 original UNKNOWN immutable. HCR15 later authenticated read-only verification and offline actual-response replay confirmed source canceled/no fill/position0, receiver+.00020BTC bought at75906.5 from external8151; active BTC orders absent. Owner said receiver remains open; no new current account read or financial lifecycle in HCR16. Previous software HCR14 accepted with4437pass3skip and actual-response replay HCR15 validated timestamp fix. Latency evidence new owner-only spread-shadow-runs/hood-read-latency-20260916/chief-v1. No matching/fill/profit target may weaken checks. Finite completion is accepted latency correction or evidence-backed blocker; no monitor or automatic new trade.
