# HCR-5 — Local read-only readiness command

## Owner-authorized finite outcome

Owner approved BTC quantity0.00020, one pair, source account27331 opensSHORT / receiver27337 opensLONG, API key index4 both. Owner now explicitly asks Chief to implement the proposed local readiness-check mode: check market, positions and margin, display exact available parameters/blockers, keep private keys in process memory via hidden local input. This is software development for later user operation, not authority for agent account/key/trade access. Root remains sole Chief; one NEW minimal-context Builder required for this independent task.

Accepted base main03d5327f2d7e87dbbbb56a472947851334240dfa, paired-opening production2b06a7b09109c2101e722103b93dc007e31fcdb9. HCR-4 public sizing completed2/2 requests: saved BTC minimum.00020, step.00001, indicative notional15.398240USD at observed ask76991.2. Owner approved.00020 after seeing that minimum; those old quotes are not a current order price. No fresh collection authorized for agents; prior HCR-2 gate12/12 also exhausted.

## Acceptance criteria

1. Add a separate explicitly user-invoked READ-ONLY readiness CLI/API path for Robinhood paired opening. It must not require signing/order execution opt-in just to check. Reject combination with execution flags before initializing SDK/key input. Existing help/default/offline preview remains no-network/no-keys, existing order execution behavior unchanged. Minimal setup inputs are selected symbol/quantity/direction, distinct account indices/key index and explicit finite read/freshness bounds. Do not require fabricated incremental-margin values merely to inspect actual account state.
2. Resolve current perpetual identity/status/minima/precision, obtain fresh bounded book and both account selected-market positions/active orders/readiness/balances, authenticate only if read endpoints need it. Hidden local TTY API-key input only in the explicit operator-run check, in-memory only, no secret in config/env/args/report/journal/logs/exceptions. A non-TTY input must fail closed. Use synthetic keys in tests; agents must not initialize real key/auth/signing/account endpoints. Resource lifetimes bounded and clients closed.
3. Report per-check status and actionable reason for unavailable/stale/contradictory inputs: wrong venue/symbol/account, nonflat initial positions, active orders, bad quantity grid/minimum, missing margin proof, inadequate balance. Display exact quantity/directions, metadata and observed bid/ask/notional, source timestamp/freshness. Do not choose live prices, tolerance, leverage or deadlines for trading. Optional already operator-selected order prices may be checked; absence is explicitly UNSET, not a ready-to-trade plan. The check must never silently create a launch-ready order configuration or send orders.
4. Current cross margin and available balance do not by themselves prove incremental opening margin. Derive required incremental margin only if supported by official schema/semantics and sufficient current account/market inputs; otherwise report UNKNOWN/MARGIN_EVIDENCE_REQUIRED with specific missing fields. Separate diagnostic completeness from readiness, never label a partial check READY. Public quote observations may get current transport time, but externally supplied evidence keeps its original time and must not be refreshed automatically. Never manufacture authorization, fee or readiness from a supplied address alone. No forced zero margin, invented exemptions, policy tuning or silent change to accepted trading preflight.
5. Enforce zero order creation/cancel/transfer/withdraw/sendTx or mutation nonce usage on the readiness path. Tests must fail if any forbidden method is reached, including accidental execute flag combinations, failure/error cleanup and auth exceptions. Relevant mocked official-SDK reads, malformed/stale/identity/minimum/margin cases, secret redaction/non-TTY and legacy preview/paired-opening regression checks. Chief independently reviews complete actual diff and distinguishing tests; one final clean isolated Python3.11 full suite before acceptance, no additional skips/unrelated cleanup.
6. Chief README documents a real local invocation, required non-secret settings and what the report can/cannot prove. No promised hidden key-input form before an actual prompt exists, no plaintext key-capture helpers. Terminal visibility issues are separate from readiness logic; do not build UI/dashboard or generic recovery framework. Keep implementation small, reuse relevant adapters while preserving a clear read-only boundary.

## Ownership and delivery

New Builder owns src/risex_spread_shadow/hood_handoff and relevant tests only in new isolated branch codex/spread-v1-hood-readiness / worktree /Users/daniilmakarov/.codex/worktrees/hood-readiness, starting from Chief contract commit. Chief owns AGENTS/SYSTEM_SPEC/STATUS/NEXT_TASK/README and sole integration/push. One Builder maximum; no subagents, no other repo imports, no merge/push by Builder, preserve existing branches. New ignored evidence under spread-shadow-runs/hood-readiness-20260915: raw stdout/stderr/exits/commands/cwd/Python/import/source hashes/exact candidate SHA.

Builder must immediately deliver identity and later final result/blocker directly to Chief native01a096cf-96fc-7f02-b2bb-8bb0a92b8f09 using send_message_to_thread. Chief does not continuously poll. If delivery fails, preserve explicit undelivered packet/path. Luna/max explicit role, Standard requested/speedUNKNOWN. Sidebar visibility must be verified honestly; do not create duplicate work solely to manufacture a card. No accumulated prior conversation or chat credentials passed to new Builder.

No agent real account/private/fee/nonce calls, credentials, signing, signed payloads, orders, funds or new public collection. All development tests use offline fixtures. Final operator setup/test is not executed by agents. Multiple wallets, automatic price selection/position closing and transfers remain deferred.
