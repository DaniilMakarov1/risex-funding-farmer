# Active bounded tasks

One slice per venue. Apply the A/B/C levels and safety core from `AGENTS.md`; operational run IDs are data, not source milestones.

## RISEx — fill-seeking taker lifecycle

Status: `AUTHORIZED LEVEL C CORRECTION AND INVOCATION`.

- The third invocation dispatched once and reached terminal zero-fill/zero-position evidence; after a final-barrier transport stop, two independent authenticated observations and public sweeps proved zero orders/exact flatness and Chief recovered only the local outcome from a protected backup.
- In a fresh visible RISEx Builder worktree, make the smallest venue-local change that uses current testnet liquidity to place a bounded taker opening which can fill, then preserves reconcile/cancel/close and terminal zero-order/exact-flat gates. Current spread and test-asset notional are not blockers. Do not repeat the unchanged no-fill order or weaken identity/no-replay controls.
- After acceptance, Chief runs one sequential lifecycle from a fresh durable run identity and performs recoverable local database recovery as needed.

## Nado — sequential Level C lifecycle

Status: `AUTHORIZED LEVEL C PRODUCT CORRECTION AND INVOCATION`.

- The signed trigger read now uses the accepted 30,000 ms window; the actual write retains its 100 ms contract. Current catalog minima are accepted as published, while the actual target opening and clamped closing amounts remain exactly step-aligned and fully safety-checked.
- All three invocations stopped with zero intents and zero venue writes. Fixed BTC product 2 now has a minimum notional far above USD 500; credentials/private access and signing remain valid.
- In a fresh visible Nado Builder worktree, select the smallest currently executable regular perpetual from the authoritative testnet catalog and bind its exact product identity throughout opening, signing, reconciliation, cancel, close, and final barriers. Current testnet minimum notional and spread are not blockers and have no fixed USD ceiling. Preserve fixed environment/account, durable identity, exact step checks, no replay, zero-order, and exact-flat contracts.
- After acceptance, Chief runs one sequential lifecycle; an ambiguous write still stops without replay.

## Extended — wallet/API Management setup and lifecycle

Status: `AUTHORIZED TESTNET ACCOUNT SETUP AND DIAGNOSIS`.

- Chief may use the existing browser wallet to connect to Extended testnet, provision/reprovision testnet-only API access, and request faucet assets if needed; secrets remain outside Git, command arguments, logs, reports, databases, and fixtures.
- In a separate visible Extended Builder worktree, prepare only the smallest venue-local correction supported by the resulting official UI/SDK/stream evidence. After account-stream Level B passes, Chief may run one sequential minimum-executable Level C lifecycle ending with zero orders and exact flatness.

## Completion

- Each venue receives one separate smallest-executable Level C lifecycle without a fixed USD ceiling. Strategy work begins only after all three finish with authoritative zero orders and exact flatness.
