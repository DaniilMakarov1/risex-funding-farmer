# A/B/C saved-pilot comparison — AUTHORIZED 2026-09-09

## Owner instruction and finite objective

Native owner message in auditor task01a08558-e1c5-72d2-ac85-c8245928cd65: “Давай сравним предложенные тобой 3 варианта. Для них ведь не нужны доработки?” Chief independently read the message and the prior three proposed variants. The owner was told A/B use existing kernel settings while C requires bounded repricing/cancellation implementation. This authorizes one finite same-input comparison, not parameter optimization or a new campaign. The previous STOP is superseded only for this task.

A: unchanged accepted120s/fixed-exit baseline, reuse hash-verified COMPLETE outputs. B: only max_hold_ns=300000000000; existing kernel/policy. C: only opt-in causal whole-cycle break-even exit repricing below, max_hold120s. Run all4existing fill/delay alternatives and all764identical conditional starts/317initial eligible each. No changing entry rules, size/cap, fees/stress, freshness/skew/delays, minimums, funding/FLAT, resource caps, entry cutoff765s, recording end or original market data. Original input SHA256 ecf9ef41de6891bcc8fc75f03516651c28243f95e4b0692a6e2e9056b82fbc01. No new collection, account/private/fee endpoints, credentials or orders. A prior closed53subset is not the comparison population.

## Roles and isolated ownership

Chief01a08506-2b07-7353-980a-6280c76629d6 remains sole scope/governance/reviewer/integration/push owner, Astra/medium. Reuse visible Builder01a08507-854f-78b2-951e-ac786c0c42ef, Luna/max, one production/test writer. Actual model/effort VERIFIED client turn_context on assignment; Standard requested, speed UNKNOWN. Auditor01a08558-e1c5-72d2-ac85-c8245928cd65 remains independent Astra/max analyst and owns only adverse expected cases/comparison review in separate evidence. No new tasks or internal agents; no global settings changes. Messages/results/blockers must be delivered to Chief and final result to auditor, who answers the owner in that task.

Builder owns bounded C implementation/tests (primarily s1b.py and separate opt-in research module if preferable; touched interfaces only where necessary), and B/C invocation/export harness in owner-only evidence. Chief owns governing files and independent acceptance/economic checks; auditor has no production writes. Preserve old branches; Builder switches clean8e86worktree to named codex/spread-v1-abc-exit-candidate from the published contract main. For B calculation concurrently with C edits, Builder may create one separate immutable clean baseline worktree/branch codex/spread-v1-abc-b-baseline, bound to accepted source with only contract docs; no code changes there and no mixed imports. No concurrent writers to a path. No Builder merge/push/main changes.

## Frozen minimal C candidate — C_BE_REPRICE_V1

Default/off behavior remains the exact accepted A behavior. C is a separately identifiable opt-in offline research mode, not a new live policy. It never changes entry admission/readiness, accumulation/cancellation or initial sizing. Initial exit preparation and subsequent updates may use the C price only after the entry barrier and when the entire remaining episode is a known executable balanced pair: signedRISEx=-Q, signedLighter=+Q, Q>0 on both operation grids/minimums; no unmatched/subgrid residue, unresolved execution, pending entry hedge or pending exit close. Actual completed partial exits/hedge closes may leave a smaller balanced Q.

Let K be cumulative actual signed fill cashflow minus all already charged fees/stress for the episode. Let N_L(Q,t) be exact executable SELL notional from currently available, fresh, causally processed Lighter depth for Q, respecting already consumed liquidity/reservations. Let fL=0, fR=0.0001 and sR=0Primary/0.0001Stress. Candidate maker BUY price is min(floor_to_RISEx_tick((K + N_L*(1-fL))/(Q*(1+fR+sR))), currentRISExBestAsk-tick). All required observations must be admissible/current; tick/minimum/notional checks apply. No re-addition of original entry margin, costs or future pending close proceeds. This is an observed execution-only break-even estimate, not a guarantee against delayed price movement or funding. Target0is the sole numerical candidate; no parameter search or extra buffer invented.

If a whole-cycle proposal is not well-defined or not valid, annotate the exact reason and retain accepted baseline paired-reduction behavior at initial placement; when an old quote already exists, retain it and its normal deadline rather than fabricating a replacement. In unmatched/dust domain explicitly mark C_OUTSIDE_BALANCED_DOMAIN; never silently call the episode break-even or suppress independently executable paired reduction. Pending closes continue under normal timing/reservations; no economic repricing until settled, then use actual remaining positions/netcash. Missing/stale data never cancel or reset obligations by themselves.

Repricing proposals occur only on the existing collection-start integer-second decision grid after equal-time processed inputs, no more than once per second (including initial placement/last price decision). Only a changed valid price may request replacement. The old quote remains active through the existing effective-cancel interval and its fills must still count. Only after cancellation is effective, no outstanding conflicting close reservation, and fresh admissible data, may a new version be placed on a subsequent eligible decision boundary with the full original scenario activation delay and post-only check. Re-evaluate quantity/price from actual state; do not submit the stale pre-cancel plan or overlap effective orders. Deadline has priority, is first fill+120s and is never reset by requotes/partials; new exit versions cannot evade terminal completion. Existing min/residue/no-reuse/exact-flat remain mandatory. Trace immutable version ids, source refs, price/quantity, decision/activation/cancel/request/effective times, reasons and fill attribution for every version. No generic order service or broad refactor.

## Execution and acceptance sequence

1. Freeze input/source/import/config/argv and exclusive outputs in abc-comparison-20260909 (Builder subdirectory) before each run. Reuse only reviewed adapter mechanics; B changes constructor policy and deadline assertions, C additionally calls the explicit causal repricing boundary and exports version evidence. Small saved/constructed fixture validation precedes a full variant. B may execute once on frozen baseline while C implementation proceeds elsewhere. Preserve first errors and terminal COMPLETE/failed states, no prefix stitching or automatic full retry.
2. C requires exact candidate SHA/full actual diff review by Chief, independent adverse verification, and one final clean isolated Python3.11full suite plus relevant interface/integration checks before its full market calculation. Preserve secret/funding/no-replay/reconciliation/exact-flat regressions. Expected numerical values must not use tested math helpers. No unrelated repairs/skips; classify environmental failures against exact base with affected tests only. Code changes invalidate affected evidence. Default baseline compatibility must be demonstrated with meaningful unchanged fixtures/material outputs, not an unnecessary full A replay.
3. Required distinguishing cases include actual net K/negative costs/stress charged once; settled partial recomputation; pending proceeds excluded; dust fallback; old-price fill during cancel; no premature new fill; activation crossing/UNKNOWN; stale/gap recovery; full deadline during cancel/activation; no duplicate volume/depth; exact final/pending/unknown inventory. Builder does not self-accept or run C full pilot before Chief acceptance. One consolidated correction per new proven cause, no endless fixes.
4. Authorize one complete B four-alternative run and, after C acceptance, one complete C four-alternative run on the immutable pilot. Full run need not be fast or profitable. End-of-record300s censoring remains explicit; never extend data/end/cutoff. If a material failure makes calculation unreliable, stop it and preserve exact phase/time/actions/versions/positions/pending/event/index/traceback, then bounded diagnosis or report specific blocker.
5. Reconcile all A/B/C rows against same start/scenario/model keys: closed net/gross/fees/stress, NORMAL/FORCED and actual exit method, all exact open positions/dust/pending/UNKNOWN, durations, worst closed result and separately available open marks, transition/paired comparisons and end-window censoring. No sum of overlapping-start PnL, no survivor-only selection, no statistical independence/profitability claim. General economic confirmation requires different evidence and is not this task.

## Deliverable / stopping

Russian finite comparison report, machine-readable complete/paired tables, reproducible scripts/logs/commands/exits/source/config/input/output hashes and durable completion, candidate acceptance or exact blocker. Owner-only outputs are versioned and originals immutable. Chief publishes only independently accepted code and current docs, verifies remote main, and sends results/paths/hashes/qualifications to auditor task01a08558-e1c5-72d2-ac85-c8245928cd65. Stop after delivered complete comparison or a concrete evidence-backed blocker. No new collection/tuning/campaign/real execution or additional general infrastructure is implicit.
