# HCR-30 — owner-operated Telegram control

Owner explicitly requested a Telegram bot to launch real-order cycles. This authorizes implementation of an operator-controlled interface, overriding the previous frozen Telegram scope only for this new hood-handoff module; old Funding Farmer Telegram stays frozen. Chief implements/tests/self-reviews alone. Base: `586b1c0b21d7b40bd59d6e033fd725fb93759da9`.

Deliver local provisioning and run commands; private-chat owner-ID allowlist, explicit `/run` for exactly one existing configured cycle, `/status` and saved `/report`; durable duplicate/stale-command protection, single-instance and launch exclusion, persistent uncertain-start barrier, no automatic replay or unsafe process cancellation. Bot token stays in native macOS Keychain with hidden local input; trading credentials stay in existing Keychain. Never accept credentials or arbitrary config/shell commands via Telegram. Child execution uses existing simple launcher and risk policy. Provisioning/run require owner-supplied user ID and token locally; development does not connect to Telegram, read credentials or execute trades.

Use official Telegram Bot API documentation and observed repository contracts. Offline injected transport/process tests must cover unauthorized/group/forwarded/stale/duplicate commands, crash boundaries, report uncertainty, process/lock ownership and secret containment. Full clean isolated Python 3.11 suite required; preserve original inputs, test evidence and worktrees. Integrate exact tested source and verify remote main. Evidence: ignored owner-only `spread-shadow-runs/hood-telegram-20260922/`. No global settings, external hosting, new strategy, sizing/fees/threshold changes, withdrawals or account-wide controls.

## Implementation result

Candidate `8edad04ce6a94cbd9bb99b7af540771d2ab53315`: 4792 passed, 3 existing optional Extended dependency skips in isolated Python 3.11.5 (138.35 seconds). Exact tested imports and clean checkout are recorded in the evidence directory. Review is Chief self-review. Owner separately authorized storing the provided bot token and reading incoming Telegram updates; token was stored/verified via native Keychain, with no token in files, argv or logs. Read-only discovery identified @funnding_bot but initially returned no private chat. Deployment needs the observed/supplied owner ID and local operator start. No trade execution, automatic bot deployment or old Telegram integration is authorized by offline acceptance.
