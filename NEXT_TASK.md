# SS-001R — Validity-Aware Dependence-Unit Profitability Report

Status: `ACTIVE / ONE FRESH PROJECT-LINKED SPREAD BUILDER`.

Objective: implement only the report/evaluator evidence needed to compute the prospectively frozen System Specification 0.19 CAL/HOLDOUT decision contract without opening or simulating either sample.

Exact base: published `main` containing the prospectively frozen contract. Verification level A. Create the Builder through saved project `RISEx Spread Shadow` in one fresh `codex/spread-ss-001r-*` worktree. The Builder must report project path, worktree, branch, exact base, and clean status before edits.

Required behavior: derive validity only after accepted episode-local gap classification; construct venue clusters and transitive dependence units from exact order identities; collapse repeated quote versions/shared trades/both arms once per unit; emit per-arm clean cluster/timestamp counts and unit-based one-/five-minute concentration; emit strict-only paired collision/distinct/reversed/unresolved counts; emit cluster-collapsed narrow/wide horizon edge and markout distributions using the frozen percentile rule; emit exact-q full-hedge shares; compute the common-denominator `300 ms` selector score; emit the exact configuration/fee/stage fingerprint and fields needed to prove later holdout separation; and return a deterministic fail-closed threshold evaluation with named failed gates. Preserve raw/contaminated evidence visibly.

Required tests: transitive shared-order dependence; repeated quote versions; both nominal arms in one unit; clean versus contaminated episode admission after gap classification; collision/equality, one-tick distinct, reversed, mixed, malformed, and unresolved levels; unit rather than quote concentration; exact-q partial hedge rejection at every horizon; worst-value collapse; exact p05/median boundaries; every frozen economic threshold boundary; common-denominator zero for no fill while retaining negative values; tie-to-`1 bps`; fingerprint mismatch; missing/duplicate horizon/fill identity; deterministic ordering; and unchanged DG-006/DG-007 legacy report fields and verdicts.

Allowed files: the Spread offline calibration/report modules and direct focused tests only, plus a minimal public report contract export if required. Forbidden: collection/feed/runtime/store/config/CLI/economics/quote/fill/stop/cap/protocol/venue changes; live/private access; credentials; signing; any sample launch or fabricated CAL/HOLDOUT observation; orders or writes; unrelated legacy code.

Acceptance: focused/adverse tests; deterministic exact DG-006/DG-007 replay proving all legacy canonical fields and verdicts unchanged; one clean isolated Python 3.11 full suite; dependency, compile/import, diff, scope, private/write-surface, worktree, and Git checks. Builder commits but never self-accepts, merges, or pushes `main`. CAL-001 and HOLDOUT-001 remain closed after acceptance; the next slice is the exact frozen runner/config/cap support.
