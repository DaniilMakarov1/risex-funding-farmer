# HCR-6 — secure local credential persistence

## Owner authorization and finite deliverable

On 2026-09-15 the owner explicitly requested persistent API keys so repeated local runs do not require re-entry. Implement optional macOS Keychain storage for the existing separately invoked utility. Keys must never enter command arguments, environment variables, plaintext files, reports, fixtures, logs or Git. Scope is credential persistence and CLI integration; no trading/margin/price/quantity policy changes. Existing hidden-input behavior remains available. Keychain reuse is explicit and bound to exact API origin/signing environment, account index and API key index. First local hidden entry is necessary when no matching stored key exists. Provide safe local replacement/removal; never print a stored key. Keychain denial/unavailability fails clearly without plaintext fallback. Default preview/help/import cannot access Keychain or SDK/network. Agent tests use synthetic injected backends, never real credentials or real Keychain.

Chief 01a0a563-dcbe-7bc0-bcde-f69f1cd3822b succeeds predecessor 01a096cf-96fc-7f02-b2bb-8bb0a92b8f09 under the owner-requested sequential handoff. Chief owns governing documents, review, accepted integration/publication. Maximum one fresh Builder plus Chief; Builder owns production/tests on an isolated branch, cannot spawn/merge/push main, and delivers directly to Chief. Chief Astra/medium USER_SELECTED, Standard requested/speed UNKNOWN. Builder explicit builder role Luna/max; actual speed UNKNOWN.

## Acceptance

Review full candidate diff and provenance. Distinguish correct reuse, first-use storage, identity/environment isolation, replacement/removal, denied/unavailable backend, secret-bearing exception sanitization, no input echo fallback, and no credential access from preview/help or invalid configuration. Verify readiness and operator-run execution provider integration offline with synthetic keys and no real mutations. Run affected-interface/safety tests and one final clean isolated Python 3.11 full suite on final source. Original accepted base 2bcfb23ae3da91db6d23795621d4339a8c226bf0; previous 4331/3 suite remains historical unchanged-source evidence.

## Pending work and live boundary

Owner explicitly states allocated balances are test-only and requests testing orders/positions. Exact test interface/API URL and its no-monetary-value identity remain to be verified; no account/key/signing/order calls yet authorized by this implementation slice. No new public market collection. Official documentation research allowed. Any verified valueless test execution needs a finite contract binding network, quantities, maximum actions and cleanup; never infer unlimited authority or reuse historical gates. Real financial transactions remain outside agent execution. Margin-proof work remains pending and prices UNSET; credential persistence does not resolve trade readiness or select prices. Supplied readiness report established both account reads PASS, BTC positions/orders empty at report time, overall UNKNOWN due to missing incremental margin and UNSET bounds.
