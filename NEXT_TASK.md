# HCR-8 — explicit operator deferral of local incremental margin calculation

HCR-8 review checkpoint: candidate1bbf3e265bd9126945855089e0a9f6eb4cfae2a8 received directly; full diff reviewed. CHANGES_REQUESTED for sdk.py normalization hiding malformed supplied incremental values when provenance absent, newly admitted by deferral. Independent actual-adapter synthetic probe reproduces -1/NaN/malformed -> None. Same fresh Builder assigned bounded correction and explicit rejection/restart evidence; no formal REJECT, no integration/full suite yet. Packet original-root spread-shadow-runs/hood-margin-deferred-20260915/chief-review-v1. Next action on direct corrected delivery; no periodic monitoring.

## Current owner decision

Owner explicitly selects existing Robinhood Mainnet, describes allocated balances as an expendable test budget, and requests initial-margin calculation be deferred to test the strategy sooner. This supersedes the prior testnet-choice question and mandatory local incremental calculation only within this finite operator-utility change. Mainnet funds remain real financial assets; agents must not access actual keys/accounts, prepare real signed payloads, send/cancel orders or manage actual positions. All development and verification use synthetic offline data. Owner locally runs any later financial execution. No further request to prove this is testnet.

## Finite implementation and invariants

Provide an explicit opt-in configuration/CLI path for operator-run paired opening that defers ONLY local incremental opening-margin proof/calculation. Default remains strict. Preserve null/unknown/not-calculated evidence honestly; never fabricate zero requirements or label unchecked margin PASS. Readiness may explain the deferral, but must distinguish observed checks from unperformed margin calculation; execution_authorized remains false. The new choice must propagate through series child configuration and durable identities/receipts so a restart cannot silently change margin policy. No new calculator or unit inference required.

Keep existing exact accounts/market/environment, quantity/grid/minima, fees, price/time/freshness bounds, authorization checks, existing margin/balance checks, selected position handling, order reconciliation, no-replay and persistence semantics. No automatic price/size/leverage changes, new funds moves, blanket bypass or retries after an exchange rejection. An exchange margin rejection is a failed attempt, never a fill or successful strategy. Preserve default preview/help and existing Keychain behavior. A missing initial-margin estimate must no longer block the explicit deferred path; malformed supplied values remain rejected. Existing required market/minimum metadata provenance must not become fabricated margin proof.

Do not broaden scope to absent-position semantics, platform migration, testnet support, fresh account collection or strategy changes. Surface a concrete pre-existing interface dependency only if it prevents this finite path. Chief handles operator documentation; Builder supplies exact usage and limits. Never invoke live paths with real credentials.

## Ownership and acceptance

Sole Chief01a0a563-dcbe-7bc0-bcde-f69f1cd3822b owns docs, independent review and integration. Fresh Builder margin_deferred_builder role Luna/max (role settings verified, speedUNKNOWN/Standard requested) owns hood_handoff production and affected tests only in /Users/daniilmakarov/.codex/worktrees/hood-margin-deferred on codex/spread-v1-margin-deferred, starting7168928. Prior keychain_builder is not reused. Chief edits no production code; no concurrent writer to Builder paths. Builder does not spawn agents, merge or push main. Direct result delivery to Chief via native messaging required, no periodic monitoring.

Deliver clean committed candidate with exact SHA/hash/provenance, focused and affected distinguishing tests and exit logs in ignored spread-shadow-runs/hood-margin-deferred-20260915/builder-v1. Test strict missing-estimate block vs explicit deferred success using fake SDK, propagation and restart policy binding, malformed estimates, unrelated blockers, exchange rejection/no replay/no false fill, offline/default/Keychain isolation. Chief reviews full actual diff and adverse behavior; one final clean isolated Python3.11 full suite before acceptance. Do not run a dirty precommit full suite. HCR-7B accepted candidate0582c581/maina4d1d29 evidence4362 passed3skipped remains unchanged baseline.

Completion is accepted/published offline utility plus concrete operator instructions and honest remaining execution limitations, not actual strategy profitability or live execution proof. No Goal, watcher or repeated Builder polling. Future independent tasks also require fresh Builders.
