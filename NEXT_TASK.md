# S2 — One-policy complete-cycle kernel, offline-only

Status: AUTHORIZED after independent S1 acceptance; no market launch.
Venue: central SPREAD, RISEx/Lighter public data only.
Objective: one small explicit cycle kernel shared by fixtures, suitable replay and later shadow, with exact positions, fill/fee/cash-flow ledger and every residue retained.
Accepted implementation: `05b46eb7a76a87509b4cb3f7d020f9c263772e82` (S1). Start from exact published main containing this gate; Chief dispatch supplies its full SHA.
Verification: Level A. Owner's 2026-09-05 authorization covers this sequential slice. S3 and observation remain inactive.

## Allowed scope and policy

- Existing Spread package and focused tests only. Reuse accepted public data models, causal quote contract and exact sizing/VWAP/fees. Ordinary functions and small explicit states; keep policy choices apart from execution and ledger. One kernel, no parallel exit engine or generic OMS/event framework.
- BTC, target $100, RISEx maker SELL entry and Lighter taker BUY hedge, entry target edge 1 bp, at most one pair per alternative scenario. New decisions at most once/second and only when both positions and every pending action are flat. Size from Lighter best ask, floor to common step, verify both venue minimums, then lock target quantity and maker price for the attempt.
- Immutable entry quote; request cancel at five seconds after activation or first partial. Primary activation/cancel/taker delays 500 ms; stress 1000 ms plus 1 bp on every RISEx fill. Fees: RISEx maker 1 bp/taker 3 bps (recorded SS-001Q), Lighter Standard taker 0. Economic input freshness and receipt skew maxima 500 ms. These are configured hypothetical assumptions.
- Count each eligible maker partial during active/cancel windows exactly once. Reuse S1 timing, source identities, uncertainty and block-fence semantics. Processing-ready governs reaction; delayed processing cannot erase fills already received in the active interval. Capture actual decision-ready in whichever offline driver produces decisions; never substitute earlier book receipts or use future books. No ambiguous possible fill becomes clean NO_FILL/FLAT.
- After entry effective cancellation, hedge accumulated quantity after taker delay (a fully filled entry has no remaining maker quantity to cancel). Fill only grid/minimum/depth-executable quantity. Unmatched RISEx residue takes a delayed taker unwind if executable, otherwise remains explicitly unresolved. No silent rounding, unlimited retry or fictitious closing liquidity.
- After hedge, compute one fixed RISEx BUY exit for actual hedged Q: min(floor_tick(LighterSellNotional(Q)*(1-fL)/(Q*(1+fR))), RISExBestAsk-tick). Apply the same hypothetical activation and post-only eligibility contract. Invalid eligibility routes to forced unwind; no expected exit profit is booked in advance.
- First exit partial requests cancel; its corresponding Lighter quantity closes after delay; remaining paired quantity unwinds after effective cancellation. Max hold is 120 seconds from the first maker fill: cancel outstanding maker quote, then force out remaining positions. Account for every late partial and pending action without duplicate closes or new entries during closing. No position-increasing over-close.
- Ledger records each hypothetical fill, venue/side, exact quantity/notional, maker/taker fee, reason and evidence/timing identity. Completed execution-only PnL is signed cash-flow sum less all fees including unmatched/forced unwinds; an incomplete cash flow is not terminal PnL. Track turnover, holding/occupancy and unmatched exposure duration, normal/forced/aborted/unresolved states for S3 reporting. Primary/stress remain alternatives, never summed.
- Funding stays UNKNOWN; no funding accrual engine or full-profitability claim. Gap, stale/missing required book, uncertain order, non-executable residue or terminal pending state yields explicit insufficient/unresolved evidence. Bounded halt is sufficient; no speculative recovery.

## Acceptance and required evidence

- Deterministic fixtures cover positive and negative full cycles, partial entry and exit, delayed queued fills, duplicate volume, insufficient depth, distinct grid/minimum residue, required-action gap/staleness, max-hold cancellation/unwind, terminal unresolved and exact FLAT barrier with zero pending actions.
- A small independent checker derives all signed fills, per-venue fees, cash flows, quantities, residual positions, turnover and complete-cycle PnL without production arithmetic/ledger helpers. Include forced/unmatched negative contributions and both primary/stress. Demonstrate positive entry edge with a negative complete cycle.
- Prove the same kernel produces identical outcomes under fixture streaming and compatible replay inputs, including deterministic boundary ordering and unavailable/future book rejection. Historical records missing S1 timing/identity are not silently upgraded.
- The used event/time advancement path must update known fills, positions, pending actions and completion as evidence arrives. Buffering all events and calculating only at finish is not streaming. Replay drives those same transitions. No event prefix advances time to a future scheduled boundary; no committed fill/action is retroactively erased or duplicated. After completion, reject an entry decision timestamp inside the preceding cycle, and keep unresolved exposure halted. Include prefix-level assertions and successive entry decisions while active, pending and truly flat.
- Focused/adverse tests during development; commit final candidate first, then one clean isolated Python 3.11 full suite and dependency/import/compile/private-write-surface/diff checks on that SHA. No redundant unchanged historical CAL replay.
- Before edits report root/branch/HEAD/status. Final checkpoint reports exact SHA, changed evidence, known limits and checks; Chief alone accepts/integrates/pushes main.

## Forbidden scope and stop

No new public market requests, credentials, private/fee-reader/signing/order/dispatch paths, legacy scanner/broker/lifecycle/storage/funding strategy imports, dependencies, framework, CLI/report campaign integration, resource-envelope implementation or governance edits. Preserve every CAL/DG evidence/claim/report and historical semantics. Official documentation read-only study is allowed only for a concrete contract question.

Fresh visible GPT-5.6 Luna max implementation Builder on `codex/spread-s2-causal-corrections`, separate worktree from exact published main containing this gate; only one Spread Builder. Predecessor is stopped. Candidate `9173409d04d3e3751057238ee39a47b7bf1a6c4c` is formally REJECTED and immutable. Its two in-project implementation commits `a8af1a445cb162cf133b70316667ce232474db3b` and `9173409d04d3e3751057238ee39a47b7bf1a6c4c` may be cherry-picked as unaccepted starting code, preserving history. Do not reuse the superseded batch draft.

This correction is limited to three independently reproduced causal defects and their regressions:

1. Continue classifying relevant delayed evidence after a modeled terminal result and across subsequent admission. An old entry fill received inside its active/cancel interval but normalized after the cycle closes must invalidate false authoritative NORMAL/FLAT and retain explicit possible exposure/uncertainty. Replay must not silently discard trailing records. Preserve committed actions, quote identities, deduplication and halted admission; use a bounded solution, no general recovery framework.
2. Enforce accepted S1 decision-input witnesses on the actual S2 admission path: exact two-book identity/session/recovery/revision bindings, readiness no later than decision, freshness/skew, and required historical metadata. Missing books, mismatched revisions, not-yet-ready books and stale books currently all produce false NORMAL cycles. Reuse accepted causal validation; later valid books cannot repair an invalid initial decision.
3. Select the latest eligible book available at the scheduled action time. Recording a later book must not shadow an older valid book at an already due boundary. Demonstrate event-triggered advancement and explicit clock advancement agree; keep rejection when no eligible book exists.

Preserve the existing sequential prefix/full-cycle/adverse/numerical evidence and policy above. Add one meaningful regression per distinct risk, including late terminal evidence in both streaming/replay and after a next admission. Run focused/adverse checks during work, commit final candidate, then one isolated Python 3.11 full suite and required dependency/compile/import/surface/Git checks on that exact SHA. Report exact tip and evidence; do not self-accept or edit governance/main. Stop for a concrete conflict or non-convergence; prefer a substantive clean checkpoint before context grows. S3 and market observation remain inactive pending independent S2 acceptance.
