# Active bounded tasks

One active slice is allowed per venue. Private-read operations and testnet order lifecycles are sequential. Never place secrets in Git, chat, arguments, logs, or reports.

## RISEx — fresh private-read invocation

Status: `READY FOR GOVERNANCE/PRE-ARM; NO PRIVATE CALL AUTHORIZED`.

1. Define one new fixed invocation ID and unused fixed store path; never reuse or inspect the consumed operational store as retry authority.
2. Perform metadata-only pre-arm: accepted `e6bb562` tree, fixed official endpoints/identities, absent store, exclusive process, credential source ownership/type/mode, and redacted durable counters.
3. After Chief operational acceptance, execute exactly one existing bounded private-read sequence. No retry, order, cancel, close, or generic transport surface.
4. Accept only complete public A, single credential/sign/private observation, public B agreement, authoritative zero orders/exact flatness, and terminal durable evidence.

## Nado — provision and run private-read preflight

Status: `BLOCKED — FIXED IDENTITY/CREDENTIAL CAPABILITY ABSENT`.

1. Securely provision the fixed owner identity and 32-byte sign-only capability as current-user-owned regular `0600` files outside the repository. Do not disclose their values to Codex or chat.
2. Run metadata-only pre-arm against the unused fixed invocation/store.
3. After Chief operational acceptance, execute the sealed one-shot read-only Query and require complete durable counters, public-round agreement, zero regular/trigger orders, and exact flatness.

## Extended — provision and run private-read preflight

Status: `BLOCKED — FIXED API KEY/IDENTITY CAPABILITIES ABSENT`.

1. Securely provision the fixed API-key capability and account/subaccount identity as current-user-owned regular `0600` files outside the repository. Do not disclose their values to Codex or chat.
2. Run metadata-only pre-arm against the unused fixed invocation/store.
3. After Chief operational acceptance, execute the sealed one-shot REST/WebSocket private-read sequence and require complete durable counters, round/barrier agreement, zero orders, and exact flatness.

## Transition to real testnet lifecycles

- After a venue passes private-read readiness, formulate its separate Tier C minimum-notional one-shot gate.
- Execute venues one at a time with official exact order identity, no blind retry, and notional `<= USD 500`.
- Success requires bounded place/reconcile/cancel/close evidence and a final authoritative barrier proving zero open orders and exact flatness.
- Only after all three venues pass may a separate strategy-testnet integration task be opened.
