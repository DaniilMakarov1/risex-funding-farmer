# Active bounded tasks

One active slice is allowed per venue. Private-read operations and testnet order lifecycles are sequential. Never place secrets in Git, chat, arguments, logs, or reports.

## RISEx — bounded testnet live-write lifecycle candidate

Status: `READY FOR PHASE 0 / CHIEF START GATE; NO BUILDER OR LIVE WRITE AUTHORIZED`.

- This governance slice starts from exact published `867ac006c7f92bbbf9529ff4385b7ae1f08036fe`. After it is centrally accepted and published, Phase 0 must start from the exact Chief-confirmed new `main` and revalidate only the official RISEx testnet order/cancel/read contract needed to bind the accepted fixture lifecycle and operational capabilities into one isolated Tier C candidate. Declare exact files, fixed identity/store, counters, redacted evidence, and stop states. Stop for the combined Chief design/Builder gate before implementation; no credential or private/live action belongs to Phase 0.
- Builder after that gate: use synthetic credentials, fixture responses, and disposable stores only. Preserve testnet-only exposure `<= USD 500`, the current official BTC minimum, official tick/step/fresh BBO/depth gates, and the existing accepted price bounds. Permit at most one price-bounded FOK opening; after an observed fill permit at most three fresh-authoritative-position-derived close intents, FOK first and only fresh-state price-bounded `LIMIT+IOC` thereafter. Permit at most one cancel for one exact known experiment order ID.
- Every intent must have a new durable client/order identity, nonce and digest before dispatch. Reconcile place/cancel/close through exact official identity and fresh authoritative order/fill/position state; ambiguity, timeout, disconnect, or an unresolved dispatch never permits blind retry, identity reuse, quantity enlargement, rounding, or mutation of an unrelated order.
- Candidate gate: genuine RED plus focused adverse fixtures must prove preflight-before-secret access, intent-before-dispatch durability, exact identity reconciliation, bounded attempts, interruption/restart, redaction, and the success, no-fill-flat, and manual-recovery terminals. Chief independently accepts and integrates the exact candidate after its clean Python 3.11 full suite and dependency check. No normal Farmer import or Scanner/runtime/repository/economics/strategy/Telegram change is allowed.
- Separate operational Chief gate: metadata-only pre-arm must verify the exact accepted tree, one unused fixed store, process exclusivity, fixed testnet identities/endpoints, and credential capability metadata without reading it. Only then may one sealed lifecycle run occur. Success requires an observed opening fill, every intent terminal/reconciled, authoritative zero open orders, and exact flatness. Terminal no-fill-flat is safe but does not prove lifecycle acceptance and authorizes no retry. Any residual, unknown order/position, stale or contradictory evidence, exhausted close/cancel budget, or unresolved dispatch stops all writes as redacted `FAILED_HALTED_MANUAL_RECOVERY`; it is failure, never operational acceptance or strategy readiness, and is never blindly retried.

## Nado — provision and run private-read preflight

Status: `BLOCKED — FIXED IDENTITY/CREDENTIAL CAPABILITY ABSENT`.

1. Securely provision the fixed owner identity and 32-byte sign-only capability as current-user-owned regular `0600` files outside the repository. Do not disclose their values to Codex or chat.
2. Run metadata-only pre-arm against the unused fixed invocation/store.
3. After Chief operational acceptance, execute the sealed one-shot read-only Query and require complete durable counters, public-round agreement, zero regular/trigger orders, and exact flatness.

## Extended — provision and run private-read preflight

Status: `BLOCKED — FIXED API KEY/IDENTITY CAPABILITIES ABSENT`.

1. Securely provision the fixed API-key capability and account/subaccount identity as current-user-owned regular `0600` files outside the repository. Do not disclose their values to Codex or chat.
2. Run metadata-only pre-arm against the unused fixed invocation/store.
3. After Chief operational acceptance, execute the sealed one-shot REST/WebSocket private-read sequence and require complete durable counters, round/barrier agreement, zero orders, and exact flatness.

## Transition to real testnet lifecycles

- After a venue passes private-read readiness, formulate its separate Tier C minimum-notional one-shot gate.
- Execute venues one at a time with official exact order identity, no blind retry, and notional `<= USD 500`.
- Success requires bounded place/reconcile/cancel/close evidence and a final authoritative barrier proving zero open orders and exact flatness.
- Only after all three venues pass may a separate strategy-testnet integration task be opened.
