# HCR-19 — resilient pre-mutation repricing and simple operator launch

## Completed owner-authorized objective

On 2026-09-20 the owner explicitly authorized the previously proposed finite implementation: make an ordinary public-quote move before the first order refresh the opening price instead of terminating the cycle; allow at most three safe preparation attempts before any mutation; provide one short `./start` operator command; and replace raw JSON-first output with concise, intuitive Russian progress/error messages while retaining complete durable evidence. This is offline software work only. It grants no live account read, credential, signing, order, cancellation, historical-position adoption or execution authority.

## Ownership and base

The accepted candidate is `ebabbfaac88ca6d69aef9b15ad396414871e641a`, produced by fresh visible Builder task `01a0bd69-2c26-7ca1-8f9e-74784ed594a0` on branch `codex/spread-v1-simple-launch`. Sole Chief task `01a0aafe-23fa-7c90-87a6-e807b3f6450a` independently reviewed it and owns integration/publication. There is no project-wide Builder concurrency cap; each new finite objective still uses a fresh Builder, and concurrent Builders require non-overlapping ownership. Prior and delayed alternate Builders are completed/archived and must not be reused. No global settings, credits or resets.

## Required behavior

- Draw the random quantity and 20..300-second hold exactly once after the first valid preparation. Preserve their integer ticks and durable evidence across all pre-mutation refreshes. Never redraw, silently shrink or increase quantity merely because the quote changes.
- Immediately before opening, use fresh official metadata, public book and both account snapshots. Recompute the existing one-tick opening price rule from that fresh book. An ordinary quote change is accepted and durably records old price, new price, attempt number, observation times and refreshed admissible bounds. Build the opening plan with the accepted fresh price and same selected quantity.
- Revalidate the selected quantity against fresh venue increments/minimum base/minimum quote and the smaller fresh available balance at the accepted price. Preserve exact Decimal/integer-grid math, flat positions, no selected-market active orders, identity, freshness, readiness, authorization and margin checks. A quantity that is no longer legal must never be submitted.
- Allow at most three total preparation attempts, and only before the first mutation has been admitted. Retry finite transient read/timeout/stale-data or changed-preparation facts that can safely become valid on a fresh complete snapshot. Pace retries with the existing poll interval. Every attempt and concise reason is journaled. Do not retry deterministic configuration/identity/authorization/non-flat/pending-order/missing-minimum/invalid-grid failures. Do not promise success after three attempts.
- Once any mutation may have been sent, the preparation retry loop is permanently unavailable. Preserve existing no-replay, exact-order lookup, maker-resting, reconciliation, cancellation, reduce-only residual closure and UNKNOWN barriers. Never send another opening merely because a write response is missing or ambiguous.
- Preserve the consumed failed `hood-cycle-race-latency-20260920/operator-v1/cycle-001`; never clear or reuse it. The simple launcher automatically chooses and atomically claims a new owner-only cycle directory and unique persisted client-order prefix, without overwriting any prior run.
- Add an executable repository-root `./start` path for the configured owner cycle. From the project root, one command shows a short Russian summary and one simple confirmation where Enter starts and an explicit cancel input exits. Before confirmation it must not access Keychain, construct the SDK client, read market/accounts or create/consume the cycle directory. Existing full CLI remains compatible for diagnostics and automation.
- Default simple output is concise Russian progress: preparation attempt, selected quantity/hold, accepted price update, first-mutation boundary, opening/holding/closing/reconciliation, and terminal outcome. An error states what happened, whether any order may have been sent, known remaining positions, and the durable journal path/action. Full structured data remains in immutable JSONL and is optionally available through the existing/full-detail interface; never hide UNKNOWN inventory or describe it as flat.
- The observed failure is an evidence fixture: initial opening price `80513.6`, selected `0.00024 BTC`, hold `287`, then quote-change block about 0.589 seconds later, no opening result and no mutation. The second quote was not persisted and must not be invented. New behavior must preserve old and new quotes on future runs and return the known selection on any later preflight terminal result.
- No policy changes to accounts, direction, leverage, fees, venue minima, 20..300 hold range, one-tick pricing rule, freshness/timeouts, order types, reduce-only semantics or execution matching. Do not optimize unrelated code or claim faster exchange fills.

## Acceptance and finite completion — PASS

The Builder delivered a clean candidate and focused coverage for the required retry, repricing, quantity, mutation-boundary, slot-allocation, Russian-output and compatibility paths. Chief reviewed the full actual diff, required one bounded prefix-binding correction, independently replayed the original quote-change boundary and ran the final clean isolated Python 3.11 suite: 4587 passed, 3 skipped, exit 0 in 117.73 seconds. The replay proved one quantity/hold draw, unchanged 0.00024 BTC and 287-second hold, and opening-plan use of fresh price 80514.2 after old price 80513.6. Operator installation identity, `./start --help`, and offline cancellation passed without credentials, private reads or slot consumption; the original cycle-001 journal hash remained unchanged. HCR-19 is complete. No live validation was run, and no new financial action is authorized by this completed software task.

## Current evidence and inventory boundary

Latest consumed HCR-19-triggering run: `/Users/daniilmakarov/Desktop/RISEx Spread Shadow/spread-shadow-runs/hood-cycle-race-latency-20260920/operator-v1/cycle-001/cycle.jsonl`, outcome `FAILED_PREFLIGHT_BLOCKED`, no opening and therefore no submitted order according to the durable phase boundary. Its selection is present in `SELECTION_PROVED` although the terminal response omitted it. Earlier supplied cycle recorded source `-0.00027 BTC`, receiver zero; current account state remains NOT_READ and is not adopted or changed by this task. Fees/funding remain UNKNOWN.
