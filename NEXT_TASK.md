# SS-001K — Exact RISEx Owner-Fee Readiness and One Read

Status: `ACTIVE / ONE FRESH RISEX BUILDER`.

Objective: implement and verify the smallest fail-closed Level-B boundary that authenticates the exact owner account through the existing protected local RISEx mainnet identity/session-signer contract and performs exactly one official `GET /v1/user/fees` read, returning only sanitized fee-tier/rate/provenance evidence needed to freeze `CAL-001` economics.

Exact base: published `main` SHA containing accepted SS-001J. Verification level: B. Before implementation, refresh the current official RISEx nonce/domain/login, session-signer, and user-fees contracts and the current official Lighter Standard fee/latency inputs. Official trade/order or write endpoints are not part of this slice.

Allowed scope: one narrowly named RISEx mainnet fee-read module/runner, direct focused fixtures/tests, and the minimum stable operator documentation required for its protected fixed-path input. Reuse accepted redaction, bounded-transport, EIP-712, and protected-file primitives where their contracts exactly fit. The runner may read only the existing owner-only identity and session-signer paths, call the exact authentication endpoints required to obtain a short-lived JWT, and call `GET /v1/user/fees` for the caller's own account. It may make the initial attempt plus one retry only after a transport failure before a valid observation. Output only sanitized account fingerprint, tier, maker/taker bps, schedule/trial/earned status when present, observation time, endpoint/source provenance, and a classified terminal result.

Required safety: exact mainnet host and chain/domain, exact owner-account binding, registered authorized signer binding, strict validation of every security-critical response, finite timeout, owner-only protected inputs, no secret bytes or raw protected payloads in Git/task/chat/arguments/logs/reports/databases/fixtures/process titles, no unclassified exception, and terminal `TRANSPORT`, `HTTP`, `SCHEMA`, `AUTH`, `IDENTITY`, or `SAFETY` failure. A semantic/auth/identity/safety failure is never retried. No endpoint outside the frozen allow-list may be called.

Forbidden: order preparation, signing of any order/write payload, orders, cancels, positions, collateral, balances beyond identity necessity, transfers, withdrawals, deposits, strategy execution, testnet, any other account, account-wide mutation, fee changes, collection/report/economics changes, `CAL-001`, `HOLDOUT-001`, `SS-002`, and `SS-003`.

Acceptance: official-contract provenance; adverse tests for host/domain/account/signer mismatch, malformed JWT/login/fee payloads, redaction, timeout and permitted retry, retry prohibition after complete failures, endpoint allow-list, and secret/process-argument surfaces; one isolated Python 3.11 full suite; clean diff/scope/Git checks; then one Chief-controlled real Level-B run. A successful read records only sanitized exact-account fee evidence. Any terminal failure closes the gate for a separate bounded diagnostic rather than widening or blindly retrying it.
