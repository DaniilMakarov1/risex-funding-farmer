# SCAN-003 — Fixed Scanner Command and End-to-End Gate

Status: ACTIVE / ONE FRESH VISIBLE SPREAD BUILDER. Verification level A only; no network observations.

Objective: finish the smallest usable command-line public scanner using the accepted feed, observer, store and offline evaluator. No new architecture. Chief supplies exact published base. Fresh codex/spread-scan-003-* branch/worktree; report root, branch, HEAD and clean status before edits.

Required outcome:

- One fixed scanner command for CAL-001 or HOLDOUT-001, plus concise offline JSON/table result. BTC only, RISEX_SELL_LIGHTER_BUY only, $100, 1/2 bps, RISEx maker 0.0001 with recorded SS-001Q provenance, Lighter Standard taker 0 and published 300 ms, horizons 0/300/500/1000 ms. No CLI economics/grid tuning. Preserve legacy smoke semantics and clearly distinguish legacy report from the new scanner result.
- Reuse ShadowConfig/SpreadObserver/PublicFeedRunner/AppendOnlyEvidenceStore with minimal explicit options. Disable material-fill stop for this fixed route. First stop: 250 run-wide eligible trade keys, 1200 seconds, 1,000,000 records, 4 GiB, or fatal/integrity failure. Caps invalidate. Drain only already-pending horizons after freeze. No extension, repeat or parameter change. Do not alter accepted strict-fill/deadline/trade identity semantics.
- Record actual sample-start monotonic and UTC timestamps, stop reason/counts, actual terminal timestamps including drain, fixed configuration and fee provenance. Header cannot pretend to know actual terminal time: bind final terminal evidence when evaluating. Use one canonical producer/evaluator shape. Bind the loaded source to the exact clean accepted release, not arbitrary current-directory Git. Stable policy fingerprint includes release, exact configuration/fees/provenance, formulas, numerical thresholds and stop contract; stage fingerprint adds stage/run identity and actual interval. The existing SCAN-002 policy hash covers only a subset and is not sufficient for public admission.
- Public admission must independently check actual records/terminal/first stop/caps/profile and provenance; metadata assertions alone are insufficient. Distinguish numerical outcome from data validity. Bad eligible units remain in the raw denominator and prevent admission; do not display clean-subset coverage as full-stage completeness. Only valid passing CAL opens a provisional selected arm. HOLDOUT requires the accepted CAL evidence/report reference, same policy/release, a new prospective start window, non-overlapping UTC intervals and no shared exact trade/order identity; selected arm must pass and unchanged selector must choose the same arm. Only both passing stages may emit the section-0.19 candidate label, rendered as conditional public entry edge, not realized profitability.
- Use the smallest durable once-per-stage reservation under the scanner store root before any public request. A claimed/failed/missed attempt cannot silently retry. Chief supplies a prospective start window and exact release after acceptance; no start window is invented by the Builder. Missing prerequisite fails before network. Do not build a general operational journal framework.
- The new CLI report calls the bounded offline evaluator directly, not legacy build_report's retained-history path. Show positive/negative/insufficient evidence, exact conditional mean/sum/minimum/p05 by horizon, gross gains/losses, unit/cluster counts, coverage and named failed gates. Scores are not executable PnL/hour. No dashboard or service.
- Prove the actual observer/store/new-report path using synthetic inputs: positive, loss, incomplete/gap, and stop/cap cases. Fixture/replay mode remains FIXTURE_ONLY even when numerical gates pass. Use accepted existing fixture mechanisms; no new replay platform. Check legitimate inactive QUOTE rows without version IDs and quote replacement against actual runner output: they must not be confused with corrupted active quotes. Any concrete producer/evaluator mismatch is in scope for the smallest repair, with regression.

Allowed: Spread config.py, runner.py, store.py, cli.py, offline_evaluation.py, minimal exports and direct tests/spread_shadow. At most one small scanner-specific orchestration module if clearer than expanding CLI. Existing feed/economics/book reconstruction behavior stays unchanged; escalate a concrete blocker there. Builder may propose operator README text in its final reply, but Chief edits governance.

Forbidden: live/public/private requests or samples; credentials/fee-reader/signing/orders/trading; paper trader; thresholds/formula tuning; other repositories; dashboards/services/frameworks; generic event/graph/storage abstractions; legacy strategy expansion; governance edits; merge/push/main integration; hidden implementing agents.

Verification: focused meaningful adverse tests for fixed configuration and fee binding; disabled fill stop and exact first-stop boundaries; actual caps/terminal failure; release/config hash mismatch; missing/reused stage claim and missed window fail before network; CAL/HOLDOUT separation and same-selector rule; synthetic store-to-report positive/negative/incomplete scenarios and FIXTURE exclusion; real runner record parity and bounded direct report path. Then freeze/commit and run one fully isolated Python 3.11 full suite plus dependency/import/compile/surface/scope/diff/Git checks on exact final SHA. No system-site-packages. Reuse unchanged-tree evidence.

Report exact SHA, verification, limits, CLI usage and proposed short README addition. Chief alone accepts. This implementation slice authorizes no observation; Chief will separately publish the prospective bounded operational gate after acceptance.
